# Orchestrator 6 reconstruction batch Q

Addresses:009E52E0,0064A610,0064B5C0,0064B5F0,00695ED0,00696360,00695F40,00CCD6A0,00BD0400,00923840,009277E0.

Q integrates complete normal raw obstacle-node lifetime, the actual observer dispatch singleton owner, a concrete raw-manager deletion binding, and persistent scene flags in the existing unit owner. Names remain hypotheses; per-routine original ABI, call and producer evidence is in the individual reports.

| Change | Evidence |
| --- | --- |
| [Raw90h node](NATIVE_SHIP_AI_OBSTACLE_NODE.md) preserves83 untouched bytes and registration-before-tail ordering | 20 native/source pairs,355 original bytes,17,300 record bytes per side,zero differences |
| [Dispatch owner](OBSERVER_DISPATCH_OWNER.md) usesE198DC and its actual CRT owner+4 aliasE198E4 | Eight original spans,434 bytes,41 canonical words,actual manager/Win32 locks |
| [Deleting binding](OBSERVER_DISPATCH_BINDING.md) routesCF7E74 through00695F40 | One actual mixed-owner source lifecycle passes; native dangling alias retained |
| [Scene flags](UNIT_SCENE_FLAGS_LIVE.md) retain5C..60 in one stable unit owner | 249-check focused fixture includes46 original bytes and production flag/filter hooks |

MSVC Win32 Release and both existing CTests passed at `e4030de667bd9cc30cbc69ddc1da005f040a1c3a`. The executable SHA256 is `e085488bc3499a384105228bb7662adfe72634678c773278826e3e0fec38b987`. The120-frame USN01 process run finished with18,557 finite trajectory rows,241 unchanged Airfield2 samples,2,400 avoidance queries,1,080 cruise reads,10,080 generic ticks and420 valid world registration nodes. The exact command, hashes and counters are in `reports/orch6_reconstruction_q.json`.

Scene flags retain the existing active/simulate cells and add only missing destroyed/removed/pending cells to the same stable GameUnitSlot. All21 supported creator initializers reach the native active store. This covers the represented kind1 projection; full initialization and type2/type3 state delivery are separate. Pending queue hooks now retain flags when supplied a valid canonical unit, but actual queue population and destruction services are still unavailable. The first ignored fixture assumed repeated local-list builds; its corrected version uses fresh consumers of the same persistent Units storage and preserves that native gate.

The process run exercises existing runtime paths and the scene-flag reader changes. It does not prove execution of the new raw node or observer owner paths, original-game visual/gameplay parity, native exceptions or concurrency. Those new C++ APIs are not drop-in binary replacements. The local archive has293 hash-verified worker artifacts, including recorded exact libraries; `local/orch6_worker_archive_q.json` maps retained evidence before worker cleanup.

Eight missing function bodies were defined through the shared write lock and exported after live/disk verification. Corrected free fall-through instructions remain distinct from stored Ghidra function-body membership; the reports preserve the known holes and disabled mutation endpoint without changing server configuration. R's pending-list destructor has a complete verified72-byte PE span and decoded free continuations, but the stored Ghidra body still omits the internal gap and final tail.

## Follow-up packets

R recovers original first-endpoint notification wrappers and producer-specific virtual getters, the core observer dispatch schedule with actual vector growth/cleanup, and actual pending entity list owners. Candidate admission still requires raw node/endpoint/controller ownership and actual input delivery before runtime binding.

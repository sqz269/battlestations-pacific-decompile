# Native renderer default surfaces

`capture_native_renderer_default_surfaces_00b238d0` reconstructs the complete 562-byte body at `00B238D0..00B23B02`. It captures current device surfaces into actual intrusive surface owners, replaces renderer +197Ch/+198Ch, and binds the current depth wrapper. Existing `D3D9StateCache::capture_default_surfaces_00b238d0_fragment` remains available; its checked HRESULT/semantic binding interface is not substituted for the original raw routine.

The new Win32 function receives actual renderer storage in ECX and a borrowed context pointer in EDX. Original B238D0 receives only ECX and returns with `RET`; no HRESULT or useful EAX return contract is established. This source is not a drop-in binary replacement, an original stack-layout reproduction, or a native SEH adapter.

## Fixed context and admitted storage

`NativeRendererDefaultSurfacesContext` occupies 12 bytes: references to `NativeSurfaceOwnerContext`, `NativeRendererSynchronizationGlobals`, and the immutable original two-DWORD D619A0 profile prefix. The canonical pool already bound through `bind_static_d3d9_surface_pool_0108db00` must be the same owner as `surface_owner.actual_surface_pool_0108db00`. This is a caller precondition, with no extra lookup or guard inserted into native execution order. The binding and all referenced storage must remain valid for the call and any unwind.

The existing surface-owner context supplies actual renderer F8D394, canonical pool 108DB00, string pool 419CC0, lifetime 1090AA0, support singleton 108FEDC, tracking counter 108DAFC, and scalar constant D7A24C. These completed providers own their existing initialization, shutdown and concrete CRT/COM behavior. The new routine creates no allocator, success callback, fallback pool or lifetime policy.

The renderer is raw storage through the final depth binder's +1BCCh counter and synchronization fields. Its +1A10h is a current actual IDirect3DDevice9 pointer. +197Ch/+198Ch contain actual `NativeSurfaceOwnerStorage`: a 34h-byte owner inside a canonical 38h-byte slot. Neither renderer nor owner is a semantic overlay. The pool's trailing DWORD and the owner's three bytes after kind +30h are not overwritten by this routine.

Only reached final-zero dispatches admit D619A0, whose slots are `{BD30E0, B3F5B0}`. The source reads current owner profile and current slot 0, then preserves BD30E0's fresh owner-profile and slot-4 reads before calling full B3F5B0 with flags 1. It expresses this domain with compiler assumptions, not a general dynamic dispatch adapter or a rejection policy. Other profiles are outside this new source contract. No profile restriction is exercised on paths that never dispatch a final-zero terminal. The compiled 94-byte terminal helper retains all four raw reads; its full COFF, linked and runtime bytes are recorded.

## Native order and exception ownership

1. Capture device +1A10h for a conditional AddRef/Release pair. Release uses the captured object's current vtable after AddRef returns. Reload renderer +1A10h for GetRenderTarget(0), into an initialized actual output cell. Ignore HRESULT.
2. Capture the current color output for a conditional pair, then reload that cell. Unconditionally call current output GetDesc into unused stack scratch. Allocate from the complete canonical pool provider; retain the allocation return for normal construction and separately spill it for unwind. Arm constructor cleanup, then construct `(current color output, flags 0, kind 0)` when allocation is nonnull.
3. Capture old renderer +197Ch after construction, then disarm cleanup. If old and incoming differ, publish incoming before incrementing it, then decrement old and dispatch its current profile at zero. Unconditionally decrement the captured new temporary afterwards and dispatch at zero. Null allocation retains the original +4 reference-address fault behavior; it does not create an early success/failure policy.
4. Reload color output for its conditional captured pair, reload after the pair, then unconditionally Release the current output. Capture the current renderer device for another pair and reload it for GetDepthStencilSurface into a distinct initialized cell.
5. Allocate into the same raw unwind spill, construct `(current depth output, flags 0, kind 0)`, capture old +198Ch, disarm, and perform the same publication/reference sequence. **The depth constructor also receives kind 0.**
6. Unconditionally Release current depth output first, then reload it for a conditional pair. Finally reload current +198Ch after all callbacks and call complete B21690. That child owns its own optional synchronization, current-device call, ignored HRESULT and wrapping +1BCCh counter behavior.

Color and depth output cleanup orders deliberately differ. The local output cells and shared raw-allocation spill are volatile; allocation return pointers and the subjects of individual COM pairs remain captured. Raw renderer/profile accesses use explicit DWORD assembly loads/stores and 32-bit address arithmetic. COM selectors are captured from current tables at each native call position.

Original handler CBCF00 points to FuncInfo DF5508 and map DF54F8. State 0 invokes CBCEF0; state 1 invokes CBCEF8. Both predecessors are -1 and both funclets reload the same current raw slot at original `[EBP-30h]`, then jump to B3DCC0. They cover only the respective conditional constructors. The extra saved EDI changes ESP-relative offsets without changing that physical spill cell.

The source uses two constructor-only `ConstructorCleanup` scopes over one volatile raw slot. The destructor is `noexcept`, so a cleanup exception terminates according to the existing C++ source boundary. The completed constructor handles its own internal cleanup; the outer guard returns the raw slot only on unwind. Old-wrapper capture precedes disarming. Subsequent intrusive deletion, output COM calls and final binding have no outer COM temporary cleanup, field rollback, or extra synchronization guard. Ordinary C++ exception behavior is tested; arbitrary SEH faults, asynchronous races and original frame-layout compatibility are not claimed.

## Validation and preserved evidence

The ignored `local/default_surfaces/` bundle belongs to worktree `J:/PROG/battlestations-pacific-decompile-native_renderer_default_surfaces`. The audit pins its sources, scripts, original spans, actual library, exact archive members, linked executable/map, observations and verifier. Preserve the sealed bundle when replaying it against an integration library.

- Fourteen fresh guarded Ghidra/installed-PE spans match, totaling 772 bytes, including all 562 owned bytes, both complete cleanup funclets, handler/map/FuncInfo and the original profile. Provider prefixes used as bridge sites are explicitly classified as identity evidence.
- Strict MSVC Win32 `/W4 /WX /fp:strict` build passed through the ignored source hook. Both existing CTests passed and all eight seed spans freshly matched the installed executable. No permanent tests or shared build files were added.
- Four focused original/full-library pairs matched **193,981 DWORDs / 775,924 bytes**, including **190 observed real COM calls** and their HRESULT/reference results. Ordinary replacement starts with nonempty canonical old owners. The mutation case changes current device during captured AddRef, changes the exposed current color output during its pair, changes +198Ch during old-color release before later old-owner capture, and clears current +198Ch during depth-output release before final binding.
- Two additional paths throw only after a real surface GetDesc has returned inside color/depth construction. They observe native FH3 states 0 and 1 respectively and matching pool/reference/renderer state after actual constructor cleanup and outer slot return. Fixture teardown separately accounts for native-retained COM references after these exceptions; production code adds no cleanup. Untested constructor-cleanup double exceptions remain under the documented C++ boundary.
- Ten exact bsp_core archive members and the fixture object are frozen. All **537 mapped COFF sections**, **1,896 relocations** and **309 complete function ranges** (126 library functions) are accounted for. All 503 mapped immutable code/read-only sections match runtime; nine postimage phases preserve sixteen original/linked regions. Function ranges include compiler tail padding where present. The compiled new parent occupies an 811-byte COFF function range; no original/source byte-equivalence claim is made.

The original 562-byte parent executes intact except one declared handler-registration DWORD relocation to a fixture-owned registration bridge. Original BD30E0, both funclets, handler and EH data execute at their literal original addresses. Six declared entry bridges compose the full built allocator, constructor, scalar delete, raw slot return, depth binder, and actual FH3 ABI. They perform no surrogate arithmetic, fake COM success or shortened owner lifetime. This is full-original-parent/full-library-provider composition, not full-original execution of those child implementations. Every mapped local/library function is covered after stripping all leading `f`/`i` map flags.

The fixture uses actual HAL devices and surfaces, a real canonical pool, registry, resource support, lifetime and CRT storage. It uses the existing desktop HWND without creating a window, setting focus, presenting or changing display settings. Observer methods forward real COM calls and make only the declared local mutations or post-return exceptions. Loaded provider identity is checked against the actual DLL PE/hash and relocated entry bytes; this does not assert a whole live DLL memory comparison. No apphelp device-Release exception was needed in this run. No game/runtime integration, whole renderer initialization closure, or logical-stream retained +4Ch lifetime claim follows from this packet.

# Actual material compiler cached-route runtime

This packet owns runtime evidence only, with no new production header, API,
native body or reconstruction row. Its isolated tree starts at `c43ceba8` and
consumes the reviewed compiler ordering correction
`d61b6329ab12ba7688a500eb2abb2a0b90ecae24` as local `eaea5c5d`.
The earlier compiler tree and immutable archive remain unchanged.

The focused Win32 fixture executes the complete existing source wrapper
`compile_native_material_program_00b3c3a0`, its original persistent builder/name
prefix and the actual `B3B536` cached continuation through `B3C390`. It returns
an actual pass with reference count one. The same caller operation owns the
same builder, local_name and tail_child throughout; the normal builder and
local-name cleanup completes. The complete cached source route has now run.
The generated-source branch and original parent machine
code were not executed.

## Real inputs and observed effects

The fixture creates a real D3D9 HAL device and compiles two input blobs with the
installed `d3dx9_40.dll`. It places those blobs in actual cache-record storage,
using real pooled names `compiler-runtime.vso` and `.pso`. These are cached
inputs, not a replacement compiler result: the actual continuation performs
both lookups, reflection, COM shader creation, raw wrapper construction,
registry publication, pass assignment and temporary releases itself. Bytecode
read back from both resulting COM shaders exactly matches the cached inputs.

The VS contains `cViewProjMat`; reflection consults a real B5BF70-produced
52-record system registry and writes semantic5/register0/count4 into the actual
metadata. The PS contains `Tint`; reflection creates one actual material
constant with semantic37h. Metadata owners retain their raw D61810 profiles and
actual +04 counters. Required pass/state/shader metadata callbacks register the
single canonical companions without changing native storage or adding retains.

The white input is a real one-pixel white COM texture, constructed as an actual
B3F930 texture owner from its canonical pool and registered in that same owner
domain. Real alias/record producers populate the native texture-cache storage.
B44B10 calls the existing B319B0 provider, which copies/normalizes the name,
pumps messages, finds the actual cached owner and increments its raw reference
count from one to two. The pass receives that exact owner at84.

The two descriptor inputs use the actual B43700 constructor. Their one-row
state arrays request root state7=0 and mode state7=1. Both B34920 operations
complete one row, and the resulting actual state is1. Both B3B280 calls execute
their real empty-list branch; all16 unused sampler slots are then pruned. No
sampler or state call is skipped by a test callback.

There are nine canonical owners after completion: the white texture, three
pass-state owners, the pass, two reflection owners and two shader wrappers.
The shader wrappers each have raw reference count one after the native slot
retains and temporary releases. The cache cursor advances from zero to two.
The frame confirms both COM releases, both engine-owner releases, persistent
texture acquisition, both descriptor sampler calls and normal completion.

Representative corrected-source run:

```
READY cache=2 registry=52 white=<actual address> refs=1 canonical=1
REFLECTION VS semantic5 register0/count4; PS Tint material1; root/mode state7=0/1 ->1; samplerrows0; native residues=7B2954AA/192EF0C3 unconsumed
PASS full source B3C3A0/B3B3C0 cached route pass=<actual address> refs=1 shaderrefs=1/1 cachecursor=2 canonical=9
```

## Shared child boundaries

The existing B319B0 source context still uses `ResourceLoadEventHost`. The
fixture supplies a concrete adapter to real XLive ordinal5030 and existing raw
BECB20. Each raw cursor operation is retained before invocation. The receiver
is borrowed, zeroed fixture storage; no platform constructor or platform profile
is installed. The online-null guard returns before platform, input or profile
reads. This executes the existing source guard but proves no platform construction,
current-profile dispatch or arbitrary replacement timing of raw0109CF04 through
the older host interface. A future raw texture-cache composition packet must
close that separate interface frontier.

Two explicit scratch preimages are observed by executing the original copied
306-byte B3B280 with empty sampler arrays. Its native empty branch leaves them
unchanged. Those observations initialize the two source call contexts. They
are fixture-entry inputs, not claimed captures from an executing original
B3B3C0 parent. The chosen route never consumes B-16 or source1-release residues;
nonempty sampler runtime composition remains separate.

All pools, publications, profiles, contexts and operations remain alive through
the call. The fixture intentionally holds the completed pass and its owner
domain until process exit. It calls no acknowledgement/disarm method and does
not fake terminal admission or destroy retained failure frames. Actual whole
parent FH3/SEH, terminal shutdown, arbitrary private-stack aliases, generated
source/VFS diagnostic output, texture cold resolution, drawing and gameplay are
not validated. Real imports and existing reconstructed children are shared
dependencies, so this is full **source-only** parent execution, not an original
whole-parent differential result.

## Evidence closure

The local fixture, exact MSVC `/MD /W4 /WX /fp:strict` commands, source dependency
JSON, link map, original profile/body spans, raw run logs and actually loaded
Win32 module paths are retained under `local/verification/material_compiler_runtime`.
The final driver uses fresh libraries from this tree. Both actual cache-input
bytecode blobs are captured before parent invocation and retained with hashes.
The original empty B3B280 and twenty profile spans (1,086 bytes total) match live
Ghidra and the read-only installed PE. The initial c43 run log/executable are
kept distinctly from the corrected final run. The report records final build,
CTests, immutable artifact closure and its manifest hash.

The frozen input commit is `7da230dd`; the archive contains 7,720 artifacts,
1,318 compiled translation units and 80 loaded modules, all PE machine014c.
Its manifest SHA-256 is
`21c0fb557abfcf55fb068e7eed86bce85340ab2a563790b6ce3a9c0caf2c3b98`.
Final metadata narrows the earlier frozen document's platform-storage sentence
as explained above. No fixture, production source, binary or archived byte was
changed by that clarification.

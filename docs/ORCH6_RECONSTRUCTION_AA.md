# Orchestrator 6 reconstruction batch AA

Addresses: `0077F0E0`, `004CEE30`, `00710BB0`, `00B6DA70`, `007135C0`,
`00876EC0`; existing wrapper `0049C940`. Analysis definitions: `007F1FE0`,
`00C65B48`, `00C84B64`.

The existing three workers and the integrator's unit-state wrapper are finished.
No new packets were dispatched during closeout. Six complete bodies account for
1,717 original bytes. Actual runtime bindings and several whole providers remain
required; the full game reconstruction is unfinished.

| Component | Behavior and focused evidence |
| --- | --- |
| Unit activation / registry count | Complete365-byte activation, current holder/table reloads and precise x87/SSE transfers. Existing checked-count provider reused with the3FFFFFFF bound for147-byte4CEE30. Six native/source caller cases and source count exceptions pass. |
| Model groups / raw node visibility | Complete593-byte selector and60-byte visibility body over existing storage. Current iterator/global/target reads, UCOMISS and x87 order retained.23 differential variants pass. |
| Unit-part construction | Complete356-byte caller, borrowed canonical fields and descending cleanup. Three native/source comparisons and six source exception states pass; native FH3 is untested. |
| Unit group-state / announcement wrapper | Complete196-byte transition composes canonical pose/group routines. Existing121-byte announcement wrapper accepts actual managers with guarded late+08 access. Seven caller pairs, two null-manager variants, source cleanup and old-view parity pass. |

All280 direct call rows pass mechanical checking. Seven indirect worker sites
remain explicit dispatch contracts. The activation caller's missing641-byte
enclosing body and two10-byte FH3 selectors were defined from live/disk evidence.
Nine names/comments were saved with previous values retained;14 affected exports
were refreshed. Internal post-free gaps were repaired. Stored function ownership
still omits decoded tails in710870,7108E0 and711000; the24 raw visibility call
spans remain explicitly separate from body-attributed evidence.

The final Win32 build and both existing CTests pass at
`07a1ffad31f8aa39d8250297103c9dc98e5cf3c2`. All eight native seeds match. Six production
objects used by the root fixture match the final build byte for byte. Ten worker
source/header Git blobs match the integrated tree;1 physical files differ only
in CRLF/LF normalization, recorded separately from their original raw hashes.

The existing120-frame USN01 compatibility run exited successfully in
11.456s using executable SHA256
`a18c97b648c0fce9fb1b26e4562b89c4b3690ee0db7f2a057679874e5ccc1da3`. It produced18,557 finite trajectory rows,
241 unchanged Airfield2 samples,2,400 avoidance queries,1,080 cruise-owner reads,
10,080 ticks across42 units, and420 valid world nodes for14 ships and20 planes.
No unavailable owners or invalid lists were reported. Observer teardown preceded
manager drain; actual pending queues remained empty through240 passes and exit.
This establishes compatibility of existing paths, not gameplay of the new ones.

All1,691 worker-manifest hashes were verified and2,232 artifacts archived under
`local/worker_evidence_aa`. The root proof retains368 inputs and74 artifacts,
including compiler-discovered headers, toolchain, searched libraries, production
objects, original/relocated bytes and matching output images. All three worker
worktrees and branches were removed only after archive and merged-ancestry checks.
The clean integrator worktree remains available for review.

See `reports/orch6_reconstruction_aa.json` for exact manifests, commits, runtime
receipts, source/object comparisons and cleanup records. Native exceptions,
hardware-fault timing, general concurrency and complete gameplay remain unproved.

# Input preimage provenance (AY)

Read-only static audit against `C:/Users/sqz269/bsp.gpr`, `/battlestationspacific.exe`, current source, and the installed `I:/SteamLibrary/steamapps/common/Battlestations Pacific/scripts/datatables/keyboardsetup.lua`. The installed script is file evidence, not evidence that the executable reached the path, loaded it without overrides, or ran gameplay. No source, Ghidra or game state was changed.

| Source input | Native write/read and shipped path | Production constraint |
| --- | --- | --- |
| `descriptor_flag_stack_preimage` | In 006A7BE0, the descriptor default pushes `FFFFFFFF,0,0,0` and `MOV byte ptr [ESP+D8],0` at 006A807D before `MOV ECX,dword ptr [ESP+D8]` at 006A8085 and 006A0DB0 call at 006A8096. Only low byte is initialized. Source `src/native_input_settings_tables.cpp:175` copies high24 into each 14h descriptor; 006AA640 later reads all five words (`src/native_input_keyboard_apply.cpp:129-131,295-303`), and 00A93750 writes the fifth DWORD into raw binding+14 (`src/native_input_binding_slots.cpp:56-74`). The installed script has structured Inputs at `keyboardsetup.lua:35-60`; this path produces descriptor slots. | Keep an explicit source policy/seed for the otherwise undefined high24, and label it as chosen host state, not a recovered native value. Do not infer zero from the low-byte clear. A single deterministic source seed is testable, but not byte-identical native provenance. |
| 006A1E70 preset mapped flag high24 | This is a **different** stack cell inside the callee. `006A1E70` at 006A1EC4..006A1ED8 writes full defaults through local+0C but `MOV byte ptr [ESP+30],BL` writes only the flag's low byte, then 0069E6E0 copies the mapped value. Its high24 are undefined independently of the caller's descriptor slot. `src/native_input_settings_tables.cpp:223` currently passes the same service seed to the preset index; this is a host seed selection, not proof that the two native frames share bytes. Installed `keyboardsetup.lua:725-739` has InputNames, so preset records are produced. | Model this as a separate preset flag seed if byte-provenance matters. Do **not** force literal zero: the saved native write is BYTE, and a retained seeded native fixture has observed nonzero high24 propagation. Existing shared seed may be a bounded source policy when explicitly labelled, but cannot be cited as native stack identity. |
| `vector_opaque_stack_preimage` | Supplied at `src/native_input_settings_tables.cpp:204,230-234,247` for AxisPairs/Groups/Pairs nested checked-vector wrappers. Installed file has AxisPairs (`keyboardsetup.lua:85-95`). In current `src/native_input_settings_vector_storage.cpp:50-76,92-108`, nested-vector construction copies begin/end/capacity only, leaving destination header word0 untouched; relocation also swaps only +4/+8/+C. Native 006A7BE0 passes wrappers with three zero pointer words to 006A6350 at 006A8786/006A8F19 and 006A4710 at 006A92B0. No native producer of a defined opaque word0 is established here. | The supplied word is not observed by the current valid nested-copy route and should not become a required global constant. Preserve destination opaque word0 without reading it. If a new consumer reads it, establish that producer before wiring; an arbitrary zero would only be a host policy. |
| `sensitivity_default_stack_preimage` | `0055A9A0` initializes the mapped vector pointer/count cells on miss but leaves its scalar default word unwritten before 005586B0 pair construction. 006A7BE0 calls it at 006A8344; source immediately appends a display string, then writes the Lua category to mapped+0 (`src/native_input_settings_tables.cpp:183-186`). Installed categories are exactly 0,1,2,3,4 (`keyboardsetup.lua:5-9,62-68,139-145,243-247`). Thus completed unmodified shipped-table records get the script value, independent of the copied default. The transient word still exists between insertion and write, including exceptional/reentrant boundaries. | For completed non-reentrant shipped parsing, no semantic category seed is needed; the current source API retains an explicit seed to represent transient copied bytes. Treat it as chosen source state and do not claim a defined native value or universal runtime invisibility. |
| `initial_sensitivity_class_preimage` | `006AA640` has a local class word (`local_84` in saved decompile) with assignments only for category 0, 1/3 and 2/4 before it is used as the inner map key. `src/native_input_keyboard_apply.cpp:216,230-242` models this by retaining a prior class for other categories. The installed `KeyboardSetup.lua` category entries use only 0–4, and entries with codes all assign before use. Modified/override scripts or malformed saved trees can introduce other categories; native gives no defined initial class choice for that case. | On the unmodified shipped table route, this preimage is not read as a key. Keep the API policy explicit for unsupported categories or constrain production to a verified category domain; never silently map unknown category to zero. |
| `suppressed_flag_stack_preimage` | 006AA640 writes `MOV byte ptr [ESP+AC],0` at 006AAB6C, then reads `MOV ECX,dword ptr [ESP+AC]` at 006AAB74 before 00A93750 at 006AAB87. Source masks high24 at `src/native_input_keyboard_apply.cpp:301-304`; 00A93750 persists the full fifth descriptor word at binding+14. The branch requires an alternate AxisPairs slot plus 00699BF0 predicate (`src/native_input_keyboard_apply.cpp:204-207,297-304`). Installed AxisPairs exist, but this audit did not prove the runtime binding descriptors/branch outcome. | Give the high24 an independent explicit source policy if this branch is admitted. It is potentially persistent and cannot be replaced by zero as if native initialized it. Establish branch reachability with actual configuration/binding data before narrowing the policy. |
| `temporary_map_opaque_preimage` | Source `ScaleTree` writes it to local header word0 (`src/native_input_keyboard_apply.cpp:132-155,223-224`), but 0055B400 clone construction writes head+4/count+8 and leaves word0 untouched (`src/native_input_scale_maps.cpp:131-136` in the committed scale packet). Lookup, insertion and erase operate on +4/+8 and nodes. The native 006AA640 local tree is passed to 0055B400 at 006AA6FE; no defined word0 producer or read in this reached path is established. | Prefer opaque untouched storage and no read. The current source seed is unused by valid tree operations; it is not a reason to invent a native zero or shared global. |

`NativeInputKeyboardConstants` have defined image values rather than stack preimages. Current Ghidra bytes: D7A218=`00000000` (float +0), D7A24C=`0000803F` (+1), D7A260=`000080BF` (-1), D7A208=`00000080` (float -0), D7A250=`000000000000F0BF` (double -1). Source operations read these by reference in `src/native_input_keyboard_apply.cpp:48-80,277-299`. A production owner may store those exact typed bit patterns as stable cells; do not substitute a mutable settings snapshot. Current `GameStartupHost::InputServices` already owns +0,+1,-0 cells (`src/game_hosts.cpp:942-956`), but does not yet own the separate -1 float/double keyboard cells.

The six source preimage fields do not share one native origin. Two are unobserved opaque header words on current valid paths, one is overwritten before completed shipped records are consumed, one is guarded by installed category values, and two can persist as high24 flag bytes. A dependency-complete production service should explicitly document seed policy for the persistent flags, separate preset/descriptor frame provenance, use exact image constants, and retain these service cells through settings table load and keyboard application. The installed scripts may be changed or overridden; static file inspection and saved listing do not establish runtime script choice, action registration, suppression branch execution or gameplay outcome.

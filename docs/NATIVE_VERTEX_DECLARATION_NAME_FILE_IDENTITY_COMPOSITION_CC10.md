# Guarded stock-profile declaration composition diagnostic

The corrected controlled invocation established guarded, live filename/file-object identity correlation for the explicit legacy MSIDCRL40 dependency and stock XLive. It then failed the unchanged fixture's post-VFS singleton-vector count assertion at line318. No case or final CRT audit marker was reached. This metadata packet credits zero native bytes and changes no production C++, fixture, executable, default loader or tracked tests.

The fixture remains the genuine B317E0 → B305F0 → B2DBD0 declaration-name composition with B48AF0's live owner+4 atomic, canonical owner registry and real cache/CRT services. Its exact source is `4cdddae7dfb1b660584ce737e0686711b9b21076c295a6b04695cf7390af79d8`; the Win32 executable is `7e76f37a1918889cc6b33dc907844237c159e7df4123556edc4d953d119f7f26`. Existing strict Win32 build and all three existing CTests passed. Embedded asInvoker manifest and active CHECKs were inspected.

The coherent source/header/object snapshot is dd244aac2fcd7636b19c05a2af1302f4c29d3c83: deletion bindings180B and material compiler borrowed view44B. Later616f private-host changes are an uncompiled comparison. All71 game objects,531 exact selected project-library members, five production bodies, three identity-helper bodies and eight actual imports were inspected. Six source pins acquired after linking are explicitly qualified; their compiled member bytes were already sealed. Newer headers were not mixed with these objects.

The helper holds read-only expected file handles before load, pins same-handle SHA256, volume/file index, size and write time, then rechecks them. Actual HMODULEs must be MEM_IMAGE with matching AllocationBase. Both GetModuleFileNameW's reported name and K32GetMappedFileNameW's NT mapped name are opened and compared with the held expected file object. The NT device path is opened through GLOBALROOT. No guessed alias allowlist or redirection override is used. This establishes guarded current filename/backing-file correlation, not a direct section-object file ID, full relocated-memory integrity or historical physical-image proof.

Attempt01 used controller fd60cc86... and the145-input seal5dd62dcd.... Parent73568 exited2; actual suspended child19048 was terminated1460 through its verified retained handle. Both handles were signaled, final exact discovery completed and all145 inputs remained unchanged. The controller incorrectly required `--case` while the unchanged bootstrap emitted `--fixture-child`; it refused ACK before any DLL or fixture case ran. Its actual exec_command return was serialized directly without a session. No loader credit follows.

Primary review separately accepted attempt02's exact three-line controller correction: point to the same sealed executable from a separate controller stage and require the exact `--fixture-child` grammar in validation and discovery. Mandatory kernel path/FILETIME, exact CIM parent/command/creation, optional CIM image agreement, ACK and retained-handle cleanup remain unchanged. Nonexecuting grammar and identity preflight passed. Controller074cad48... and the149-input sealfc0bce4b... preserve the original145 pins and add the controller, corrected preparation archive, first-attempt archive and direct tool-capture wrapper.

Attempt02 acknowledged child96200 of parent43840. Both exited3221226505 (C0000409) after the active assertion printed `FAIL line 318`. There was no controller error, timeout, termination action, capture/hash error or retry. Both handles were signaled, exact final discovery completed, remaining recorded processes were zero, and all149 inputs remained unchanged. The actual exec_command return was serialized directly; no wait was needed. The fixture's fixed child/fault/host logs were archived alongside the separate attempt02 controller receipts.

The child observed both DLL basenames absent before load. The explicit ordered dependency was the legitimately extracted local MSIDCRL40 payload SHA `623cb6ca98e566357abbd0e76b15713921e1d7e1144c0c4f589a0407c7eff1ee`. It loaded at27500000. Expected, reported and mapped opens matched volume12bda9c6/file index0001000000aac5ad; ordinal43 resolved to275684f0 in that module's MEM_IMAGE allocation.

Stock `C:/Windows/SysWOW64/xlive.dll`, SHA `79da26ab6b2dc25936c3354087de0ba41da1a8b62924972e9100c29b95d34385`, loaded at55d90000. Its reported name was `C:\Windows\System32\xlive.dll`; the mapped NT name was `\Device\HarddiskVolume3\Windows\SysWOW64\xlive.dll`. Expected, reported and mapped opens matched volumee8dcd6e0/file index0009000000155f68 and the stock hash. Ordinal5030 resolved to55e44e6c in the stock MEM_IMAGE allocation. All six temporary/expected profile handles logged normal closes before the later failure. This does not retroactively prove the physical backing of earlier diagnostic runs.

Progress to line318 supports bounded inference from sequential active CHECKs that the genuine miss/hit count2/3 checks, canonical releases2/1, B32210 terminal0/exact retirement, free32 slots and BE25C0(flags1) VFS retirement check completed. These are not separately logged measurements. Actual ordinal5030 call count is UNKNOWN: its counter print follows the failed assertion. The case marker, focus-count assertion at319, token-return capture and final CRT marker were not reached. Empty fault/host logs provide no final retirement proof. No forced message, SDK initialization, application activation or original-game execution occurred.

The read-only audit proves the fixture's count1 expectation is wrong. Native BCF910 computes raw vector length `(end+8 - begin+4) SAR2`, including null slots. BCFCA0 unregister writes zero into the matching slot without shortening the vector. Native BE21CC calls BDA790; BDA790 unregisters CURRENT0109CEEC before clearing the publication, and the outer scalar wrapper frees afterward. The source lifetime bridge uses the same actual01090AA0 manager. Direct BE25C0 therefore includes genuine unregister; an additional invented shutdown is unnecessary. The earlier passed raw count2 is not reduced to1 merely by unregister. Later final-audit line216 repeats the same mistaken raw-length expectation.

BD0400 decrements end before dispatch and skips null entries, so a correctly unregistered tombstone is distinct from a freed registered identity. Actual post-VFS vector length and slot bytes were not logged; their expected state remains source/native schedule inference. The focus2 expectation is statically consistent with two B305F0 entry pumps and one GameSoundLoadEvents increment per BECCD0 return, but line319 and its actual count were not reached. No count normalization or fixture change was made. Current Ghidra BE1F60 body/export stops atBE2028; the BE21CC base call is separately pinned original-PE disk evidence, with no Ghidra mutation.

The initial closeout draft misidentified line318 as the focus assertion. A packaging assertion caught the source-line mismatch before creating a runtime archive. That draft and failure are retained in nested freeze_packaging_attempt01_evidence.zip; the corrected receipt pins the actual singleton-count expression. Runtime inputs never changed.

Immutable archives under ignored local/output are verified by rereading every direct row:

| Archive | SHA256 | Members / rows |
| --- | --- | --- |
| preparation/preparation_evidence.zip | e96a1b6f53a6a14e3285f52bbfa31d62f8ad513015bb2d1578ad91e7113083be | 227 / 226 |
| preparation/runtime_attempt01_evidence.zip | 9f6937e7b2d9bd8c1a2249bf5736ec70e5daf72193dd081f3b8e4d81003e9732 | 15 / 14 |
| attempt02/preRun_evidence.zip | 756c3b84feb457d85e4ac1676ab937e77dcb6cd8580a4834772d6e260fc46ffe | 9 / 8 |
| attempt02/runtime_attempt02_evidence.zip | ec73138a003f27251c4978b428d2065a80cd2c7cde56e9c29f1beb97a6878fb6 | 21 / 20 |
| post_vfs_manager_count_audit/evidence.zip | 9979df0a4f9c544c6eedef02b0b773f0f659b505d6385fa17d9e116c0217d0ce | 25 / 24 |

The companion report gives exact paths and receipts. Attempt02's preRun archive contains attempt01, which contains the preparation archive. All earlier component, redistributable, path-identity and failed-run archives remain immutable. No further invocation is included.

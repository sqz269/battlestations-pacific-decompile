# Raw Light lifetime and directional terminal

The new source reconstructs Light destruction and the directional scalar terminal over actual storage, independently of `DirectionalLightOwner`, `NativePointLightOwner`, and the logical scene runtime. It supplies a separate same-count directional companion for raw construction/pool storage; it does not install that companion into the legacy destructor graph.

| Entry | Range, end exclusive | Bytes | Coverage / original ABI |
|---|---|---:|---|
| B7C5B0 | B7C5B0–B7C6AA | 250 | Complete normal Light body; ECX actual light; RET |
| B7C1C0 | B7C1C0–B7C1D7 | 23 | Complete borrowed-array cleanup in valid descriptor domain; ECX actual12B; RET |
| B7C820 | B7C820–B7C846 | 38 | Complete directional scalar wrapper; ECX actual1F0h, stacked flags; EAX original identity; RET4 |

All311 body bytes match live Ghidra and the installed PE. Final instructions are B7C6A9 RET (1B), B7C1D6 RET (1B), and B7C843 RET4 (3B). At worker freeze B7C1C0 was truncated in Ghidra at inclusiveB7C1D1. Primary integration cleared the call-site override, decoded the5B returning-free continuation, and recreated the exact23B body preserving prior annotations: ADD ESP4, POP ESI, RET atB7C1D6. Final live listing has10 instructions and0 gaps; the callee no-return flag is unchanged. Primary also defined10B handlerCC1EE6; all11 direct/tail rows now receive instruction verification, with4 indirect rows separate. The worker made no Ghidra edits.

B7C5B0 stamps D62F58, reads initial signed17C, then captures currentCE2220 atB7C5DD **once**, even if the count is zero. That captured service is reused for every scene release and shadow174 release. The current borrowed array consists of begin178/count17C/capacity180. Each iteration rereads count then begin and calls B83EC0 on the back scene. After that gate returns, it rereads count then begin and captures the back scene again for release; the two identities need not match. A zero result reaches that captured actual3Ch owner's same-count canonical current0. After the terminal returns, current17C is reread, decremented only if nonzero, then reread for the signed positive loop test. No scene or array identity is substituted by host metadata.

The current shadow174 is captured after the scene drain. Its decrement uses the same captured service. On zero, the source requires an explicit family-aware `NativeNodeTreeRetirementDispatch` binding: resolve the captured current numeric profile to its real table, read current0, resolve the same registry's canonical actual+4, then invoke the genuine target. The174 clear is after that callback. The code creates no shadow companion or fallback. Null shadow and a release leaving a nonzero count are the directly admitted domain; a zero shadow requires an independently supplied genuine terminal and remains unexercised here.

The real shadow family is508h, allocated byA8FD30 and constructed byA8FA30/A8E2E0: finalD5B5D8/BD30E0/A8FCD0, baseD5B574/BD30E0/A8E160. Existing raw shadow destruction still depends on typed camera viewport/legacy root removal paths through its canonical camera references. The new2Ch depth-target owner has dimensions at+4/+8 and is **not** this refcounted shadow. This packet does not claim raw shadow/camera terminal closure.

Before array cleanup, native consumes state1 to0; before directB6F440 it consumes state0 to-1. Normal Light code inlines B7C1C0's exact resize0/current-begin/free sequence, so source composes the same helper without claiming a native direct call there. For valid nonnegative count/capacity, existing B7BC70 resize0 cannot reserve and never dereferences a `SceneResource` object. It decrements count to zero, then writes zero; the current begin is captured afterward for the genuine shared CRT free. Begin/capacity remain stale. No array-key retain/release is added. After the gate, the newly selected back element must be accessible: a callback making count0 there would cause an invalid native read and is outside the valid domain. A terminal making count0 is supported.

The raw B7C6B0→B7C4C0→B6F5A0 constructor materializes `NativeNodeStorage` and its live atomic+4 before native callbacks, restoring count1 at that boundary. The Light tail constructor uses assembly words and does not itself establish a `SystemAmbientBacklinks` C++ object. The cleanup helper therefore snapshots12B, placement-constructs that trivial descriptor, and restores all12B before typed resize. No callback observes changed preimage and no owning tail is introduced. Callers using typed attachment helpers earlier must likewise supply a live descriptor over those same bytes; the focused fixture explicitly starts that lifetime, preserving its entire preimage, before genuine B7C020.

The node-base context, frames and acquisitions are the genuine `native_node_base_destruction` providers. Light preserves the same actual owner registry, current import cell, type cells, raw scene registrations and name pool. Its caller-owned frame contains initialized gate-erase scratch plus a persistent node-base frame. Acquisitions retain the captured decrement epoch, reached identities, state, completed releases and nested node diagnostics. Source failure consumes array/node cleanup once; second exceptions terminate. Completed cleanup does not imply Light success, pool return or resolution of all scene credits. Keep raw backing, acquisitions and unresolved credits for explicit disposition.

Compiler evidence: CC1ED0[8] ends with CC1ED3 JMPB6F440 (5B); CC1ED8[14] ends with CC1EE1 JMPB7C1C0 (5B); undefined rawCC1EE6[10] loadsDFAFC4 and ends with CC1EEB JMPBF6B43 (5B). DescriptorDFAFC4 has19930522 magic/maxState2/mapDFAFB4, entries {-1,CC1ED0},{0,CC1ED8}. These32B boundaries and52B map/descriptor window match live/PE. They support the source cleanup schedule, not native FH3/SEH/private-stack transport.

B7C820 first stamps D62FB0, invokes the whole Light body, then rereads the current flags low byte. Bit0 calls genuine `DirectionalLightPool::return_raw_slot_00b7b2f0` against actual01090154/38h pool storage and the same1F0h slot's live1EC slab ID. No native payload read follows return. The separate `NativeDirectionalLightStorageReference` borrows actual+4 in that same registry without native stores or additional credit. Admission requires completed raw construction and real pool provenance. Duplicate refusal leaves bytes unchanged. Canonical zero requires currentD62FB0/BD30E0, then a freshly current4=B7C820/flags1. Explicit scalar deletion permits positive counts and flags0. Normal return and source unwind retire/unbind metadata once; unbind and retired companion destruction never read a returned slot. Exceptions retire only metadata, and noexcept canonical release terminates if an exception escapes. Caller synchronization prevents reuse until retirement finishes.

The adjacent typed terminals remain separate: B7C800[28] selects native-node poolB6E670, B7C850[32] calls pointB7C770[112] then200h poolB7B1D0, and cameraB71FE0[32] callsB71F10 then45Ch poolB711E0. Their old typed bodies/canonical companions cannot be used as raw attached-owner admissions. In particular B7C800 is not an alias for the directional1F0h return path. No point/camera method is edited here.

Strict MSVC Win32 build and both configured existing CTests pass. One ignored executable compares the complete23B array helper and two original/source directional/Light compositions using real directional pool slots, raw node/name construction, actual3Ch resources and genuine light attachment/registry providers. It establishes real credits through native providers, preserves descriptor preimages, compares normalized496-byte flags0 results, and checks a real flags1 pool return without reading returned payload. A registry-free callback changes begin and the current imported service; the body observes the fresh back while retaining its captured decrement epoch. A genuine resource terminal callback changes count; the following loop observes it. A decrement callback changes the incoming scalar flags0→1. Separate source checks cover unchanged/duplicate admission, canonical observed-zero and explicit positive-count flags0, plus a missing-current-gate-target diagnostic with consumed cleanup and explicit retained-credit/slot disposition.

The311B extent is byte evidence, **not311 unchanged execution bytes**. Direct calls and the captured-import operand are relocated to genuine shared providers. The8 bytes B7C620–B7C627 (`MOV EDX,[EDI]; MOV EAX,[EDX]; MOV ECX,EDI; CALL EAX`) are replaced with `MOV ECX,EDI; CALL canonical bridge; NOP`, because D63168 is occupied by the reconstructed executable; that mapping remains untouched. The bridge keeps capturedEDI, resolves the real same-registry canonical owner, checks actual+4/observedzero, and calls genuine current-profile/current0/fresh-current4 resource retirement. Those original dispatch instructions are not independently compared. Shadow-zero instructions are unexercised. Nested callee machine code, native exception transport, full terminal graph, binary ABI, application/AC59A0 admission and gameplay are outside this proof.

The exact probe command is retained in the report and `local/output/cc10_raw_light_lifetime_probe.cmd`: `/MD /EHsc /std:c++20 /O2 /Gy /W4 /WX /fp:strict`; no `/DNDEBUG`, compile-time NDEBUG rejection; `/link /OPT:REF /MANIFEST:EMBED`. All earlier evidence remains preserved.

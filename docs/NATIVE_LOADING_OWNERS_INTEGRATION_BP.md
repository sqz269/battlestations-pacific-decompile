# Native loading owners integration BP

Addresses: 00B7CB70, 00B7CB90, 00B7CE80, 00B7FA60, 00B801C0, 00BE0980, 00BDC9B0, 00BF0980, 00BF09B0, 00BEA150, 00BE9F10, 00CC2110, 00CC2118, 00CC70C0, 00CC70C8, 00CC70D3, 00CC7130, 00CC7138, 00CC7810, 00CC781B

This batch integrates 11 reconstructed ordinary native bodies across three translation units. The validated combined source is `cfec38f9866ac0d923b5dbf1edd9dd58ffa21674` with 2612 recorded build inputs. Full details and receipts are in `reports/native_loading_owners_integration_bp.json`.

The cache erase packet preserves actual node identity while transplanting the successor and repairing the red-black tree. Keys return to the raw string pool before the original node is freed. Mapped resources remain borrowed. Returning diagnostics, current count reloads, and failure after unlinking retain their native ordering; the owner is not rolled back.

The FileBlock scope packet implements entry and exit on the captured actual VFS manager. Gate allocation precedes checked growth; observer calls precede depth/name updates. Exit retains the three distinct returning checks and current-node reloads. Nested exit does not restore an outer name. Escaping observers retain outer invocation storage, subject to the dependency-internal lifetime limits documented in the BO packet.

The reader owner packet initializes and destroys actual 10h/70h readers. Its ordinary destructor order is name, ten strings in reverse, then base; the base releases the captured stream before its current buffer. The source carries the recovered native cleanup state transitions, including remaining-element unwind and termination on secondary C++ exceptions. The normal owner fixture passed; failure and secondary-termination paths have assembly and source evidence only. Original CRT/FH3/TLS/CLR identity is not replaced.

Primary validation passed 308 artifact checks, five existing fixture modes, 77 direct-transfer checks, the combined MSVC Win32 build and both existing tests. A separate live/disk audit covers 1,677 bytes and 588 instruction boundaries. The unreachable three-byte alignment at B7CB7D remains separate; all other 587 starts have the expected function owner. BF0980 now owns its complete returning-free epilogue. Four ten-byte FH3 handlers were defined, and 20 source/handler/unwind annotations were saved with prior evidence retained and exports refreshed. Six decoded CRT support transfers remain explicitly outside established function membership.

Incoming main `32dd35290e645c74a4af57f8eb87af751b2a44de` was checked for integration overlap. Its changes leave all 100 recorded borrowed source/header inputs unchanged. The sole CMake append conflict preserves both sets of registrations and the final placement include. Incoming gameplay behavior and other owners' game-run claims remain their evidence; this batch adds no gameplay run.

The reader worker stopped on a usage limit with four uncommitted deliverables. The primary verified and recovered those exact files, preserving the original worktree. The following BQ workers also stopped: resource-manager trees retain two unreviewed source drafts, while FileBlock owner retains analysis only. Their states are recorded as stopped, not running; local backups preserve the two drafts.

Production loader/observer wiring, original ABI compatibility and gameplay validation remain incomplete. The continuing reconstruction goal remains active.

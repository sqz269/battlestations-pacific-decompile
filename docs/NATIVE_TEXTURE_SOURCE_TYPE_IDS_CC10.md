# Texture-source type descriptors

`NativeTextureSourceTypeIds` reconstructs four complete native bodies: B19C70[62], C30310[71], C30360[80], C30460[12], totaling 225 bytes / 63 instructions. B19C70 calls the existing `LightTypeBootstrap::initialize_root_00bea780` and `TypeIdCounterLifetime::get_006fac20`; the derived leaves use those same providers. No alternate registry or callback substitute is introduced.

The source borrows actual live 12-/16-/20-byte descriptor objects aligned to four bytes, three live byte guards, and the parent descriptor cells F8D428/0109E9C4. It stores original image name addresses D5E580/D79B20/D79B38 as numeric evidence. The context/view retain references only; their construction does not establish object lifetime, initialize descriptors or reset guards. Caller-owned descriptors, guards and existing providers must remain stable through every call and reentrant callback.

Counter, bootstrap and storage must belong to the SAME process domain: actual counter publication 0109DB7C, root guard/descriptor 0109DB80/0109DB84, data guard/descriptor F8D424/F8D428, texture guard/descriptor 0109E9C0/0109E9C4 and animated guard 0109E9C1. Every reached read requires valid initialized contents. A nonzero process guard does not prove that a different target was initialized; partial failure does not establish valid parent contents either. The caller must qualify these reached reads and supply its existing exclusion/lifetime contract. `volatile` preserves access scheduling and supplies no synchronization.

Each initializer checks its process guard, then sets it and writes the target name before invoking the parent provider. It preserves interleaved current-parent loads/stores. The counter getter returns the actual owner; its current DWORD at+4 is loaded, incremented with unsigned wrap and stored before the captured old ID is stored in the target. This is the existing type counter, with no new atomic/count lifetime, reference credit or normalization. C30460 returns the captured target after normal completion; guard-set and normal paths return the same target.

Provider exceptions leave prior guard/name/parent/counter writes intact. There is no reset, default, retry, rollback, null repair, adoption, catch or new cleanup. Admitted reads require a live actual counter owner; the original allocation-failure fault boundary is not repaired. The three guarded native bodies have ECX targets, no stack arguments and plain RET; C30460 captures/returns ECX through ESI/EAX. The source exposes different C++ interfaces and is not a drop-in register-ABI replacement. These bodies have no local original FH3 frame; source propagation is not original EH proof.

One MSVC Win32 Release build passed `/W4`, `/WX`, strict floating point and all three existing CTests. Complete emission includes seven unique code sections, 739B: four entries, context constructor, private counter helper and an existing root-storage-view COMDAT. Fully inlined parent/helper work is included. Four raw objects equal their exact library members; three preserved 13c provider/leaf members remain byte-identical. Forty-two byte-order checks cover every emitted body and all 14 REL32 calls target the genuine root/getter symbols. The remaining relocation is a `.debug$F` DIR32NB reference to the emitted private helper. Physical COFF parsing records zero raw bytes when `PointerToRawData` is zero; declared `.bss` length does not synthesize bytes from an object header.

No own symbol is selected in the current application map. Library emission, provider selection and existing CTests do not establish linked own-body equivalence, application execution, original ABI/EH, exact numeric IDs or game validation. There is no new test/runtime, source0/W/default, application shader-cell/CRT installation, dispatcher table, producer activation, full sampler/program graph or complete texture-source owner admission. Those remain separate composition work.

Evidence is frozen under ignored `local/cc10_texture_source_type_ids_implementation/`: `before_build_inputs.json` (12 source/config and 15 compiler/SDK pins), `build_receipt.json`, full build/CTest logs, `compiled_inspection.json`, `emitted_order_checks.json`, raw objects/library/maps, original 225B bodies plus 18B name and exact installed-PE comparison. `pre_sync_preservation.json` preserves the previous 180-row packet/five committed blobs/four outputs before the authorized rebuild. The reviewed proposal archives remain immutable and are nested under `readiness/`; the prebuild collector's missing SDK-header assumption is preserved and corrected without a compiler/source failure. `closure.json` gives final archive/index hashes; `committed_git_blobs.json` identifies this packet's exact five committed files. Workers made no ledger or Ghidra writes; primary owns native accounting/annotations.

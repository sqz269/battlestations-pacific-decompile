# Explicit clock unregister fixture, CC10

One primary-released controlled case passed: genuine 004DE4B0 empty-clock registration, reconstructed 00736930 complete-profile deletion, length-preserving first-slot clear, then genuine 00BD0400 manager drain. No production implementation changed, no new native bytes are credited, and the application shutdown caller at 00738319 was not executed.

The exact compiled baseline is `09edb1f1da09abfbbcc8562b7d0b3ad4eff8cb60`, before the independent canonical-owner correction. The reviewed 189-line probe and 228-line controller were copied unchanged. One strict MSVC Win32 build passed all three existing CTests, and one /MD /EHsc /std:c++17 /O2 /Gy /W4 /WX /fp:strict /permissive- probe compilation passed with active CHECK and embedded asInvoker/uiAccess=false manifest. All 362 selected library members matched their actual build objects; 300 compiler-reported headers matched the frozen inputs. The complete 366-byte explicit entry at `100081E0` matched COFF and linked bytes, with all 23 relocation targets verified and one fixture call at `10001817`.

Preparation is frozen separately at `local/explicit_clock_unregister_fixture_cc10/evidence.zip`, SHA256 `178240da79d827ab4068c78502ac57036504df5f25717e4edd1abe2f195ae09d` (1,753 indexed rows/1,754 members). The launch manifest is SHA256 `4fa3f9764cae407bfd86051eb294291489e931f144fada21a8f0faa52b9fb6b3`; the 449,536-byte executable is SHA256 `c17c522760604a106c2639b4536f86d464f1daf529b4670c5edcaf0bdd7a1fff`. Primary verification and exact single-case release are copied into the separate runtime evidence. Old preparation files, source, controller, seal and libraries remain immutable.

Exactly one controller invocation ran with cwd equal to the fresh run01 directory. Parent PID 94092 and child PID 96268 had mandatory retained kernel image/creation identities matching the suspended-child receipt. Actual child command was the exact quoted probe path followed by `--case --bsp-native-data-handoff=00000188`; both existing command validators retained that grammar. CIM image_path was unavailable, but the mandatory kernel path matched; the CIM creation time differed by only three 100-ns ticks. Controller ACK occurred before resume. No declaration-fixture grammar, forced input messages, retry, occupied-range replacement or original game process was used.

Before reservation, the suspended child's CE0000 band was MEM_FREE: base `00CE0000`, AllocationBase null, RegionSize 254,935,040, protection `00000001` and allocation protection zero. Genuine bootstrap reserved the one 64-KB band before startup. The child acknowledged ReadOnlyV1 windows CE7D08[20], CE7D24[20], CE7D38[8] and CE3818[8]. The original installed executable was only a read-only data source; original native instructions were not executed.

The real getter created manager `006C0930` and clock `006C7AB8`; the SAME actual `void* volatile` publication cell was at `001AFED0`. Existing pooled-string storage, record-resize/validation domain, canonical owner registry, actual GetProcAddress decrement callable cell and retained shutdown/deletion contexts were used. Before explicit deletion, the genuine empty clock had zero records and one manager slot. The canonical registry stayed empty; there was no stand-in clock, registry write/reset, fake counter or compensating ownership reference.

After 00736930, active CHECK verified the original manager begin/end/capacity unchanged, native slot count still one, original first slot zero, SAME publication null, SAME manager/section identity and balanced physical section +18h depth. Exact geometry/depth values were not logged; successful executed assertions establish their equality. BCFCA0's first-match slot clear is therefore exercised for this single original registration, with the complete CE7D38 -> 004DE340 flags1 route. CE3818 -> 00412440 was admitted and checked as data but was not executed. Every clock read stopped before its disposal; the untouched clock +18h preimage was never read.

Then BD0400 popped the null slot and drained/freed vector and section backing without a second clock deletion. Only surviving manager-shell +4/+8/+C/+10 zeros and the existing zero-length accessor were checked afterward. No freed clock, vector or section was dereferenced. The already-drained manager was manually freed as in the accepted original fixture; the remaining raw manager-publication address bits were not reset or dereferenced. All borrowed cells and contexts remained live through both routes.

Both parent and child exited zero. The controller recorded both retained handles signaled, final exact-child discovery complete, zero remaining recorded processes and no termination actions, capture/cleanup/hash errors or timeout. Raw direct exec result exited zero and exactly matched run.json; no wait tool call was needed. The exact final case marker appeared once in the direct child log. All 1,684 launch pins and all 1,753 original preparation rows were independently rechecked unchanged after execution.

The original 00736930 body is 164 normal bytes, cdecl/no arguments/RET0. Its reconstructed provider adds four bindings and 16 caller-cleaned source bytes; this fixture validates that source interface, not drop-in native ABI compatibility or original FH3/SEH cleanup. It is one normal empty complete-clock case: no failure injection, exceptional cleanup, duplicate registration, base-profile execution, record/sink decrement, nonempty canonical companion, source0/W, platform activation, full sampler graph or game validation is claimed. Pre-fix raw-storage/C++ lifetime/strict-aliasing limits remain; this empty domain does not establish the forthcoming canonical-owner correction. Prospective DLL file/API-set pins are not an observed loaded-module census.

Initial preparation collectors and the release-preparation path assumption failed before any additional runtime: generated project refresh, receipt-local serialization reuse, and the primary receipt residing in the cc10 worktree rather than main. Each original script/outcome was preserved and corrected without repeating a build, compile or runtime. Runtime itself passed on its sole invocation. `reports/native_explicit_clock_unregister_fixture_cc10.json` records exact identities, limits and runtime archive/index hashes; `local/explicit_clock_unregister_fixture_runtime_cc10/` holds the separate immutable direct-tool, admission, ACK, child-log, controller and closure evidence.

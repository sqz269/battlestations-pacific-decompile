# Runtime-volume owner constructor (CC10)

`construct_native_runtime_volume_texture_00b3d720` reconstructs the complete original [B3D720,B3D7A8) constructor, 136 bytes/46 instructions. Native ABI: ECX actual 34h owner; stack INPUT volume COM and flags; EAX same owner; RET 8. The new C++ interface is cdecl with explicit owner, typed INPUT, flags, SAME process serial reference and actual owner context; it is not a binary replacement.

The source reuses genuine B340A0 once. It clears retained source +30 before final D618B0, then captures the INPUT's current table and current +44 GetLevelDesc target. One plain automatic `D3DVOLUME_DESC` has a real POD lifetime and no braces/value initialization. The HRESULT is ignored. Width/Height/Depth are captured in that order, then stored Height +28, Width +24, Depth +2C. The SAME captured INPUT is used for the fresh current table/+34 GetLevelCount call. Its result is stored at +14 before the fresh descriptor Format read/store at +18. The original receiver is returned.

Unwritten descriptor bytes retain their machine preimage. The MSVC volatile DWORD observations are a bounded machine-byte contract; they do not make ISO C++ reads of unwritten/indeterminate members defined, establish descriptor numeric validity, or prove HRESULT success. There is no invented zero descriptor, failure branch or successful fallback. Real COM calls must provide the admitted descriptor/backing behavior. The descriptor dies on return; retaining its address for later use is unadmitted. Callbacks may update current COM tables, descriptor and owner fields; the code keeps the captured INPUT distinct from current owner +10.

B340A0 owns the single actual +4 atomic initialization and requires aligned, fresh or fully retired, exclusively accessed backing plus the SAME live nonoverlapping serial. B3D720 starts no duplicate count, retains no COM reference and adds no owner credit. It does not establish whole-owner/header/renderer lifetime from a raw pool slot, profile or diagnostic projection. Owner payload +34 is excluded; the actual 38h pool slot's trailing index stays untouched.

Source `OwnerCleanup` arms after INPUT table capture and before descriptor target/call, then disarms after the final Format store. Its admitted C++ unwind invokes existing B340F0 -> B33F50, with the caller-live actual string domain. It adds no COM release, count decrement or raw-slot return. Failed construction retains count/COM/raw-slot obligations; no rollback, replay or discard is introduced. Native FuncInfo DF759C/map DF7594, action CBED80 and dispatcher CBED88 are preserved as separate original evidence. Source EH is not proof of private FH3 stack layout, hardware faults or foreign/nonlocal exceptions.

Backing, contexts, strings, pool, renderer and retained metadata must outlive their reached use and later retirement. Pool return alone is not count/metadata retirement: terminal callback, canonical unbind, Entry erasure and caller postcallback completion must precede reuse. No concurrent reuse proof is supplied. B2A5A0 factory/publication, canonical binding, reset-array disposal and full COM/pool cleanup remain separately required; no application, factory or original-game path is activated.

Validation: one strict MSVC Win32 Release build with /W4 /WX /fp:strict and all three existing CTests passed; no new tests or runtime cases. Source exactly matches the independently reviewed two-file proposal. CMake is unchanged. Prebuild seal records 113 source/provider/build inputs and 29 tool/SDK rows with timestamps. The exact CL command/read/write records and 258 actual include identities are pinned after the build; these later identities are not retroactive prebuild seals.

Full old/new core libraries and exact volume members are preserved (1,783 -> 1,784 members; unrelated intervening member changes are not attributed to this constructor). All 15 existing volume code sections, 1,033 bytes, and both existing EH tables, 72 bytes, have identical bytes and relocations. Added constructor entry is 365 bytes; its complete funclet/EH section is 52 bytes, with the handler at +13; the source unwind table/FuncInfo is 44 bytes. All 11 new relocations and complete normal/EH sections are recorded. Source normal disarm lets the optimizer omit the normal guard destructor/flag store; the compiler restores EH registration before its cookie check. This is source compiler behavior, not original instruction-byte equivalence.

`compiled_sections.json` preserves all 61 physical COFF sections across old/new volume and current B340A0 objects. PointerToRawData zero means declared .bss storage with zero physical bytes, never initialized header slices. `compiled_code.json`, `compiled_calls.json` and `compiled_schedule.json` preserve full code/relocations and 28 emitted events; slicing at the next function symbol is not used. Five original PE windows, 198 bytes including compiler evidence, match the pinned installation. The original constructor has three normal calls: B340A0, INPUT +44 and INPUT +34.

The new constructor is unselected in the application linker map. Six selected existing provider physical code sections, 731 bytes, match the PE after resolving all their relocations; this does not give B3D720 linked execution or runtime credit. `linked_selection.json` distinguishes selected bodies from unselected/dead code. Exact map, executable, library/member and compiler receipts are in ignored `local/cc10_runtime_volume_constructor_implementation`. The nested readiness and independent review remain immutable. Two inspection-only helper failures are preserved: wrong frozen-source directory name, then attempting UTF-16 decoding of plain-text CL.items.tlog. Corrected collection changed no source/build artifact and required no rebuild.

Workers made no Ghidra changes or native ledger additions. Original function annotation/credit and integration belong to the primary agent. Native ABI, private EH, COM failure preimages, full factory composition, canonical lifetime retirement, pool reuse and gameplay remain outside this packet's evidence.

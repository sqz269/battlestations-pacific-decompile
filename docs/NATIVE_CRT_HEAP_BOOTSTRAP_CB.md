# Native CRT heap bootstrap and selection CB discovery

The complete C119BF heap initializer is90 bytes, and C11964 heap selector is91 bytes. All181 bytes agree between the installed original PE and the live saved BSP program. Raw decoding accounts for75 instructions. Six direct calls pass the supported live checker; two IAT calls resolve to actual KERNEL32 HeapCreate and HeapDestroy. No source implementation, Ghidra annotation/repair or runtime validation is supplied here.

C119BF is cdecl with one original DWORD at entryESP+4, EAX0/1 and caller cleanup. The saved prototype incorrectly omits that argument. It calls HeapCreate with flags1 if the argument is zero, otherwise flags0; initial size1000h and maximum0. It publishes the returned handle to actual109E1BC even on null. Null returns0 before selecting or publishing a mode.

On success, it calls actual C11964 and publishes its raw result to109ED7C before testing mode3. Other modes return1. Mode3 calls complete C11CF5 with threshold3F8h. That existing source already borrows the actual heap/SBH words; it does not own their lifetime. If C11CF5 returns zero, the initializer calls HeapDestroy on the then-current heap, ignores its BOOL, performs the actual read/AND-clear of109E1BC, and returns0. It does not reset109ED7C or repair other partial SBH state. Successful C11CF5 returns1. No private heap, alternate mode, rollback or shutdown policy is inferred.

C11964 is cdecl with no arguments and EAX1/3. It initializes both local output words to zero, calls actual BFBAB2 osplatform getter first, then BFBB61 winmajor getter. Each nonzero getter status invokes actual BF65BB Watson with five zeros. If Watson returns, the body cleans those five stack words and continues. The two three-byte ADDESP14h instructions at C1198A and C119A5 are absent from the current stored listing, but physically present and PE/live equal. There are no hidden calls in those gaps. Exact flow overrides and no-return properties remain unknown; no repair was attempted.

After the getter paths, platform equal2 and unsigned major at least5 select1; all other values select3. This is the original selector schedule, including failure paths; it is not a replacement operating-system query. The saved __heap_init caller is ___tmainCRTStartup; this packet does not establish all startup argument/state providers or teardown.

Existing complete source providers are C11CF5 in native_crt_small_block_heap_init, BFBAB2 in native_crt_osplatform_getter, and BFBB61 in native_crt_pointer_decode_support. They require their existing actual borrowed CRT state and error services. The selector also requires the actual Watson/termination path. Full source composition cannot discard that path merely because the usual initialized version state makes it unlikely. Canonical CRT platform/TLS/error/termination and startup/teardown ownership remain separate.

Evidence is under local/native_crt_heap_bootstrap_cb: supported CLI captures, raw full bytes/listings, decoded instructions, import identity and explicit omitted instruction rows. The report draft outside this directory inventories the whole packet without a self-reference. Primary and independent review accepted this complete discovery. The committed report retains the original frozen draft and local inventory provenance. Original Watson, canonical startup/teardown ownership and source composition remain open.

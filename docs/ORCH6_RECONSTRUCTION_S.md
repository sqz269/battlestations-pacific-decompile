# Orchestrator 6 reconstruction batch S

Addresses: 00925CE0, 00928630, 006953C0, 00695760, 00CD3910, 00CD3940, 00CDF4A0, 00CDF4B0, 009273A0.

S integrates exact unit observer prefix store projections, primary observed-endpoint lifetime, and process-static pending entity owners. Application startup now initializes those owners with real CRT callbacks, and the existing frame pass reads their actual counts. Descriptive names remain hypotheses.

| Change | Evidence |
| --- | --- |
| [Unit prefix projections](NATIVE_UNIT_OBSERVER_ENDPOINT.md) preserve two distinct endpoint roles and21 verified leaf table pairs |25 native/source pairs,3200 compared bytes per side, zero differences; all25 projections remain partial |
| [Primary lifetime](OBSERVED_ENDPOINT_LIFETIME.md) invalidates first-endpoint edges and reuses concrete deletion providers |551 original bytes,44 matching words, four native edge deletions and three array frees |
| [Process pending owners](GAME_PENDING_ENTITY_RUNTIME.md) use one static pair and real atexit |14 verified spans, one actual-library lifecycle with three exit observations |
| [Application binding](GAME_PENDING_ENTITY_BINDING.md) initializes and reads that same pair |Successful CRT registration,240 frame passes, clean process exit |

MSVC Win32 Release and both existing CTests passed at `6fa29036625dbc65a866916ef391ea5e49523110`. The executable SHA256 is `98a524b45a4e81f4f9838718b6ce67b6af4f1b4d208c2a1969341c2b7553fc52`. The120-frame USN01 mission run completed with18,557 finite trajectory rows,241 unchanged Airfield2 samples,2,400 avoidance queries,1,080 cruise reads,10,080 generic ticks and420 valid world registration nodes. Actual observer dispatch startup/teardown and both pending-list CRT registrations passed the log checks. The final pending counts remained zero through240 frame passes.

These process results establish owner initialization and count reads. They do not establish nonempty queue population or delivery, runtime unit prefix ownership, original exceptions/concurrency, binary replacement compatibility or gameplay parity. Missing nonempty list-copy/clear bindings now fail explicitly. The original CRT table evidence establishes relative order of the represented initializers; the source uses the current CRT for exit registration.

All 569 archived evidence artifacts are hash verified, including exact libraries, objects, toolchain inputs and failed fixture attempts. S corrects R's compiler-artifact provenance: vcvars32 selected Hostx86 tools while R retained Hostx64 compiler/linker artifacts. The immutable R manifest and its independent source/object/library/executable proof are preserved.

Two exact callback RET4 bodies and two pending-lock exception handlers were formally defined and exported. Repaired pending-lock free continuations remain outside stored Ghidra function bodies; reports retain that limit.

## Follow-up packets

T connects the raw prefixes to existing stable unit owners and cleans up their borrowers, recovers actual plane/shipyard callback providers, and implements the pending entity lock singleton through the existing application raw manager. Source HUD-detach and observer-liveness support is present for that integration.

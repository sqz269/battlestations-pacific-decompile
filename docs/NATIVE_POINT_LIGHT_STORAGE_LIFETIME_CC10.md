# Actual point-light construction and lifetime

This packet closes raw-name construction, derived destruction and scalar pool return over genuine200h point-light storage. It composes the actual backlink runtime and the published raw Light/node lifecycle. A separate same-count canonical reference supplies postconstruction metadata; no legacy `NativePointLightOwner`, logical scene or whole-owner conversion is used.

| Entry | Range, end exclusive | Bytes | Coverage / original ABI |
|---|---|---:|---|
| B7C710 | B7C710–B7C73D | 45 | Complete constructor wrapper; ECX actual200h, stacked raw name, EAX same owner, RET4 |
| B7C770 | B7C770–B7C7E0 | 112 | Complete normal point destructor; ECX actual owner, RET |
| B7C850 | B7C850–B7C870 | 32 | Complete scalar wrapper; ECX owner, stacked flags, EAX original identity, RET4 |
| 5A1610 | 5A1610–5A1627 | 23 | Complete array cleanup in valid count0-resize domain; ECX actual12B descriptor, RET |

All212 body bytes,32 compiler-boundary bytes,52 EH map/descriptor bytes,226 immediate dependency bytes and16 actual-profile bytes match live Ghidra and the installed PE. Final instructions are B7C73A RET4 (3B), B7C7DF RET (1B), B7C86D RET4 (3B), and5A1626 RET (1B). Primary repaired/saved/exported5A1610 before this implementation: its returning-free continuation at5A1622/25/26 is now present,10 instructions/0 gaps. The worker made no Ghidra changes or duplicate repair claim.

B7C710 captures the raw name pointer and calls the genuine rawB7C4C0 atB7C718. That provider retains the current-one/currentCE7820 read schedule and rawB6F5A0 name-pool behavior. On success the wrapper stores D63008, then zeros1E0/1E4/1E8. The real base creates the live174h prefix and actual atomic04. The source materializes only the trivial12B scene descriptor at178 with its entire preimage restored, and uses the existing exact backlink initializer for the actual1E0 object lifetime/native zero stores. No host tail fields or logical owner are constructed. Native-unwritten1EC..1FB and the pool-owned1FC are preserved. Base construction supplies its existing one-shot cleanup on failure; this wrapper has no additional native EH handler and physical slot recovery remains the caller's responsibility.

B7C770 stamps D63008 atB7C78E and arms state1. B7C79C calls genuine B7C160 through the already registered same-identity `NativePointLightLinksBinding`. Its actual reverse1E0 array supplies each raw node to B6F3C0; current begin/count are reloaded in the original unsigned-index schedule, and the reverse count is cleared afterward. Duplicate entries remove one equal forward light per occurrence. No node/light retain or release is introduced.

B7C7AB consumes state1 to0 before the second59FCE0(0) atB7C7B0. It reads CURRENT begin atB7C7B5 and frees atB7C7B8, leaving dangling pointer/capacity unchanged. The source composes5A1610's identical resize0/current-begin/free sequence; native B7C770 inlines that sequence and has no CALL5A1610. The helper implements only the reached valid count0 path: nonnegative count<=capacity means no reserve, current count decrements to0, then a final0 store. It uses the SAME real `NativePointLightLinksRuntime` provenance/free service and never casts the descriptor to a logical group/container. Full arbitrary59FCE0 growth is not a new claim.

After reverse cleanup, the source explicitly retires the host backlink association while count0 and the payload are still live, before entering Light. This is a metadata step, not a native call or ownership credit. Existing `unbind_light` reads count, so it must not be deferred past pool return. The binding is caller-owned and persistent: a failure before this boundary retains it for disposition; canonical retirement alone does not erase or destroy that caller object. B7C7C2 then consumes state0 to-1 before direct rawB7C5B0 atB7C7CA. A subsequent Light failure uses that provider's own consumed cleanup states and cannot repeat point-array cleanup.

The native EH map atDFAFE8 contains {-1,CC1EF0},{0,CC1EF8}; DFAFF8 has magic19930522, maxstate2, mapDFAFE8. CC1EF0[8] tail-calls B7C5B0 atCC1EF3; CC1EF8[14] adds1E0 and tail-calls5A1610 atCC1F01. Undefined rawCC1F06[10] loadsDFAFF8 and jumpsBF6B43 atCC1F0B; its inclusive end isCC1F0F, final instruction length5. Only boundary evidence and source cleanup scheduling are supplied, not native FH3/SEH/private-frame transport. Cleanup states are consumed before calls; a second cleanup exception terminates at the documented source boundary.

B7C850 performs no extra entry profile store. It calls the point destructor, then tests the CURRENT low byte of its incoming flags atB7C858. Bit0 returns the captured original owner through B7B1D0 atB7C865; EAX returns the same identity. Explicit flags0 and positive-count scalar deletion are supported. No decrement is added. The genuine `NativePointLightPool` is actual0109011C:200h slots, live1FC slab ID,32 slots per4044h slab, free indices at4000h and free count at4040h. Pool return reads current1FC after critical-section entry, uses signed SAR9 on slot-minus-slab, stores the16-bit index, increments the freshly read16-bit count, and updates first-free-slab. Directional1F0h and camera45Ch ownership are distinct and unused.

The separate `NativePointLightStorageReference` borrows that same live atomic04 and binds it into the existing actual-owner registry without native writes or extra credit. Before atomic/registry admission, pure checks validate current1FC against the supplied pool, exact200h slot range/alignment, current free count<=32 and absence from the active4000h free-index stack. Wrong-pool and returned slots are refused. Caller quiescence, accessible200h backing, completed raw construction, initialized current profiles and the already registered matching backlink binding remain required. Duplicate admission leaves native bytes and the existing binding unchanged.

Canonical release requires observed actual count0 and currentD63008/BD30E0, then delegates to the genuine reference-counted dispatcher for a freshly current slot4=B7C850 with flags1. Explicit scalar entry does not impose count0. All destruction of an admitted owner routes through this reference. Normal return or source failure retires/unbinds canonical metadata exactly once; registry unbind and retired reference destruction access only host metadata, never a returned payload. A source failure does not prove native destruction or pool return completed. Persistent frame/acquisition/binding/raw backing and unresolved credits remain available for explicit disposition. No reentry or slot reuse is permitted until metadata retirement finishes.

The existing raw Light contract applies to scene arrays, current imported decrement epoch and same-count canonical resource terminals. Null shadow and a release leaving a nonzero shadow count close normally. Reached zero shadow requires the already documented genuine family-aware provider; no invented shadow companion, camera-depth substitution or default callback is added. Native body coverage, host admission/retirement and the wider application graph remain distinct.

Strict MSVC Win32 build and both configured existing CTests pass. One ignored executable compares a direct23B cleanup pair and two original/source constructor/destructor/scalar compositions, with flags0 and callback-late flags1. It uses the real200h pool, raw names/current constants, live atomic and descriptors, reciprocal actual node/light links including duplicates, and actual3Ch resource/ambient canonical providers. It verifies full normalized512B flags0 storage, genuine pool free-count change, unchanged1FC before return, byte-unchanged admission/duplicate refusal, canonical observed-zero and explicit positive-count flags0 retirement, and wrong-pool refusal with unchanged owner/registry/backlink association. A source-only missing-current-Light-gate case verifies consumed cleanup and retained-credit/slot disposition.

All212 bytes are byte evidence, not blanket instruction/path coverage. Direct CALL operands in copied native bodies are relocated to genuine source providers; B7C850 calls the copied112B destructor and the copied45B constructor calls the genuine raw Light constructor. Reverse unlink, count0 resize/free, real pool return, and Light/node/resource/name/type bodies are declared shared-provider boundaries. The original-side Light bridge performs the same explicit metadata unbind while the payload is live. Nested provider machine code, native exception transport, arbitrary shadow-zero/derived-terminal paths and application/gameplay are not independently compared. No original virtual instruction bytes are replaced in these four bodies.

Exact compile/link command is retained in the report and `local/output/cc10_point_light_storage_probe.cmd`: `/MD /EHsc /std:c++20 /O2 /Gy /W4 /WX /fp:strict`, `/link /OPT:REF /MANIFEST:EMBED`. NDEBUG is absent and the probe rejects it at compile time. No tracked tests were added; earlier frozen artifacts remain unchanged.

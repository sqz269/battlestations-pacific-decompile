# Native BF fixture audit

The camera worker archive at `C:/Users/sqz269/bsp-bf-raw-camera/worker_capture.zip` hashes to `8806f59c804d3c5918b5a2e7dc2d9f2ab8ef0cb18327a6f88aa9d49cd02de13f`. Its captured run reports 13 original/current snapshots, one CW027F case, camera body and pool return checks, and a source-only genuine raw-getter `bad_alloc` after hooks are restored. Those are bounded fixture observations, not game validation.

The model-base worker seal was checked live with PowerShell 7 `Get-FileHash`: 19 of 19 recorded files match, including the three Release libraries. A complete 24-file worker capture, including sealed source, report, evidence, recipes, seeds, logs, executable, libraries and manifest, is at `C:/Users/sqz269/bsp-bf-model-base/worker_capture.zip`, SHA-256 `19c107de62799f02a3e8a735be4315614f7fee4d552592c842501f827e1cdcc0`. The archive contains `content_manifest.json`; a copy is alongside it. The replay recipe and bit-identical `probe.cpp` / `native_bytes.hpp` are at `C:/Users/sqz269/bsp-bf-model-base/current/run.ps1`.

Replay has **not** run on the integrated root. Invoke `pwsh -File C:/Users/sqz269/bsp-bf-model-base/current/run.ps1 -Repo <integrated-root>` after its Release libraries are final. The recipe compiles only the sealed probe source using the selected root's include tree and three selected root libraries, with Win32 MSVC, `/W4 /WX /fp:strict /MD /O2 /MANIFEST:EMBED`. It records selected src/include, fixture, recipe, and library hashes before and after, plus build and run logs and executable hash. No archived library or worker source TU is linked by the replay.

Evidence boundary: original model-base scalar B74B60 and destructor B6F440 bytes execute in the worker's normal cleanup differential. The B86720 factory is source smoke, not original factory differential proof. The probe executes the copied original BD30E0 virtual0 forwarding body through its fixture callable table and copied scalar. The unresolved retained-binding `std::logic_error` is a C++ source-domain check; it is neither an original exception nor native/raw-getter `bad_alloc`. The camera parent-body and shared-pool bridges and the model fixture remain separate. Source review found no basis to inflate these into game reachability or visual proof.

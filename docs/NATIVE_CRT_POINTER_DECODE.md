# Actual native CRT pointer decoder

This packet reconstructs the complete 110-byte pointer decoder at `C04FDE..C0504C` (exclusive end) with a naked Win32 source entry that retains its caller's actual pointer argument word. It composes the complete [module-gate prerequisite](NATIVE_CRT_POINTER_DECODE_SUPPORT.md) and current actual OS/CRT storage. It does not create TLS/PTD storage, populate callbacks, implement the encoder, or close libm/SSE2 pow.

The source [native_crt_pointer_decode.cpp](../src/native_crt_pointer_decode.cpp) and [header](../include/bsp/native_crt_pointer_decode.hpp) are the only code owned here. The four-file packet also owns this document and its audit. Shared headers, CMake registration, Ghidra and ledgers remain primary-owned.

## Caller slot and actual storage

The new naked cdecl entry takes the original pointer value as its first stack argument and an added context reference as its second. At entry E, the actual pointer word is `[E+4]` and the stable context address is `[E+8]`. Only ESI is pushed. Thus `[ESP+8]` after that save is still the caller's original pointer argument word. A selected nonnull decoder is called stdcall with the current value of that word, then EAX is stored back to it. Every normal path finally reloads the word into EAX; identity paths make no write. Plain RET leaves both arguments for the source caller to clean up. This is an actual outgoing stack-slot contract, not a promise that a normal C++ by-value call mutates the original C++ variable.

`NativeCrtPointerDecodeContext` contains four stable borrowed reference bindings in Win32 order: current getter TLS index `E15B00`, current PTD/FLS index `E15AFC`, the actual current `TlsGetValue` IAT function word corresponding to `CE20BC`, and the existing `NativeCrtPointerDecodeSupportContext`. The import reference is not a copied function value, thunk address or generic replaceable resolver. It has the actual stdcall `void*(uint32_t)` getter type. The context argument word and reference bindings remain valid/stable across the call while native index, TLS getter, PTD and decoder values remain current. No private allocator or provider population is supplied.

## Complete current-selector schedule

The entry pushes the current `E15B00` index before capturing the actual `TlsGetValue` IAT word into ESI. It calls that captured import even if the index is -1. A null result selects module fallback. Otherwise it reads current `E15AFC`; -1 selects fallback. It pushes that captured PTD index, rereads current `E15B00`, and calls the same captured import again. The freshly returned raw getter is immediately invoked with the captured PTD index, with no extra null check. A null returned PTD selects fallback. A nonnull PTD supplies its current DWORD decoder field at `+1FC`; a null field goes directly to identity, without module fallback. The native record must provide a readable 200h-byte prefix at this point.

Fallback calls actual `GetModuleHandleA("KERNEL32.DLL")` and captures its result in ESI. A null handle goes to identity. Otherwise the full existing `C04EFB` source gate receives the stable borrowed support context. A zero result goes to identity. A nonzero result calls actual `GetProcAddress(captured module,"DecodePointer")`. The selected pointer from either route is tested once. A nonnull pointer is called with the current original argument; only this route writes the returned pointer back to the caller word. A null pointer simply reaches the shared final reread. There is no cache update, extra null validation, forced direct host `DecodePointer`, LastError restoration, exception translation or cleanup.

The selected getter and decoder must be the actual established OS/CRT providers with their stdcall ABI. This is a caller-owned current-storage contract, not an arbitrary callback domain. The prior sealed discovery grounds one actual construction route: `C053DC` publishes a raw FLS/TLS getter through `E15B00`, creates `E15AFC` and a 214h-byte PTD, and `C050F8` conditionally installs `DecodePointer` at PTD+1FC. Their complete startup, locale, allocation and cleanup dependencies remain source-incomplete. This packet neither assumes those current values are always populated nor synthesizes them.

## Provider and ABI boundaries

The sole fixed reconstructed child is `native_crt_pointer_decode_module_gate_00c04efb`, including the actual current OS major/platform reads, complete raw `strcmp`, current module headers and existing owning-CRT errno/returning invalid services. Those services remain explicit source boundaries. Original errno `BFFB8B -> C051B7` and invalid dispatcher `BF66EF -> C04FDE / current handler / C04EF3 / BF65BB` are not newly reconstructed by this composition. The support context preserves their previously reviewed contract.

The original decoder is cdecl with one stack argument, returns EAX and preserves ESI plus the other nonvolatile registers through its actual callees. The added second argument and context-address loads change its source ABI and volatile-register/instruction/fault footprint. The actual argument-slot read/write schedule and plain RET are retained. Its called gate has the established C++ frame limits; native SEH continuation, exact exception stack traversal and binary replacement compatibility are not claimed. No handler or exception behavior is invented to make an unsupported context valid. The two original NUL strings are reproduced byte-for-byte in source storage; their literal original addresses are not promised to OS imports.

## Evidence and verification

Fresh BSP queries verify `C:/Users/sqz269/bsp.gpr` and `/battlestationspacific.exe` before each capture. The complete decoder body, current TLS index image cells and exact two names match the installed PE with SHA-256 `b682a82c52f81f957b2c70222077305a933f72481686c88843077f714b956dd6`. The four disk spans total 145 bytes. Disk index values are initial image values, not current runtime evidence. Import-table identity is pinned separately for `TlsGetValue`, `GetModuleHandleA` and `GetProcAddress`.

Primary approved the full source/header before the strict build. Both existing CTests passed and all eight guarded seeds matched the installed PE. The initial static link exposed that this worker base contained the full module-gate source but lacked its shared CMake registration. The ignored hook was extended to include that existing source; an incremental strict build/test and static link then passed. Production source was unchanged. The successful final archive and both build/link stages are retained; no unresolved stub was supplied.

The final actual library has two selected archive members: this decoder and the complete prerequisite. Their seven complete COFF **CODE** sections total 640 bytes with ten relocations; this is not a claim to enumerate every COFF data/debug section. Three linked entry sections total 493 bytes: decoder 127, module gate 230 and raw comparator 136. The standalone getter and raw-read/compiler helper code remain in the captured archive member even though the gate inlines the required getter/reads. The comparator remains identical to all 136 original bytes.

Static proof checks all 46 linked decoder instructions against the full intended schedule and maps every one of the 36 original instructions. Its ten additional context instructions, every branch target, both current TLS-index PUSH locations, the later captured IAT load, the fresh unchecked getter call and current PTD read are recorded. Abstract stack accounting covers every normal CFG path, including both stdcall getter calls, module/proc/decoder calls and the cdecl gate cleanup. The selected-decoder store and common final reread both resolve to entry ESP+4; every normal RET restores entry ESP. No extra restoration is inferred on faults or exceptions.

All ten linked fixed relocation bindings across decoder and gate are individually resolved: direct actual Win32 imports, exact read-only NUL names and opcode-checked calls to the complete current library providers. The runtime reference to the actual `TlsGetValue` IAT and selected TLS/PTD/decoder domain remains a caller contract, not something proven by a static executable. The ignored link artifact was never executed. Existing tests do not exercise this decoder; no runtime fixture, callback injection, fault/SEH experiment or game claim was made. The audit records the full source/provider pins and compile TLog entries. `local/pointer_decode_handoff/manifest.json` seals the exact archive, source and proof; its hash is provided externally in the handoff.

## Supplemental compiler layout evidence

The original `sizeof(Context)==16` and hard-coded assembly-offset mapping did not establish where C++ stores the four reference bindings. A separate compile-only supplement now checks that question directly against exact copies of the decoder and support headers from the original seal. Four ordinary noinline C++ functions return `&context.member`; they use no inline assembly, `offsetof` or constructed runtime context. The compiler emits six instructions for each, loading its context pointer then the binding word at offset 0, 4, 8 or Ch. All 43 accessor bytes match between the object and linked artifact, with no accessor relocations. `/sourceDependencies` captures the ten actual header inputs used for that compilation.

The supplemental artifact was not executed and does not rebuild or relink the original decoder library. It proves the field layout for the sealed headers under MSVC Win32, not runtime IAT/TLS/PTD bindings or fault/SEH behavior. The original 63-file manifest `9c802d9477934bb81ae59a14f369b6e5dddab689ec68dd9a5e744467b502c1d2`, including the original report/document versions, remains untouched. Updated doc/audit and the additional compiler evidence have their own manifest at `local/pointer_decode_layout_supplement/manifest.json`.


Primary integrated these entries into the main Win32 library and replayed the unchanged complete static verifiers against its frozen actual objects. Full110/36 mapped to127/46;2actualobjects7CODEsections640B/10relocs,3linkedsections493B; all10fixedbindings and argument-slot/stack map. The same46 prebuild source/header/build inputs remained unchanged; both existing CTests and8 seed comparisons passed. The main artifact is frozen under `local/lifecycle_pointer_build_frozen/`; fresh native captures and complete static replay are under `local/lifecycle_pointer_replay/`. Reviewed names/comments are saved and all affected exports refreshed. These checks do not execute the new routines or establish native SEH, original caller ABI or game behavior.

# Raw VFS host dependency graph (AZ)

Read-only source audit at `320f7031`. This records source reachability and required production ownership, not an executed raw VFS or game validation. The primary's current raw Lua fundamentals work is concurrent and was not used as evidence of executable reachability.

## Present executable versus raw graph

`GameVfsHost::construct_provider_manager_00beda60` in `src/game_hosts_vfs.cpp:80-85` allocates a projected `VfsProviderManager`. Its factory getters return host tokens (`:98-119`), and the MPAK comment expressly says its creation is absent in that host (`:106-119`). This is not actual `0109CEEC` owner storage or a native profile/table. No source construction of `NativeVfsRuntimeBindings` or `NativeVfsManagerLifetimeContext` occurs in the game host: usages are confined to their native modules and archive adapters. The settings/fundamentals Lua callbacks therefore cannot borrow that projected manager as a raw owner.

The production graph needs one retained set of actual publication cells, raw owners, source services, and numeric table data:

| Edge | Existing source contract | Required owner or input |
| --- | --- | --- |
| Shared singleton and strings | `NativeVfsManagerLifetimeContext` in `include/bsp/native_vfs_manager_lifetime.hpp:36-45`; `NativePathCanonicalizerRuntimeServices` in `include/bsp/native_path_canonicalizer.hpp:25-56` | One actual `01090AA0` manager domain; actual `01090AA8` string pool publication, `01090AA4` return gate, `ActualNativeStringPoolStorage`, actual `0109CEEC` VFS and `0109CEE8` log cells; caller-provided native lowercase/CRT behavior, six source C strings, validation callbacks. Retain across VFS and raw manager drain. |
| Manager construction and deletion | `native_vfs_derived_manager.hpp:8-24` and `native_vfs_manager_lifetime.hpp:47-67` | Raw A0h owner built through `00BEDA60`/`00BE1DC0`, physical factory getter and registration, six path-canonicalizer calls; delete through captured current log/provider methods and native member order. Bind deletion before registration. |
| Open/lookup/stream dispatch | `NativeVfsRuntimeBindings` in `include/bsp/native_vfs_runtime_bindings.hpp:18-75`, `src/native_vfs_runtime_bindings.cpp:18-31,67-115,213-268` | Retained open route, lookup route, physical stream, FileStore conversion, open logging and memory-owner contexts. Physical factory and logger deletion contexts are required on their reached branches; MPKG and MPAK contexts must be installed for those factories/providers. Unsupported current entries throw rather than succeed. |
| Physical and FileStore | `include/bsp/native_physical_provider.hpp:14-29`, `native_physical_stream_open.hpp:17-21`, `native_filestore_factory.hpp:12-34`, `native_filestore_open.hpp:14-27` | Actual physical provider pool `0109DBF0`, stream pool `0109DC28`, file-store factory `0109DB68`, stream type descriptors and retained memory counters `0109DB98/9C`, shared string/lifetime services. Actual file-store provider methods are dispatched by profile, but factory publication and owner must be composed into the same manager. |
| MPKG and MPAK | `include/bsp/native_mpkg_runtime.hpp:15-39`, `native_mpak_runtime.hpp:13-32`, `native_pak_registry.hpp:12-15` | MPKG archive/directory services need the VFS dispatcher, canonicalizer and memory counters. MPAK runtime additionally needs actual registry `010904D8`, lock `010904E0`, cached provider `010904DC`, container, open and directory-search libraries. `rg` found no concrete subclasses of those three MPAK library interfaces in `src` or `include/bsp`; their valid populated behavior is a separate source boundary. |
| Lua consumers | `include/bsp/native_lua_fundamentals.hpp:19-32`, `native_lua_file_loading.hpp:7-8` | Borrow this actual `0109CEEC` manager and an explicit `NativeVfsRuntimeBindings`. Retain the raw string pool and VFS graph through script evaluation and manager drain. No projected-to-native cast or physical-only substitute is valid. |

## Numeric table data

`NativeVfsRuntimeBindings::slot` dereferences `reinterpret_cast<const volatile uintptr_t*>(table)` in `src/native_vfs_runtime_bindings.cpp:18-20`, and `open`, `exists`, `is_open`, `length`, and factory dispatch consume captured entry words. The header explicitly requires original numeric vtable bytes readable at their recorded addresses. Other raw routines similarly read profile slots, including memory backing/stream and physical conversion. The current game host does not map or supply such a verified data image. A production host must provide exact, readable original table **data** at those addresses (or a separately evidenced source route preserving each current read); writing numeric identity words into owners without that data makes these paths invalid. The table bytes are not executable host callbacks. Verify image/layout/provenance before mapping; do not call the numeric entries as code.

## Smallest dependency-complete next slices

1. **Raw data and owner composition packet:** A new, disjoint game-side `GameNativeVfsRuntime` header/source can own stable publication cells, table-data provider, raw A0h manager, shared raw string pool/physical pools, factory and logging contexts, path canonicalizer, VFS binding, MPKG services and the supported archive contexts. It should borrow the canonical raw singleton manager and bind all deletion routes before getters register objects. First prove exact numeric table byte availability and address compatibility; a source-only object composition without that data cannot execute. This packet should not edit concurrent orch2/3 VFS/resource owners or claim their native addresses.
2. **Unresolved valid-behavior packet:** Supply concrete MPAK container/open/directory-search libraries, and any reached unsupported VFS table/profile operations. Determine their native ownership and exception contracts under new disjoint leases before enabling MPAK mount/open. MPKG/physical/FileStore must likewise be exercised with actual contexts and tables, not accepted from interface coverage alone.
3. **Executable host wiring packet:** Replace `GameVfsHost`'s projected manager/factory tokens at the recovered Phase 2/6 sites with the proven raw graph, wire actual `0109CEEC` into fundamentals/settings Lua callbacks, and drain the raw singleton manager while the VFS graph and every borrowed context remain alive. Registered deletion performs the VFS owner teardown during that drain; source service objects can be released afterward. This packet owns the game-host source route and its verified call-site evidence; no such edit is part of AZ.

These are dependency slices, not a claim that the existing native modules are incomplete at their documented bounded functions. Their source APIs are present; production graph construction, exact numeric table bytes, missing MPAK libraries, and end-to-end valid behavior remain separate proof obligations.

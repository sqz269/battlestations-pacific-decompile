# Validated cockpit helper lifetime and model-base bootstrap

Addresses: 00B3C5C0, 00B3C6C0, 00CD7EB0, 00CD7F20, 00CE0E60

The helper now destroys its actual camera and attached/retained members in native order, stamps base state on failure, and frees its actual24h allocation only after normal destruction. Its terminal callback can delete both separate host companions without later access. Model-base startup uses the actual shared type counter and a separate actual38h pool whose binding identity stays fixed until its real registered exit callback.

Exact combined source `daa9706c969f965ceb70790ccd2ef68f68ca0b1b` passed strict MSVC Win32 compilation, both existing CTests and eight original seed comparisons. Both external probes linked only their own source against the three current libraries. All 2448 tracked build inputs and four accepted source/header hashes remained unchanged. Worker controls and the final source/library/fixture archive are separately pinned in `reports/native_bg_integration_validation.json`.

The helper compares one original/source CW027F pair:15 camera checkpoints,16740 camera bytes per path,11 events and36 normalized helper bytes before free. Camera/name pool returns precede the one helper free; the callback then deletes both helper companions. Original mapped code remains unchanged across8880bytes. The bootstrap checks shared type IDs, consumed-guard reentry, null/different binding rejection, actual pool slot allocation/return and teardown observed by a real process-exit callback. The bootstrap is source lifecycle evidence, not a native differential comparison.

Five new bodies cover279bytes. Their names, original calling conventions, complete body ranges and evidence were saved in Ghidra and exports refreshed. The adjacent01090054/D62DD0 NativeModelPool startup/destructor already existed; older notes treating those bodies as incomplete are superseded.

Full cockpit construction still requires preparation for persistent camera registrations and viewport views. The helper case has zero08/18 fields and starts with constructed storage. CRT invocation order, full application wiring, native FH3/SEH, original binary ABI and gameplay remain unvalidated.

# Raw string prefix integration BQ

B4E840 now constructs its camera name through the raw string-pool context. This removes the nested typed prefix helper's nonthrowing-cleanup restriction within the documented ordinary C++ source domain. Assignment425F40, concat4261A0 and prefix43C130 gain raw-pool overloads; the existing typed overloads remain available. This extends existing body records with zero new native-body credit.

Worker `5a89873e` was independently reviewed and cherry-picked as `7d9a6804`. Constructor adoption and two saved EH-handler definitions are in `dae81c7bbfb50c9b9675a7e42c6938f2d629c2da`. That exact source passed MSVC Win32 Release and both existing CTests, with all 2555 tracked build inputs unchanged and a clean tree before/after. The worker separately configured and passed one existing CTest.

One reviewed source probe also passed against the combined libraries: real already-published pool, normal prefix bytes/identity, backward-overlap assignment, right/output concat alias, self assignment, explicit returns and pool shutdown. Its source/provider/library pins remained unchanged. The primary built it with Win32 `/W4 /WX /fp:strict` and verified its embedded asInvoker manifest. This does not execute original native bytes, lazy pool/manager creation, cleanup exceptions or the post-effect constructor.

The source preserves current header loads and logical cleanup ownership. Prefix.data is captured before output ownership is armed and prefix cleanup is disarmed; a failure during the normal raw prefix return destroys output once without retrying prefix return. Initial concat-copy failure does not own output cleanup; append failure does. Raw copies use memmove to support BF7680 backward overlap. Secondary cleanup failures propagate the newer exception, outside native FH3 equivalence.

Primary review covered all source/header changes, volatile access helpers, all three native listings, cleanup maps, generated sections, COFF parser and probe. Seven live Ghidra/installed-PE spans and all twelve source/provider pins matched; fifteen call-site rows passed. Legacy assignment/concat/prefix code remains67/219/133 bytes with2/8/5 equivalent symbolic relocations, respectively. Anonymous-namespace path hashes alone are normalized for relocation comparison. All three current raw sections match the worker's generated hashes.

The constructor's5025-byte section changes one byte: its context argument PUSH reads+44h instead of+48h. One symbolic relocation selects the raw prefix overload; all other bytes and relocations remain identical to the sealed BO build. This static comparison confirms the intended adoption, not constructor execution. The earlier BO archive and report retain their exact typed-prefix scope.

Compiler reorderings remain explicit: concat compares already-captured pointers after output clears, and its generated EH state write precedes right.length load. Ordinary C++ valid-header behavior is preserved; original hardware-fault/private-EH-frame timing is not established. Existing typed helpers keep their previous cleanup/overlap domains.

Four existing Ghidra names were retained, original thiscall/fastcall signatures saved/read back, prior comments preserved and exports force-refreshed. C5E849 and C5F371 were defined as their exact10-byte handlers, saved and exported, with no body credit. The initial missing-forward-declaration compile failure and probe link failure are preserved; the latter was resolved by linking real advapi32/shell32 imports, without changing probe source.

Renderer callable binding, completed internal material identity on failure, other provider admissions, terminal noexcept domains, surviving-child disposition and external quiescence remain open. There is no new native fixture, cleanup-exception injection, binary ABI/FH3/SEH, constructor, GPU or gameplay claim. BR20h construction proceeds separately. See `NATIVE_STRING_PREFIX_RAW_BQ.md` and `reports/native_string_prefix_raw_review_bq.json` for the detailed schedule, exact artifacts and sealed build.

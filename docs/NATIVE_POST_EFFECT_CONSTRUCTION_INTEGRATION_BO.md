# Post-effect constructor integration BO

The 36-byte D61EC8 post-effect constructor and its two surface accessors are integrated at `fb6d43da11fa65b13c48f86140bc8feb6c08bdff`. The batch adds three reconstructed native bodies covering 956 bytes. Worker commit `93a66683` was independently reviewed and cherry-picked as `fb6d43da`; main remains separately owned.

MSVC Win32 Release and both existing CTests (`reconstructed_math`, `native_math_differential`) passed at that exact commit, with all 2555 tracked build inputs unchanged and a clean tree before and after. The worker's separate build configured one existing CTest. Neither run executes the new constructor. No tests were added.

The primary reviewed the complete header/source, the 312-instruction normal body, twelve-state logical cleanup map and generated constructor code. Two source findings were fixed: the reserved MSVC inline-assembly identifier `offset`, and inaccessible reference companions needed for external disposition after registration failure. The final source exposes borrowed acquired-reference handles before binding; it does not automatically reclaim completed creators.

Five live Ghidra/installed-PE spans matched their hashes, including both four-byte surface leaves, the constructor, EH funclets/handler and FH3 data. The call auditor checked 47 direct/tail rows with zero failures; twelve indirect rows retain explicit profile/service evidence. All 52 source/provider pins were checked: one provider differs only because the primary checkout uses CRLF while the worker used LF, and its committed blob is identical. Exact primary bytes remain pinned in the build stamp.

The primary object is SHA256 `819b3922f1f73f2ec6ac79f842de256a3fa34815132dc5de7f920eda3a467fd6`. Its 5025-byte constructor section has SHA256 `d89c11f25d3dc5474e7d6bda5ac90bd9368271359c11a265416ed2e585f5083f`, identical to the worker section. The generated initial writes contain only the original count1 store; fields +08/+18/+1C remain untouched before publication. All seven x87 operations retain the single D7A260 load/FST/FSTP sequence and the later FLD1/model/FSTP/camera/FLDZ sequence. This is static generated-code evidence, not a native differential execution.

Ghidra now has saved descriptive constructor/width/height names and original thiscall signatures. Prior names, prototypes and comments are preserved in the annotation records; all three exports were force-refreshed. The two surface leaves were defined and saved. The ten-byte CBFC3B exception handler was also defined and saved, with zero additional body credit. A transient save lock conflict was retried successfully.

The source requires real callable renderer binding at +48, shared canonical owners and actual constant/pool domains. Existing material-factory failure can leave a completed internal material whose acquired identity is not exposed. The typed 43C130 prefix helper still admits nonthrowing nested cleanup; separate BQ work is not part of this tested batch. Secondary cleanup exceptions follow an explicit C++ policy that propagates the newest exception after remaining cleanup, outside native FH3 equivalence. External quiescence and surviving-child disposition remain caller responsibilities.

No constructor execution, optional non-null surface trajectory, injected cleanup failure, drop-in ABI, hardware-fault/FH3/SEH, GPU or gameplay result is claimed. B4E470 and parent B52860/B529A0 source composition remain open. See `NATIVE_POST_EFFECT_CONSTRUCTION_BO.md` for the detailed native schedule and `reports/native_post_effect_construction_review_bo.json` for exact build, review, artifact and archive records. Earlier BN/BO20 and BP validation retain their recorded scopes.

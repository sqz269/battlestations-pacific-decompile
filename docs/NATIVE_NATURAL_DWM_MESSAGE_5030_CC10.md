# Natural owned-window DWM message and stock ordinal 5030 (CC10)

One separately authorized Win32 diagnostic acquired a real `WM_DWMNCRENDERINGCHANGED` (`0x031F`) message for its genuine owned window, delegated the same `MSG` to the pinned stock XLive ordinal 5030, and observed one normal return normalized to false. The unchanged reconstructed drain pump then returned normally with one focus update. Window teardown and exact parent/child process closure completed. This is a bounded source-composition and explicit stock-profile observation.

## Inputs and separate case

The coherent provider/header build is `db3bf29a0c73b046700ce134436ec880c1df5552`. All 666 sealed launch inputs matched before and after the one invocation. Preparation used strict MSVC Win32, the three existing CTests, active `CHECK` assertions, an I386 executable with embedded `asInvoker` manifest, and reviewed whole emitted bodies/relocations. No production source, header, loader configuration, test or native BSP ledger changed for this case.

The ignored evidence directory is `local/output/cc10_natural_dwm_message_case_preparation`. Exact identities are:

- Preparation ZIP: `f5fbb158ac7b0c8adecaa203b993c74de52c2387598224374e2653353217dbf5` (1,434 rows / 1,435 members).
- Launch manifest: `9cb0214f9da9314fc82f82b474b8028ed0ea1dfc64a35b25027f0eb8aaff5c3b` (666 inputs).
- Fixture source: `bf0dceee03138666a8c17b8a8a1b4075697bdadf65f305c13bf00525b2b4fe44`.
- Controller: `f3088014fafce103991ab3c26ff91203d591d60447c02e2bca9cea62a509eea8`.
- `natural-dwm-message-probe.exe`: `c39008dd510a139274dde7407f0fad86889c1c19e88cc124b1214041b18433bd`.
- Separate runtime ZIP: `a980653a1c4717a7ff12afe3dd31fa646675e57bf0202d411f4bc5d616c3faaa` (25 rows / 26 members; 287,420 bytes). Every archived member was independently rehashed; the preparation archive remains unchanged.

The prior WM_PAINT-only diagnostic remains immutable. It refused its actual `0x031F` before any ordinal call. This distinct case used a separately reviewed admission rule; it did not revise or rerun that prior executable.

## Actual message, delegation and teardown

The fixture used existing `GameWindowHost`, `configure_platform_window_00becee0`, genuine `game_window_procedure`, and the actual `BECCD0` drain-pump route. It published its live platform state before reentrant window creation and retained state/procedure/log/profile through `DestroyWindow`. Its schedule was one non-alertable zero-handle five-second `MsgWaitForMultipleObjectsEx` wait and one unchanged drain pump. It posted no message, fabricated no `MSG`, installed no timer, and invoked no SDK initialization.

The actual log records HWND `0x02F52884`, child PID 35136 / thread 27872, genuine procedure `0x10005C80`, class token 50165 and 24 extra bytes. The wait returned `0x00000000`; that wake alone is not message evidence. `PeekMessageA` supplied `MSG` at `0x001AE7F4`, message `0x031F`, `wParam=1`, `lParam=0`, time 295250609 and point (3952, 941). The owned-window admission accepted this exact returned message. Its producer/sender was not identified.

The same `MSG` reference reached real stock ordinal 5030 once. The direct log records one attempt and one normal return, `zero_or_nonzero=0`. The unchanged public API returns C++ `bool`, so the original numeric `BOOL` was not observed. The post-call message address and every logged field equal the pre-call values. Current HWND ownership/PID/TID/thread, state receiver, per-window procedure, class default procedure and 24 extra bytes were rechecked before returning the genuine result. Those checks are established by sequential active-check progress, rather than separate numeric postcheck log rows.

Direct completion rows show acquired messages 1, ordinal attempts 1, ordinal normal returns 1, focus updates 1, and source attempt counter 1. `DestroyWindow` returned and the owned HWND was dead; class unregister and active-state clearing completed. Six expected/reported/mapped file handles were explicitly closed. The final marker is `NATURAL_MSG_CASE_COMPLETE outcome=QUALIFIED_MSG_NORMAL_RETURN`. The profile object was retained until process exit; no native CRT drain or SDK unload/shutdown was exercised.

## Profile identity and process closure

The explicit stock file is `C:/Windows/SysWOW64/xlive.dll`, SHA-256 `79da26ab6b2dc25936c3354087de0ba41da1a8b62924972e9100c29b95d34385`; ordered legacy dependency `payload/msidcrl40.dll` has SHA-256 `623cb6ca98e566357abbd0e76b15713921e1d7e1144c0c4f589a0407c7eff1ee`. Neither basename was preexisting. Expected, reported and mapped backing handles agreed in file ID, volume, size and hash. Stock reported `System32`, while its opened/mapped backing resolved to `SysWOW64`; equality was proved by file identity, without a guessed path allowlist. This does not prove immutable historical physical loaded-image bytes.

Legacy base `0x27500000` contained ordinal 43 at `0x275684F0`. Stock base `0x55D90000` contained ordinal 5030 at `0x55E44E6C` (RVA `0xB4E6C`). Both ordinal addresses had the matching image allocation origin. Stock guard/registered-message/language snapshots were zero immediately before and after delegation. These snapshots do not prove the executed internal branch under mutation or SDK initialization.

Parent PID 45380 and child PID 35136 both exited 0, with retained kernel handles signaled. Kernel image paths/creation identities, exact `--fixture-child --bsp-native-data-handoff=00000194` command, parent relationship and one stdin ACK were verified. CIM's child image path was unavailable; mandatory kernel identity supplied it. Kernel creation `134348983859291177` and CIM creation `134348983859291170` differ by seven 100-nanosecond units, within the reviewed comparison. Final scoped discovery completed with zero recorded processes. A separate read-only scoped CIM query at `2026-09-26T12:13:57.7602599Z` found neither PID nor a child of this exact parent. The actual execution-tool result is archived separately and equals `run.json`: exit 0, no session, one fixture launch, no timeout, termination action or cleanup/capture/hash error.

## Limits

This demonstrates one genuine natural message / real ordinal call / normal false return and source pump completion for the pinned profile. It establishes neither handled-true behavior, a raw numeric `BOOL`, a particular stock guard branch, SDK initialization, later message domains, native ABI/EH equivalence, full application/game activation nor gameplay. There was no retry, forced message, fallback or subsequent runtime. This documentation/report publication adds zero reconstructed native functions or bytes.

# Actual PAK registry owner

`native_pak_registry.hpp/.cpp` reconstructs ten complete logical bodies (663 installed bytes) over the actual 1Ch registry and its raw DWORD slots. `NativePakRegistryContext` borrows the actual manager publication01090AA0 and registry publication010904D8. It creates no private publication, pool, semantic registry, or alternate lifetime domain. Names below are descriptive hypotheses. The original library names are retained.

| Native inclusive span | Bytes | Operation | Original ABI |
| --- | ---: | --- | --- |
| 00736C30..00736CF6 | 199 | getter | cdecl(), EAX captured fast/current slow publication, RET |
| 00BB4FB0..00BB4FE5 | 54 | raw owner constructor | ECX owner, EAX same owner, RET |
| 00BB4FF0..00BB4FF7 | 8 | secondary deleting adjustment | SUB ECX,8; JMP BB5410, inherited flags/RET4 |
| 00BB5410..00BB542D | 30 | primary scalar deletion | ECX owner, DWORD flags stack, EAX captured owner, RET4 |
| 00BB5000..00BB507F | 128 | full owner destruction | ECX owner, no stacked arguments, RET |
| 00BB49C0..00BB49E8 | 41 | temporary base scalar deletion | ECX actual base allocation, DWORD flags stack, EAX captured this, RET4 |
| 00BB46F0..00BB474E | 95 | slot capacity growth | ECX raw0Ch vector, signed capacity stack, RET4 |
| 00BB47A0..00BB47EF | 80 | slot resize | ECX raw0Ch vector, signed count stack, RET4 |
| 00BB3F50..00BB3F5A | 11 | reference base cleanup | ECX primary; JMP actual BD30F0, inherited RET |
| 00BB4610..00BB4620 | 17 | lifetime base cleanup | ECX secondary, RET |

The source adds explicit context and C++ interfaces; these are not drop-in native calling conventions or FH3/SEH replacements. There is no semantic EAX contract for reserve/resize.

BB4FB0 is the layout producer. In order, it writes primary CEB130 at+0, reference count1 at+4, temporary secondary D64188 at+8, final primary D64190 at+0, final secondary D6418C at+8, ordinal100 at+Ch, then zero vector base/count/capacity at+10/+14/+18. All28 bytes are initialized; surrounding bytes are untouched. Fresh table bytes resolve D64188 to BB49C0, D6418C to BB4FF0, and D64190 slots0/4/8/C to BD30E0/BB5410/BB5770/BB5910. The latter two lookup operations are separate work.

736C30 captures the first registry publication on its fast path. Otherwise it calls the existing actual415350 getter, captures that manager's physical section+10, enters it and increments its DWORD+18, then rechecks publication. It allocates1Ch through the canonical allocator, constructs, disarms allocation cleanup, publishes, and captures the current publication plus8 (or null) **before** calling415350 again. BD0C30 receives this captured secondary and the current second manager. It can return from validation; it adds no reference or duplicate filter. Normal release decrements and leaves the original captured section. Only afterward does the slow return reload current registry publication. The application call73D968 and provider-constructor callBB8275 are static incoming evidence; BB8275 reads returned owner+Ch.

BB46F0 clamps signed requested capacity to at least1 and returns if current signed capacity suffices. Its allocation byte count is wrapping DWORD capacity*4. It captures the new base, reloads current count and source base during forward DWORD copying, frees the current old base, then writes the captured new base and captured clamped capacity. It never owns a slot's pointee. BB47A0 grows capacity if necessary, zeroes each added nonnull slot, decrements current count while shrinking, then stores the requested count. It does not clear removed slots, free pointees, add references, or install allocation rollback. All three reserve callers (BB47AE, BB4B58, BB5860) were inspected; only BB5000 calls resize in the live graph.

BB5000 resizes its actual+10 vector to zero, reloads and frees the current backing array, clears registry publication unconditionally, writes secondary CE3818, then primary D64178 followed by existing actualBD30F0's CEB130. It does not unregister, retry vector disposal, zero the stale base/capacity, or change reference count/ordinal. The publication clear is independent of whether publication still identifies the captured owner. BB5410 frees the captured primary only after successful destruction and only when flags&1; high flag bits are preserved at the source interface. BB4FF0 subtracts8 without adding a null guard. BB49C0 performs only the base publication/profile cleanup and optional free of its received pointer, without adjusting it; flags1 requires an actual base allocation.

Getter FuncInfoDB5570/mapDB5560 has state0 -> C86080 (captured guard ->411EE0), and state1 -> C86088 (free captured allocation, then state0). State1 is armed only after allocation returns and is disarmed before publication. Registration failure retains allocation/publication and releases the captured section. The stored C86088 body ends atC86090; rawC86091..92 is its `POP ECX; RET` continuation. RawC86093..9C installs DB5570 and jumps to the existing FH3 runtime.

Destructor FuncInfoDFDC0C/mapDFDBFC maps state1 through CC4328 to BB4610 on captured owner+8/null, then state0 through CC4320 to BB3F50 on captured owner. The vector resize/free is inside state1. If it throws, only these two base cleanups run; there is no second attempt to destroy the vector. RawCC434F..58 installs DFDC0C and jumps to the existing FH3 runtime. These raw handler spans and the allocation-cleanup tail are explicitly qualified as having no stored function. The source expresses C++ cleanup ordering, not original hardware-fault, mutable EH-spill, double-failure, or original CRT exception identity.

The primary repaired the missing BB49C0/BB4FF0 definitions, recreated BB5000 through its actual507F return, and decoded omitted returning-free continuations in BB46F0 and BB5410. Saved readback and force-refreshed exports agree with installed bytes. This worker made no Ghidra writes. Primary repair records are `native_ay_flow_repairs.json` and `native_ay_function_definitions.json`; the report retains all exact current inclusive bounds.

Strict MSVC Win32 `/W4 /WX /fp:strict` build and both existing CTests (`reconstructed_math`, `native_math_differential`) passed. Attempt03 passed eight native/source binary comparisons and759 checks, with one source-only registration-validation exception. All569 pre-execution inputs and588 final physical files were rehashed, read-only, and exactly accounted;121 actual map-selected archive members (93 core and28 Lua) match their frozen/current objects and sources, with217 compiler-observed dependent headers. The full core/Lua/zlib archives are pinned; no zlib member is linked. The precise output, executable, build and seal hashes are in the report. Attempt01 retains a pre-execution link failure from missing Lua/SDK dependencies; attempt02 passed743 checks before the fixture added explicit allocation-liveness witnesses for scalar flags in attempt03. Production source and native bytes were unchanged between successful attempts. The driver is `local/pak_registry_ay/run_fixture.py --repo BUILT_REPO --attempt FRESH_DIRECTORY --build-log BUILD_LOG`. The bounded fixture uses all ten owned native bodies with explicitly listed relative-transfer and absolute publication/IAT operand rebases. It also executes the exact original BD0430..BD047A pop span on the actual canonical manager, reads the captured D6418C profile's sealed BB4FF0 target, then invokes that native/source secondary deleter. Its empty-manager cleanup uses existing actual BD0400 source. This is not a composed PAK canonical dispatch claim: the new D6418C source dispatch binding and integrated replay belong to the primary. Native EH is not executed. No installed archive, complete registry lookup, concurrent access, application startup runtime, or gameplay validation is claimed.

The source-only exception case deliberately gives the genuine manager an invalid end bound, uses the actual CRT validation handler to throw, and observes the retained registry plus released captured physical section before restoring the fixture input for caller disposal. It does not test native FH3 or inject an exception into BB5000; the destructor base-only unwind is established by the complete native maps/funclets and source inspection. The raw100 field's further semantic meaning remains provisional. Source/header/library parity and fixture agreement do not establish the historical semantic `app_init_tail.cpp` runtime fragment as this new raw path.

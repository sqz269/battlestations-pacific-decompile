# Procedural table, lookup and resource source composition (CC10)

One ignored standalone **source** composition passed using the fresh production
Win32 library at baseline `7a6025ddbd7cc062cf6444427cffe21022aaaa4a`. No production
implementation changed and no production body was included/recompiled by the
fixture. This closes the normal integration between the previously reconstructed
table producer, borrowed factory registry, raw-name lookup, both genuine34h
creators and the existing canonical resource ownership providers. It adds zero
new native reconstruction bytes and no tracked tests.

The fixture was `local/output/cc10_procedural_table_resource_composition/probe.cpp`.
It compiled and ran once, PID66336, exit0. The fresh strict MSVC Win32 production
build and all three existing CTests passed. Fixture assertions remain active and
its executable contains the extracted embedded manifest. The final exact source,
headers, production objects, three libraries, fixture object/executable and
manifest were sealed before the first launch. No failure revision or extra case
was needed.

## Genuine normal chain

Actual manager, pool, registry and table publication cells initially hold zero.
The table wrapper BBCB40 calls actual BBC900. Its first raw-name operation lazily
creates and registers the genuine string pool through the same raw manager; the
factory getter then creates/registers the real registry. The fixture observes
the actual manager pointer container holding `[pool, registry]`, in that order.
There is no second semantic lifetime domain, getter substitute or success seam.

The table owns four distinct4B factories, with final D644E8 at+0 and D644F0 at
+4/+8/+C. Real registry nodes borrow those exact factory identities at+14. The
fixture verifies all four registrations and cardinality4 while backing is live.
It neither retains factories nor adds count/ownership metadata to native payload.

Two separate live actual8B raw headers are constructed through genuine41E870
against the same actual pool/manager. Their keys are `CausticsTextureSource` and
`ShoreWaveTextureSource1`. They are independent of the table constructor's stale
temporary raw header. B19E90 performs the actual B19D60/B19B90/443D00 lookup and
reads each current borrowed factory and profile+4 selector. Those profiles call
the production BBC6F0 and BBC810 creators, returning two distinct34h allocations
with final resource profiles D64478 and D644B4.

The published B19980 correction begins the single aligned actual+04
`std::atomic<int32_t>` lifetime with value1 at the original native store. The
fixture does not initialize/reset that count, placement-construct a replacement
payload, copy an original raw count into an atomic overlay, or cache a parallel
count. It checks each genuine creator's zero child-data/count/capacity, +08 zero
and byte+1C zero, reading no uninitialized field.

Each existing NativeProceduralResourceReference borrows the SAME live count, and
both are bound into one NativeRenderActualOwnerRegistry shared with their lifetime
context. Bind is metadata-only and leaves count1. Genuine cache4DDB20 retain
reaches2; actual owner release reaches1. Final actual release reaches0, resolves
the exact existing companion and runs current BD30E0/current BBC6D0/BBC7F0,
C304A0, B19750/BD30F0 and real CRT free. Terminal0 follows the existing actual
decrement and companion's live-zero guard; the fixture performs no dead-count
read after the terminal.

Profile views and native names are generated from the frozen native data spans.
The production dispatchers read these borrowed live views normally; no callback,
private vtable, substituted terminal or copied-native body supplies success.

## Ordered normal disposal

All future registry lookup/table consumption is quiesced after the two successful
lookups. Each independent pooled key is returned once with throwing raw41DD20
while the real pool is live. Raw header release leaves stale fields; the fixture
adds no owning string destructor or second return.

The final resource releases use the same canonical registry. Retirement callbacks
track whether this caller's bind actually succeeded. For these successful binds,
the callbacks unbind exactly the raw identity and SAME companion after concrete
terminal destruction/free, then dispose the retired companion. They read only
metadata after free. Both canonical entries are gone before the registry's
destructor, and all contexts/profile dependencies survive retirement.

The existing BBC5D0 shutdown captures the actual published table once, destroys
its four slots through the genuine qualified helpers/scalars, then frees that
captured table. Publication is unchanged and can contain the freed identity.
The fixture compares pointer bits only and never dereferences or shuts it down
again. The real registry still holds borrowed dangling factory bits, now quiesced;
no unregister, erase, retained factory or rollback is invented.

Actual BD0400 manager drain pops the recorded last registration first. Explicit
registry and string-pool deletion bindings dispose registry names/nodes/tree
while the earlier pool is live, then dispose the pool, clear its publication and
set the return gate. This order is supported by the observed actual registration
order and reviewed production reverse-pop body, rather than intercepted destructor
callbacks. The fixture observes registry/pool publications null and gate1 after
normal drain. It frees captured manager backing through the real CRT and leaves
its publication bits unchanged. Any dangling manager/table publication bits remain
quiesced; no native outer clear operation or application shutdown policy is claimed.

## Metadata failure obligations

This is one successful normal case; allocation, companion/bind failures, source
EH cleanup, changed profiles and nonzero child vectors were not injected.
Companion construction failure leaves count-one backing with the caller. A
completed companion whose bind fails remains live in its guarded phase and may
not simply be deleted. The retirement callback's bind-success flag prevents
unbind of a nonexistent entry; an unbound genuinely fresh zero-child resource
can be explicitly released through its existing companion interface and retired
without registry resolution or invented registration. This is a caller obligation,
not a newly implemented automatic rollback or a tested failure route.

All previously established table current-cell/partial-name/borrowed-registration
failure frontiers remain separate. This success evidence does not account for
arbitrary redirected/replaced backing or prove native double-exception transport.

## Frozen evidence and limits

The readiness archive remains immutable:
`local/output/cc10_procedural_table_resource_readiness_evidence.zip`, SHA256
`5b532ff2b2d8d641c64b8c25f16444ef88b9a33b69cfb778a2ed485bb7167d59`.
It pins27 complete normal native bodies/2171 bytes, nine profile/name data rows,
44 current source/API inputs and the published sibling count producer contract.
Native live/PE equality is static evidence; those bodies were not executed by
this composition. Existing native-math CTest is separate regression validation.

The new archive is
`local/output/cc10_procedural_table_resource_composition_evidence.zip`. Its manifest
includes immutable prebuild55, precompile69 and prerun72 identity checkpoints;
production objects/libraries and unchanged fixture inputs were physically sealed
before fixture compilation, and the final fixture object/exe/manifest before
launch. All22 source object payloads were also extracted from that immutable
precompile library and match the current CMake objects byte for byte; extraction
adds no compilation or runtime attempt. Exact artifact hashes and the build/compile/run logs are recorded in
`reports/native_procedural_table_resource_composition_cc10.json` and the archived
receipts. No probe process remains after normal exit.

This is genuine-provider source integration for two fresh zero-child resources.
It is not original ABI/native FH3/SEH/private-register proof, a native copied-body
comparison, cache insertion/B1A4F0, sampler/B4 or source0 activation, nonzero
child-vector/device/GPU admission, application/global startup or game validation.

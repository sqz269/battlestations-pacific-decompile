# Application particle-clock shutdown composition, CC10

The application now supplies one host-owned actual F8D420 `void* volatile` cell and a retained complete-clock shutdown domain. The existing `00CE7D38 -> 004DE340 -> 00B1B680 -> 004DE290` raw manager deletion route is reused. This packet composes providers; it adds zero native-body bytes and does not call the clock getter, register a clock or activate sampler/application startup.

`GameSingletonHost` owns the stable cell. Its preparation accessor returns the actual reference and performs no getter, publication read, reset or registration. Any future getter or registration must wait until the renderer has installed `native_deletion_bindings().particle_clock`; exposing this accessor does not prove future consumer wiring. The separately integrated getter/adapter correction uses this same actual `void*` reference shape without a typed publication alias.

Renderer `Impl` retains the actual records resize context and shutdown context after `CameraGraph`. Records borrow its SAME `vfs.strings` and existing renderer `validation`, also used by the effect record context. Shutdown borrows those records, the actual host cell, exact 20-byte CE7D08/CE7D24 profile windows and `&owners`, the SAME canonical registry used by actual renderer/resource companions. Producer admission, live atomic counts, real terminal dispatch and exact companion retirement remain separate obligations; this composition creates no owner or reference credit.

The current decrement cell is the existing retained `CameraGraph::decrement`, resolved through the real Windows `InterlockedDecrement` export. The clock and cockpit aliases have the same function-pointer type, so shutdown borrows that exact const cell directly. Calls read its current value through the existing volatile reference interface. This is a source equivalent of the CE2220 IAT-cell contract; the original numeric import word in mapped read-only PE data remains unresolved and is not called. No fake counter, new export resolver or target copy is added.

All new context construction is allocation-free reference binding. Profile admission may throw during member initialization, before a deletion pointer is installed. The constructor checks both cell and binding before the last throwing worker-context bind, then installs the new clock pointer as its final nonthrowing assignment. No later constructor call can throw or strand this pointer in an ended renderer. Failure after later native operations retains existing backing and obligations rather than resetting or replaying them.

Application teardown drains singletons while renderer/VFS services remain live, deletes the host through its fallback drain, then releases renderer and VFS later. Renderer contexts therefore outlive both host drains. Reverse member destruction ends shutdown records before the camera callable cell and canonical registry. The after-drain check observes the SAME cell, including a prepared renderer; it performs no repair.

Native cleanup remains capture-last-sink, current secondary profile/slot read, one current decrement, canonical dispatch only on returned zero, fresh count/data read, current record destruction, current count decrement, resize-zero and array cleanup. Base cleanup unconditionally clears SAME F8D420 and stamps CE3818. Scalar deletion reads flags after destruction and frees captured complete backing only on bit0; returned dead bits are diagnostic only. Source C++ cleanup is not original private FH3/SEH proof. The original explicit `00736930` unregister/slot0 route remains separate from `00BD0400` manager drain and is not wired here.

Original `004DE340` has ECX complete owner, one stacked flags word, RET4 and captured EAX return; `00B1B680`/`004DE290` have ECX owner and RET0. Added shutdown context/reference arguments are source interfaces, not drop-in native ABI replacements. This build uses the 180-byte deletion binding unchanged, an 8-byte records context and a 24-byte shutdown context. The host private cell shifts later source members; renderer gains retained context storage. Production callers rebuilt together; old ignored fixture binaries are neither reused nor claimed ABI compatible.

One fresh strict MSVC Win32 build and all three existing CTests passed. Fifteen source checks and 37 emitted checks with ten relocation checks verify reference identity, final pointer publication and host drain ordering. Two exact separate `bsp_game` application objects and five actual core-library members provide 23 full COFF sections and 17 unique full relocation-masked linked matches. Twenty-two sections fully linearly decode; the existing dispatcher is qualified as 2,972 code/padding bytes followed by 137 bytes of embedded switch tables. Its initial naive decode receipt is preserved. Four unchanged existing provider members and all 41 function sections/relocations match the prior frozen build. The PE embeds an `asInvoker` manifest; existing math checks remain active in Release independently of `NDEBUG`.

Evidence is under `local/application_clock_shutdown_composition_cc10`; the approved read-only proposal remains immutable under `local/application_clock_shutdown_readiness_cc10`. Source/compiler/library/object/link inputs and outputs are pinned around the successful build. Earlier current build paths were verified before authorized replacement; earlier frozen outputs and archives remain unchanged. No new tests or runtime were added. Full sampler/material-program graph, MSG/platform provenance, source0/W, explicit shutdown wiring, application execution, native EH parity and original game validation remain untested or unresolved.

# B674C0 actual Lua string field

`set_native_lua_string_field_00b674c0` reconstructs the complete 97-byte `00B674C0..00B67520` setter over the actual 20-byte (14h) `NativeLuaObjectStorage` and two borrowed 8-byte string headers. The installed PE SHA256 is `b682a82c52f81f957b2c70222077305a933f72481686c88843077f714b956dd6`; the saved original span SHA256 is `ee7bb8eecce3d417d7e13e5ff69d252d39fb71c88d378b61fa1d4817bfafa7c5`. Primary native evidence is `local/dh-sampler-parameter-evidence.json` in the primary worktree, SHA256 `2e67a690558d0376ce6781217e90abf295dab2661bbeeb1131a1abe8a2b47f76`. The original public ABI is ECX object, key/value header pointers in two stack slots, `RET 8`.

The public source ABI keeps that ECX and those two original stack slots. EDX is an explicit borrowed pointer to the actual `0108FF2C` zero byte used when a header's data pointer is null. The naked adapter forwards addresses of the original stack slots to a private C++ body. It does not capture either pointer on entry. Source callers must supply the actual zero-byte binding; the PE position is virtual zero-fill and has no raw on-disk byte.

The private body reads current object owner and current owner state, calls linked Lua `lua_checkstack(state,2)`, and ignores its result. It then reads the key public slot, key length followed by current data, substitutes the bound empty-byte pointer only for null data, reloads current owner/state, and calls `lua_pushlstring`. Only after that call does it read the value public slot and current value header in the same order, followed by another owner/state reload and second `lua_pushlstring`. Finally it reads current object owner, current object index, then that captured owner's current state and calls `lua_settable`. It neither gates on object kind nor changes tracking records. It adds no checked-stack repair or cleanup.

The source links the concrete stock Lua 5.1.1 C API through `bsp_lua511`. Those C calls do not claim the game's private Lua register ABI or original VM bytes. The adapter's EDX context is new; private C++ frame, Lua error transport, and original exception behavior are not binary-equivalent claims. Verification here is Win32 build, existing tests, saved native call-site checks, and generated adapter/body inspection; no game execution is claimed. B1B830 is a separate parent.

Primary validation accepted exact source `acab985894f54eda1946cfecc17861ff0e856beb`: Win32 Release and both existing CTests passed with2613 unchanged tracked inputs. Complete generated entries/private bodies and concrete relocations were reviewed. One linked-Lua source fixture exercised both routines with binary key/value strings, balanced stack, normal global-object cleanup and preserved receiver/padding bytes. The parent source/provider contracts also passed the independent DJ consistency review. No repository tests were added; original native entries, source C++ failure paths, Lua longjmp, native FH3/SEH and gameplay remain unvalidated.

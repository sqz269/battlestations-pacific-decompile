# Native input host integration gap (AX)

This is a static source-routing audit of the current executable checkout, not a run result. Target project is `C:/Users/sqz269/bsp.gpr`, program `/battlestationspacific.exe`. No native function or Ghidra state was changed. The primary's current, uncommitted `include/bsp/native_input_keyboard_storage.hpp` / `src/native_input_keyboard_storage.cpp` supplies the fourteen `NativeInputKeyboardLibrary` methods and is an integration input, not a built or deployed claim from this audit.

## Currently connected

`GameStartupHost::InputServices` owns SDK, XInput, constants and `GameInputRuntime core` (`src/game_hosts.cpp:932-989`). It binds the core backend/action contexts to `GameSingletonHost` before the backend starts. Startup constructs `InputServices`, publishes the core and calls `startup()` (`src/game_hosts.cpp:1470-1474`); OnInitOnce configures three class slots (`:1563`). `GameInputRuntime::Impl` owns actual backend, action records, action owner and device service contexts (`src/game_input_runtime.cpp:11-41`) and exposes backend/action/record/device accessors (`:109-114`). These are source-callable and borrowed by all later input composition.

The separate `GameScriptHost` supplies projected script tables and projected `InputSettings`; the executable reads counts from it (`src/game_hosts.cpp:1326-1332`). There is no call to `get_native_input_settings_005547d0`, `construct_native_input_configuration_00698680`, `load_native_input_configuration_00698a10`, or `GameInputActions` outside their implementation files (repository symbol search). `GameInputActions::Impl` already composes binding, configuration and tick contexts from a `GameInputRuntime`, but no host constructs it (`src/game_input_actions.cpp:30-69`). The settings getter, table parser, defaults, and keyboard apply modules are compiled source APIs, not yet reached from the executable host.

## Exact missing ownership and connections

| Required connection | Current source evidence | Needed production owner/wiring |
| --- | --- | --- |
| Settings singleton | `NativeInputSettingsLifetimeContext` borrows E198E8, 01090AA0 and table services (`include/bsp/native_input_settings_lifetime.hpp:5-9`); `GameSingletonHost` has the raw manager but no E198E8 cell or `bind_input_settings` (`include/bsp/game_hosts_singletons.hpp:24-58`, `src/game_hosts_singletons.cpp:12-40`). Its deletion binding leaves `input_settings` unset, so CF81CC cannot dispatch the settings deleter (`include/bsp/native_singleton_destruction.hpp:72-75`, `src/native_singleton_destruction.cpp:115-121`). | One stable E198E8 publication and context tied to `GameSingletonHost`'s actual manager; install its deletion binding before getter registration and retain all borrowed services through `shutdown()`. Call the getter at the recovered Phase 5 site, before consumers; do not replace the existing projected settings object with a cast. |
| Table script and native storage | Tables require `NativeInputSettingsScriptServices`, a `NativeInputKeyboardLibrary`, SSE2 conversion, constants and three explicit preimages (`include/bsp/native_input_settings_tables.hpp:9-18`). Primary's current `NativeInputKeyboardStorage` handles all fourteen storage calls and borrows a `NativeStringStorage` (`include/bsp/native_input_keyboard_storage.hpp:10-30` in primary checkout). | Own that provider alongside the settings context, with a real pooled/native string service and validated constant/preimage sources. The getter constructs 540h, runs tables, publishes/registers on success (`src/native_input_settings_lifetime.cpp:87-119`). |
| Lua/VFS services | Both script settings and configuration loading require `NativeLuaBootstrapInputs` and `NativeLuaFileServices` (`include/bsp/native_input_settings_startup.hpp:25-29`, `include/bsp/native_input_configuration_load.hpp:21-29`). Those require a callable native 0109CEEC manager, fundamentals callback, DoFile callback and override append (`include/bsp/native_lua_bootstrap.hpp:15-23`, `include/bsp/native_lua_file_loading.hpp:10-15`). `GameVfsHost` currently constructs a projected `VfsProviderManager` (`src/game_hosts_vfs.cpp:54-81`); no `NativeVfsRuntimeBindings` or `NativeLuaFileServices` is composed in `game_hosts*`. | Compose a real raw VFS owner/publication and `NativeVfsRuntimeBindings`, fundamental owner and callable Lua callbacks against the same application lifetime. The projected VFS pointer is not an actual 0109CEEC owner and cannot satisfy these APIs. Keep raw VFS/Lua/string services live through settings destruction. |
| Embedded configuration and action services | The native configuration is a borrowed 524h member of the actual game at `game+3C`, not a separate allocation (`include/bsp/native_input_configuration_owner.hpp:5-25`). Its loader needs parse, tick, settings and defaults contexts (`include/bsp/native_input_configuration_runtime.hpp:6-11`). Parse needs same action owner/record/configuration services, flags and CRT values (`include/bsp/native_input_configuration_parse.hpp:20-27`). A concrete checked-vector provider already exists (`include/bsp/native_input_configuration_storage.hpp:23-28`). | First construct/publish an actual game owner and its embedded configuration at the correct constructor stage, then compose parse/cleanup and tick using `GameInputRuntime`'s actual contexts. Expose or share `GameInputActions`' private binding/configuration/tick contexts without duplicating owner state. Call full loader at its recovered site, not its prefix. |
| Defaults and keyboard apply | `preserve_native_input_default_bindings_006ab820` requires real action owner, binding storage, backend and a keyboard application (`include/bsp/native_input_settings_defaults.hpp:10-25`); `NativeInputKeyboardApplication` wraps raw 006AA640 and borrows the fourteen-method provider and constants/preimages (`include/bsp/native_input_keyboard_apply.hpp:35-79`). `load_native_input_configuration_00698a10` gets settings/defaults only after its action rebind/tick and x360comp gate (`src/native_input_configuration_runtime.cpp:29-44`). | Own one keyboard apply context sharing the same settings receiver, backend and action owner. Preserve the full loader order and real action callback path; no successful no-op service or projected keyboard-restore call is equivalent. |
| Action callback/game timing | `GameInputActionServices` needs the real F8BBFC callback, timing cells and callback object; `GameInputDeadlineCallback` needs actual game publication and deadline map (`include/bsp/game_input_actions.hpp:18-44`). No host constructs either. | Connect the game clock/map publication and the recovered callback writer before action tick/configuration can reach that callback. A null callback or fabricated deadline map is not a complete route. |

## Smallest dependency-complete production packet

Treat raw input script/storage composition as the next **prerequisite production packet**, with disjoint host files `src/game_hosts.cpp`, `include/bsp/game_hosts.hpp`, `src/game_hosts_singletons.cpp`, `include/bsp/game_hosts_singletons.hpp`, and a new owned input service file/header plus one `cmake/startup.cmake` registration. It must first supply the actual raw VFS/Lua/string/fundamentals graph (or consume a separately integrated production raw-VFS owner), construct `NativeInputKeyboardStorage` and `NativeInputSettingsTableServices`, bind a stable `NativeInputSettingsLifetimeContext` to singleton deletion, and call `get_native_input_settings_005547d0` at Phase 5. It must preserve the projected script host independently until every consumer is migrated. This packet is dependency-complete for real settings-table construction, provided the raw VFS owner is part of its scope or already integrated; simply adding the getter call is not.

The following configuration/keyboard packet then owns actual game+3C construction and full 00698A10 call-site wiring, `GameInputActions` context sharing, real F8BBFC/game/deadline-map timing and defaults/keyboard application. Candidate disjoint files are the game-owner composition source/header and `src/game_input_actions.cpp`, `include/bsp/game_input_actions.hpp`; it may borrow the stable input settings service but must not create another settings owner. This second packet is required before claiming that the current executable uses keyboard apply. Its exact call site in the game constructor/loading route must be verified against the owner body before edit; no address lease is claimed by this audit.

The primary blocking dependency for even the first packet is the absence of a production raw 0109CEEC VFS/Lua callback graph in `GameStartupHost`. The first packet also needs provenance for the table preimages and constants; using zero by convenience would change native opaque/stack behavior. The second packet is additionally blocked by absent actual game+3C ownership and callback/map publications. Build success of the individual modules or the primary's keyboard provider does not establish executable reachability. No game or single-instance check was run here.

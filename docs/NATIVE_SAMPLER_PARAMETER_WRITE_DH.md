# Sampler Lua parameter write B1B830

`set_native_sampler_parameter_00b1b830` reconstructs the complete 94-byte body at `00B1B830..00B1B88D`. The descriptive sampler-parameter name is a hypothesis from its callers. Original ECX points at the complete receiver; its actual Lua owner starts at receiver+4. Two public stack words point to the actual key and value string headers, and the entry returns with `RET8`. The source adds an explicit context in EDX and caller-retained diagnostic storage for the native 14h local Lua object.

The concrete B67980 provider constructs the globals object before the wrapper reads the original value argument and then the original key argument. A naked adapter passes addresses of the public argument slots so these reads remain late. State0 arms after both reads; the returned Lua object goes to the full B674C0 string-field writer. The normal path disarms before destroying the current local object with B67700. No substitute VM, callback, fixed-result stub or synthetic tracking update is used.

Installed/live bytes pin the parent and its three direct calls. Private handler `CBC7D8` selects FuncInfo `DF4B64`, whose single state0 map at `DF4B5C` selects `CBC7D0`: `LEA ECX,[EBP-20h]` followed by a tail jump to B67700. Source C++ failure while armed destroys the current local object; a second failure during this cleanup terminates. Failure in the explicit normal destructor is already disarmed and leaves diagnostic obligations for the caller. The source frame rejects replay or destruction with unresolved obligations; acknowledgement releases no resource.

The linked Lua5.1.1 C API is a source dependency. Original register-based Lua ABI, native FH3/SEH and hardware faults are not implemented by this host frame. Lua C longjmp is not converted into a C++ exception and is outside this source catch contract. The explicit empty-string binding represents native `0108FF2C`; that byte is loader zero-fill beyond section raw data, with live Ghidra zero confirmed. VM, owner and header storage must satisfy the existing actual-storage contracts. Source/global-header placement, the extra EDX context and diagnostic policy prevent a drop-in binary compatibility claim. Build, generated-code and focused source validation are pending.

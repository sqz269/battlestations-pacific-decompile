# Game-resource classification integration BX

Addresses: 0071BB40,006F9A80,00721C90,00B86950 and the24 list entries named in
`NATIVE_GAME_RESOURCE_LISTS_BX.md`.

This batch connects the actual game-resource append/classification target to
BW root dispatch and supplies the complete classification-list growth and
insertion dependencies. It reuses existing resource construction, primary
append, string/hierarchy pools, raw node/reader services and allocation domain.

Twenty-eight ordinary bodies total3033bytes. The list families share verified
instruction schedules. Three returning-free instruction memberships were
repaired under the Ghidra write lock, retaining prior evidence. New names are
descriptive hypotheses and correct compiler/library names are retained.

The integration report pins the final build revision and libraries. A single
controlled-child fixture checks actual game-resource composition and24 paired
source/original list scenarios. Native parser/type implementations beyond the
fallback predicate, production type-cell publication, native exception runtime,
the raw manager/loader and gameplay remain unverified or incomplete as detailed
in the two packet reports. No production loading admission was added.

# Original sampler controller preparation (CC10)

The newly wired v3 controller passed one prepare-only run, `v3_prepare01`.
It created no original child. The original-attempt count remains two.
The frozen launch template remains disabled.

All 24 artifact roles are concrete. Helper, source, guard, writer, build and
compiled evidence bind directly to the frozen r2 preparation. The only controller
changes are those paths, the accepted exact fresh observation/log-overlay hashes,
and offline-report wording. Primary review independently verified all 35 indexed
files, 1,050 prior artifact rows and current bound paths/hashes. An AST comparison
confirms the execution protocol is unchanged.

Preparation checked all 67,814 copy metadata/identity records plus current
EXE/xlive/log hashes. Full stream content comes from the separately reviewed fresh
observation. The log overlay accepts only that exact observed row and provenance;
it grants no launch permission and does not permit future drift.

The helper journal contains only adapter, preparation-only and successful result
events. Continuation and entry-transaction counters are zero. Its child-exit field
259 is an unused sentinel because no original child exists. The outer job reports
wrapper exit zero, all three owned processes closed, no timeout and no cleanup
errors. The unchanged profile dry-run restored only its local scratch fixture;
its receipts report live product bytes, security, creation/write times and file
identity unchanged. Reads retain the documented access-time limitation.

No execution backup, launch admission, owned-child exit receipt or consumed review
was created. This establishes preparation only. Original guard execution,
continuation, sampler input and game behavior remain unproved. Any actual attempt
uses a separate primary-reviewed one-run receipt and the unchanged fail-closed
controller. Profile changes would require separate post-run review before restore.

The primary report is `reports/native_original_controller_ready_cc10.json`.
The ignored evidence archive is
`local/cc10-platform-evidence/original-controller-ready.zip`.

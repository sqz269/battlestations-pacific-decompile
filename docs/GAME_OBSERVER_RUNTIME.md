# Application observer lifetime

Addresses:00CCD6A0,00696360,00CD2D80,00BFBC47,00BD0400.

The reconstructed application now creates the actual dispatch owner through the recovered CRT initializer. One `GameObserverRuntime`, owned by `GameSingletonHost`, retains the actualE198DC/E198E0/E198E4 publication cells and the existing observer lifetime. Both deletion bindings use the same application raw manager. The wrapper has no separate native allocator, manager, edge array or implicit native destructor.

Live table bytes establishCCD6A0 atCE2BAC (index286) andCD2D80 settings atCE3054 (index584), insideCE2734..CE36E4. The original__cinit loop atBFBC90..BFBCAE calls nonnull entries in ascending order. `GameStartupHost` now executes the represented observer initializer before settings. This establishes their relative order only; the unimplemented intermediate CRT entries remain separate work.

The context binds deletion services before the initializer registers its raw14h owner. The raw8h observer lock remains lazy until an actual observer operation needs it. CanonicalCF7E64 edges use the existing concrete deleting routine; unknown edge profiles reject an unavailable source contract. Native validation uses the current CRT service and preserves a potentially returning call.

The context remains alive while the application drains the raw singleton manager. Its owning publications are cleared by the actual scalar destructors. TheE198E4 alias is deliberately retained and is never dereferenced by the shutdown diagnostic. The source constructor keeps its temporary singleton host owned until represented initialization succeeds; normal destruction drains before member contexts disappear.

MSVC Win32 and both existing CTests passed. No new tests were added; the final combined executable run will record actual startup/shutdown observations in the report. Previous focused native owner proofs and the mixed-owner source lifecycle remain in OBSERVER_DISPATCH_OWNER.md and OBSERVER_DISPATCH_BINDING.md. Descriptive names are hypotheses and these explicit source contexts are not native global-address or binary ABI replacements.

Actual endpoint delivery, notification scheduling and raw obstacle candidate integration remain separate. No artificial endpoint is registered during startup. Full CRT ordering, native failure/unwind, concurrency and original-game gameplay parity remain unverified. Native registration failure can retain a publication outside the registered list, as the recovered getter does; this adapter does not invent rollback.

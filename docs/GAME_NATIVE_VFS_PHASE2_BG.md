# Retained raw VFS phase 2 composition, BG

`GameNativeVfsRuntime` now retains one four-profile `NativeVfsProviderEnumerationBindings` over its existing physical, FileStore, MPKG and MPAK contexts. It also retains the physical enumeration context, actual `00BDD990` visitor context and package-scan context through the same `01090AA0` singleton manager drain. Its input explicitly requires a real duplicate diagnostic sink and caller-owned readable `0109CEF0` empty-name bytes; there is no no-op logging or projected enumeration fallback. The supported installed PE (`b682a82c52f81f957b2c70222077305a933f72481686c88843077f714b956dd6`) maps this address to initial zero-filled `.data`, beyond the section's raw bytes. The input supplies that initial empty-string behavior, not the original fixed address, global identity or later writes. Numeric visitor data at `00D6846C` comes from the verified read-only data image. The contexts borrow existing owner cells and pools; the composer does not publish a second manager.

`scan_phase2_packages()` executes the actual `0073CB10` package scan against the current VFS publication. `register_phase2_search_defaults()` passes the borrowed `0109CEEC` publication cell so the complete ordered `00738360` source registrations can reload it at each native call site. `run_phase2_mount_scan_search(root)` performs the three established loose mounts, runs two fresh package scans, then registers the search defaults. The MPAK archive-factory/registry tail remains the later `register_archive_factory_tail` call, preserving startup order. The separate methods allow callers to observe each phase without changing its body.

The scan's provider dispatch preserves the captured numeric entry, raw provider pointer, original suffix and extension headers, low-byte flag semantics at the native caller, and actual `0Ch` output vector. Unknown provider entries fail explicitly. Native function bodies and their ABIs are evidenced in the BF provider and BE package-scan reports; this BG packet claims no new native address or Ghidra mutation.

The corrected search-defaults dependency `2bb336a0` is merged. MSVC Win32 `/std:c++20 /permissive- /EHsc /W4 /WX /O2 /MD /fp:strict /c` passed for both the composer and the updated ignored primary probe against the merged headers. Primary commit `9aed5ec4` registered the missing dependency translation units for its pending combined archive build. The probe source runs the full phase and shared drain, but its linked runtime result is pending that build. The installation has the 657-byte loose `scripts/fundamentals.lua` and no discovered `.mpkg`/`.mpak` files, so the probe cannot establish archive payload equivalence. The original SEH/FH3 failure identity, later `0109CEF0` mutations and fixed-address data mapping conflicts remain outside this source claim.

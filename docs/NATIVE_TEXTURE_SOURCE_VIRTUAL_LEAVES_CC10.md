# Texture-source construction and deleting leaves

The bounded packet reconstructs C30210, C30270 and C30550 using genuine existing B19980, B19750 and C304A0 providers. Native bodies total78B/29instructions, with two bit0 conditional branches and five call sites. Ghidra's current listing omits ADD ESP,4 at C30285/C30565 after the free calls; complete byte decoding includes both. The first audit refusal is preserved, and no listing/flow mutation is made here. It supplies ordinary MSVC Win32 C++ entry points, not drop-in register-ABI replacements or callable numeric vtables. Names describe recovered behavior rather than original symbols.

C30210[18B] captures ECX, calls B19980, writes D79AEC at +0 and returns captured self in EAX/RET. The existing provider begins the single +04 atomic lifetime with1 at its original write; caller supplies fresh, four-byte-aligned, exclusive backing of at least8B, with no count users/canonical companion. The leaf changes no other bytes, allocates nothing and constructs no source payload or complete derived owner. Its raw base/profile access assumptions and the complete-owner boundary remain those of the existing provider.

C30270[30B] captures ECX, completes B19750, tests the low-byte flags bit0 and frees the captured owner through the genuine CRT domain only when set. C30550[30B] has the same outer schedule around existing C304A0. Native ABI is stacked flags, RET4, EAX captured owner even after free; source returns those raw pointer bits without dereferencing freed storage. No count write/decrement, automatic canonical unbinding or companion deletion is added by either wrapper.

C30550 reuses the SAME actual C30470-created payload and current canonical child domain. The caller supplies a fresh retained `NativeProceduralResourceLifetimeOperation`; its function/site remain the delegated C304A0 diagnostics. The wrapper never rewrites them to invent a separate native state or cleanup action. Only successful destroy return permits owner free; the free marker is then recorded. Failure preserves the existing array/base cleanup, partial count/child effects and retained failed-operation obligations, without owner free or replay. Nonempty children still need genuine live slots/+04 atomics/current terminal providers; the wrapper does not admit null-child disposal, raw payload adoption or complete-owner lifetime.

Read-only D64494, D644D0 and D79B70 contain C30570: they are producer entries at D64478/D644B4/D79B54+1C, not profile starts. The independent prefix reads prove D79AEC+04=C30270 and D79B54+04=C30550; caustics/shore profiles instead publish existing BBC6D0/BBC7F0 at+04. The new standalone leaves close the base deleting bodies without installing DWORD tables as C++ vtables or wiring all callers. The requested refs' following type routes C303B0 and C302F0 are relative+0C/+10 (actual profile+28/+2C), external to this packet. The initial mistaken C30550 first-word assertion and its failed collector are retained.

C30310[71B], C30360[80B] and C30460[12B] were inspected completely but left unimplemented. C30310 sets guard0109E9C0 before writing its name/calling missing B19C70, copies current F8D428/F8D42C fields and consumes the genuine6FAC20 counter. C30360 similarly sets0109E9C1, calls C30310 on0109E9C4, copies its three current words and consumes the same counter. C30460 captures its destination, calls C30360 and returns it. Existing logical type-counter services do not close B19C70's actual ShaderDataSource descriptor/storage/parent contract. No callback substitute, second counter, atomic guard, reset/retry or invented descriptor is supplied. A failure leaves the original early guard/partial-write schedule; that boundary needs its own concrete provider packet.

For the two omitted ADD ESP sites, live instruction-context reports no instruction and live function-by-address reports no enclosing function. The gaps are undisassembled and excluded from stored function bodies, while the neighboring calls resolve to their wrappers. BF65AC is a one-instruction JMP BF9DC8; both expose `void __cdecl _free(void*)`. Exact no-return/flow-override property queries were refused by the disabled script endpoint and are retained as unknown; the report-only gap helper does not prove their property values. No shared CRT flag or call-site override is changed.

The prior clean c21f build's four outputs were archived before sync. After the producer handoff, this packet built once on13c41f6a13b57cdad6694ae4ea9b30e5b50544a9. Strict Win32 and all three existing CTests passed. No producer C30570 query/edit, original-game/controller/leaf-runtime launch, new test or Ghidra mutation occurred.

Compiler inspection covers three complete unique new function sections (112B), all five REL32 call targets and full bit0/free/return ordering. C30550 stores the diagnostic free marker only after successful free, without reading the freed owner. Four exact current object/archive-member identities are frozen; all three reused provider raw objects and99 function-symbol byte/relocation comparisons equal the preserved c21 build. This comparison names the historical build honestly rather than attributing intervening source changes to these leaves. None of the new own symbols is selected by the current bsp_game map; the packet establishes library emission, with no linked-body equivalence or leaf-runtime claim. Native register ABI/private FH3, complete caller dispatch, raw complete-owner admission, nonempty teardown and gameplay remain untested.

Ignored evidence: `local/cc10_texture_source_virtual_leaves/` contains raw wrapper queries, exact full native body/table bytes, branch/call coverage, source/build/tool pins, compiler output, before-sync preservation and frozen manifests. Root owns native naming/annotations/accounting and integration; no shared ledger change is made by this worker.

# Material effect numeric resolver frame binding

The existing B2EBB0 loader can explicitly select the reconstructed BDF4C0 VFS resolver while retaining a distinct acquired frame for each load. The selected provider resolves the same actual 8h header and captures the current manager once. The callback provider remains available through the original helper; configuration must select exactly one provider. There is no fallback between providers.

`NativeMaterialEffectLoadAcquired::name_resolution` owns a concrete `NativeMaterialEffectNameResolutionCall`. Its incomplete VFS acquired type has out-of-line construction/destruction. The numeric call validates the shared string domain, current-manager publication cell, device backlink and logging string domain; attaches a fresh acquired frame; then reads the current manager, records the manager/header identity and started flag, and calls the genuine `resolve_native_vfs_existing_name_00bdf4c0`. A result, including false, records returned/result. An exception records failed and preserves the acquired frame. Frame creation or invocation cannot replay.

`bind_native_material_effect_vfs_name_resolution` performs domain validation before clearing the callback/context and publishing the explicit numeric context. Configure at quiescence, with no concurrent call or mutation. All referenced contexts, services, publication cells, manager/backing and live provider dependencies must remain valid through the call and required continuation. It does not construct a manager, mount a provider or manufacture a resolver result. The old two-argument helper rejects numeric selection; the retained overload requires the actual load's stable resolved header and shared construction/program strings.

The Name cleanup sequence stays unchanged and armed during resolution. A failed frame may retain diagnostic pointers into caller string buffers after cleanup; those pointers must never be dereferenced or replayed. Retain the failed metadata and all still-required provider backing; this change supplies no automatic recovery or cleanup. The later B46950 call still disarms the resolved Name and marks it retained immediately before the call; normal return restores its cleanup. Its failure retention and store order are unchanged.

Native evidence is the accepted read-only readiness archive `local/post_effect20_immediate_provider_readiness_cc10/evidence.zip`: B2EBB0..B2EED2, 803 bytes; BDF4C0..BDF588, 201 bytes; and B2ED13..B2ED22, 16 bytes. Each complete range matched installed PE and live Ghidra with complete instruction decoding. The bounded window loads current manager from F10C28, passes the stable local header, and calls BDF4C0 at B2ED1E. Original loader ABI: unused ECX registry, name/ignored DWORD on stack, EAX effect, RET8. Original resolver ABI: ECX manager, header on stack, AL boolean, RET4. New C++ contexts/frame APIs are source reconstruction boundaries, not binary replacements. Names are descriptive hypotheses. Existing bodies retain their earlier reconstruction credit; the new ledger record credits only this 16-byte composition window. Ghidra remained read-only.

Validation used baseline `23238ecba3c8a3248699cc168bd113ef2dfc8334`. Frozen source inputs, the strict MSVC Win32 build, and all three existing CTests passed once. Production source did not change after that build. `build01.inputs.json`, `build01.receipt.json`, and versioned compiler/run manifests pin source/header/object/library/compiler/linker/manifest/executable inputs. The ad hoc fixture rejects NDEBUG, uses `/UNDEBUG /W4 /WX /fp:strict`, and embeds an asInvoker manifest.

The initial provider fixture PID42240 exited 2 before VFS/resolver execution because the readonly data band was unavailable. Its precise refused band and occupant were not captured and remain unknown. No behavior is credited to that run. The first preparation attempt also remains preserved: the empty-name byte lies in the PE's zero-filled virtual .data tail, not file-backed data. The corrected preparation establishes that boundary instead of substituting arbitrary content. `attempt01_frozen.zip` preserves both preparation and runtime evidence.

The separately approved prerequisite correction uses the existing controlled `GameNativeDataBootstrapChild` and one-use handoff machinery with the exact ReadOnlyV1 span CE2000[1202980]..E07B23 and all 19 bands CE0000..E00000. A fresh run02 directory preserves relative child logs. All bands were MEM_FREE in the suspended child before reservation; no occupied range was replaced and no span was narrowed. Parent PID54252 received the mapper ACK and child PID99648 exited 0; the parent exited 0 and both were closed. No original game was launched.

The unchanged provider-case body uses the genuine VFS runtime/core, actual physical provider pool, current directory mount and pooled string headers. Two call records execute genuine actual-manager BDF4C0 true/false resolution, preserving the same headers/current capture and distinct completed frames. One deliberate host dispatch throw checks source failure: the exact acquired frame remains, replay is rejected and caller string cleanup is explicitly consumed once. ExitProcess retains remaining backing. This is provider-only call-record evidence; the binder, retained overload, B2EBB0, B4E470, native EH/SEH, aggregate teardown, cold effect startup and game behavior were not executed. Original source0/W preimage obligations remain unresolved.

`compiled_inspection_run02.json` contains all 462 complete emitted BSP COFF sections from five exact frozen library members and 61 selected linked sections with relocation-masked equality. Compiled call/order checks establish frame attachment before current manager read, direct genuine resolver selection, loader call-record selection, and unchanged B46950 retain/disarm/rearm order. Four Name helpers are byte and relocation identical to cached prechange sections. Static inspection qualifies unexecuted glue; it does not expand runtime claims.

The ignored packet folder `local/material_effect_resolver_binding_cc10` contains the final archive/index/receipt, both fixture versions and runs, first refusal archive, input manifests, all compiled evidence, accepted design/readiness archives and prior archive preservation receipt. The report is `reports/native_material_effect_resolver_binding_cc10.json`.

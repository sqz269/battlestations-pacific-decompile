# Post-effect geometry BT and frame binding BU integration

Four reconstructed native bodies cover 605 bytes: B4D2D0 writer (483), B4D4C0 mapping wrapper (59), B4E3D0 frame assignment (59), and B4CC00 count getter (4). Worker BT source 8b4b9db7 and doc correction 099552aa were integrated as f6ab4a1a and c0139fe8; primary BU source is 9ac26670. Main remains independently owned.

MSVC Win32 Release and both existing CTests (`reconstructed_math`, `native_math_differential`) passed at exact source `f6ab4a1ac5889c14888caaadb3c10badaf352f3d`, with all 2563 tracked build inputs unchanged and a clean tree before/after. The later doc correction changes no build input. The worker separately ran its one configured CTest, comparing 2565 inputs captured during/after build; its 2570-input fixture guard has a different recorded scope. No repository tests were added.

The primary independently reviewed all BT source/native instructions, the complete probe and recipe, COFF parser and generated setup/reloads. An independent Astra worker reviewed BU source, its 24-instruction setter, two-instruction getter and real frame-owner provider closure; no blocking findings remain. The primary checked 13 live/PE spans and 26 source/provider entries. Eleven direct call rows passed; five indirect sites retain explicit IAT/profile/provider evidence and were mechanically skipped.

Both compiled BT functions retain the byte-identical 414-byte native loop, SHA256 695336baf725bc251f941bf55c99e3dde23a01e699c14f2028323b36c0441c9a. The 477-byte writer and 723-byte wrapper sections and symbolic relocations match the worker. The loop preserves alias-sensitive MOVSS loads/stores, eight x87 duplicate conversions, SUB/JNZ loop flags and the late color load. The wrapper uses real B49980/B49A80 providers and fresh current +20 after mapping and +18 after writing, with no automatic unlock on failure.

The primary rebuilt and reran the reviewed writer probe against its exact current libraries. One two-record overlapping trajectory uses a wrapping first-output index and signed-zero/SNaN/subnormal words; all 768 buffer bytes per path agree. Both paths produce CW 037F, SW 0003 and MXCSR 1F80. The 432 guard bytes, both constants and 483 mapped code bytes remain unchanged. Before execution only the two proven absolute constant operands were relocated into one shared read-only constant page; all other original bytes and relative control flow stayed intact. Source/library/fixture pins remained unchanged and the Win32 executable's asInvoker manifest was verified. Root snapshot bytes exactly match the worker's final result.

Two prior worker attempts remain archived: a fixed mapping was unavailable before native execution; the next overlap placement matched bytes/state but failed the requested denormal-coverage assertion. The final input starts one record before output, consuming original special values before overwriting the next input record, and passes both invalid/denormal status checks. This is one focused writer fixture, not wrapper or GPU execution.

The 287-byte compiled BU setter preserves publication before incoming retain, a fresh decrement target afterward, captured old identity, current virtual0 and reloaded deleting slot, followed by existing B1FCF0 flags 1. It performs no post-free access or extra clear. Its getter is exactly `8B 41 20 C3`, without relocation. Source and generated-code review are static; no setter or final-zero disposal trajectory was executed.

All four descriptive Ghidra names and original conventions were saved/read back, prior comments preserved and exports force-refreshed. Writer is stdcall with four original words; wrapper/setter/getter use thiscall metadata. The new naked count leaf uses the original ECX/EAX/RET behavior. No listing repair or EH body credit was introduced.

Valid mapped storage, current renderer/synchronization/physical domains, nontrapping FP controls and the concrete D5E600 frame/surface/CRT provider domain remain prerequisites. The compiler can defer the wrapper's unused input argument read for zero count and reuses the BU incoming argument slot as scratch; private argument/frame aliases and hardware-fault timing are outside this new C++ ABI. No wrapper/GPU execution, setter/injected failure, unmasked FP traps, native FH3/SEH, drop-in ABI or gameplay result is claimed. BV submission and B529A0 update composition remain open.

See `NATIVE_POST_EFFECT_STREAM_GEOMETRY_BT.md`, `NATIVE_POST_EFFECT_FRAME_BINDING_BU.md` and `reports/native_post_effect_bt_bu_review.json`. The sealed attempt retains exact primary build inputs/libraries/objects, source/probe reviews, saved annotations/exports and copied worker evidence including failed attempts. Five large worker library/executable artifacts were hash verified in the preserved worker tree but are not duplicated in the archive.

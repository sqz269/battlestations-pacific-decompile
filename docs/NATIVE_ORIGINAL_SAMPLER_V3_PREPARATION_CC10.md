# Original sampler v3 preparation

The original-specific diagnostic helper is compiled and reviewed, but has never
been invoked. Both frozen controller templates disable launch; missing fresh
copy-observation and overlay bindings prevent preparation or execution. Original
attempts remain2. There was no attach, profile operation, private-log observation,
overlay refresh, copied-game mutation or admission consumption in this packet.

The guard checks the exact owned primary pre-entry first-chance invalid-handle
event. It requires original xlive's complete172-byte body with its five HIGHLOW
fixups, five complete system-provider bodies, current three-hop CloseHandle IAT,
native/WOW64 context and stack relationships, and the original caller's current
module/header/size/protection locals. It verifies mapped PE fields, committed
image/stack ownership, copied-file identities, hashes and exact loaded paths
through the existing module registry. The whole armed32-byte entry window and
initial20/current40 protection are required. Before/after contexts and entry
bytes must match. The guard makes no code/context write or continuation call.

The byte writer matches the four-case benign fixture in source and masked linked
code. It preserves the protection actually returned by VirtualProtectEx. Normal
entry restoration requires40 after one confirmed special continuation; cleanup
requires an owned pending event and no incomplete write transaction. The unused
already-original cleanup path now labels read-only verification separately from
an actual restoration. General exception forwarding/second-chance closure, owned
jobs/handles, marker capture and authenticated backup requirements remain.

Primary review found that revision01 formatted diagnostics after successful
ContinueDebugEvent but before clearing pending state. A formatting allocation
failure could therefore enter cleanup with a stale stopped-event flag. Revision2
clears pending state and advances confirmed counters using scalar operations
before formatting. Failure leaves the event pending. The complete original
40-file package remains immutable. The cleanup assignment is correctly ordered
in source but optimized away because that state is dead afterward.

Independent verification covers the accepted56-file revision,987 prior indexed
rows, seven pinned disk providers and exact COFF/PE identities for the6709-byte
writer,10219-byte guard and26504-byte event loop. The three actual imported
ContinueDebugEvent calls and success-path stores/counters before the first
formatting call were checked. Strict x64 compilation and embedded manifest pass.
This packet adds no tracked runtime tests and invokes no helper or fixture.

The controller has24 artifact roles:22 bound and two explicitly unavailable.
The primary repeated pure binding and disabled-review rejection checks without
importing the outer/profile providers. The historical run02 evaluation has11
supported predicates and10 unavailable live predicates; its result remains
UNAVAILABLE_NOT_ADMITTED. Disk bytes and benign fixtures do not fill those gaps.
Fresh observation/overlay wiring and any actual original capture are separate.

No original CloseHandle result, subsequent entry, source0 sampler input,
architectural equivalence, recursive EH/provider closure or gameplay proof is
claimed. The public receipt is
`reports/native_original_sampler_v3_preparation_cc10.json`; the indexed evidence
archive is `local/cc10-platform-evidence/original-sampler-v3-preparation.zip`.

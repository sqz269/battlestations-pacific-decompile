# Global-config diagnostic-discard precondition

Four comments in `include/bsp/native_global_config_load.hpp` clarify that `NativeGlobalConfigLoadOperation::discard_retained_state_for_diagnostics()` requires an independently valid Lua state. A failed/running phase or last call site is not a restoration token. After foreign C++ escape through Lua C frames, the method is not admitted as recovery: its object releases and close can execute Lua, and it establishes neither restored state invariants nor finalizer-service safety.

The C++ diagnostic helper has no claimed native address or new native-byte/function credit. Non-comment tokens, API, failure/cleanup order and behavior are unchanged. No guard, error transport, protection conversion, replay, rollback or new retirement mechanism is added. Existing raw fatal/current-handler selection remains intact; direct fundamentals-getter failures remain distinct from foreign exceptions crossing active Lua C frames.

The frozen negative audit is `local/cc10_lua_foreign_exception_retirement_readiness/PROPOSAL.md`, archive SHA256 `2282c5999fb4f716fb75d0eb9414cf8b06c41870609e2f86a4ce80ae47edc902` (30 rows/31 members). It records the missing owning invocation/restoration values, alias quiescence and finalizer/provider lifetime contract. It does not categorically infer Win32 longjmp destructor, `__finally`, Activation or original private FH3 cleanup.

One strict MSVC Win32 build and all three existing CTests passed. Exact token comparison confirms the four comments are the only header change. No new tests, Lua execution, runtime or application activation occurred. Evidence is under `local/cc10_global_config_discard_precondition_implementation/`; before/after source, Git blobs and actual build receipts are frozen. The prior32c607 build is separately preserved; the new9287a5d58 baseline includes intervening source changes, so binary differences are not attributed to these comments. Historical09ed evidence retains its original limits.

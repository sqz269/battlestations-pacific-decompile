# Guarded stock-profile declaration composition with singleton tombstones

One controlled source composition passed the genuine B317E0 declaration-name miss and hit, canonical count/terminal retirement, VFS deletion, both raw singleton tombstone observations and final CRT cleanup. Parent PID 72316 and child PID 88716 exited 0 with exact handle-based closure and all 522 launch pins unchanged. This metadata packet credits zero native bytes and changes no production C++, default loader, tracked test or game installation.

The explicit coherent snapshot is `09edb1f1da09abfbbcc8562b7d0b3ad4eff8cb60`. Its current headers were rebuilt with all 71 game objects before linking. It includes the then-current private host layout, the 180 B deletion binding and 44 B material compiler borrowed view, and precedes later independent clock fixes. Newer headers or clock objects were not mixed into this case.

The approved ignored fixture source `e44ef8a115922e83c9a7024756a1c02d61c074d7caadbbcbcd5359d34f7fba90` differs from reviewed design 597c only in three declared log directory strings. The unchanged reviewed controller is `3aed684c8ed65542898fb506bbc52f8d02dea33191ee39bfb4966a5754edf4a5`. The I386 executable is `4eb6aa06d40cd7d9b3e425787eea36e7433439832339d675b71d9de78898d5af`, 676864 B; its embedded asInvoker manifest and active CHECKs were inspected.

The existing strict Win32 build and all three existing CTests passed. One fixture compile/link passed. Inspection matched 531 selected project-library members with current CMake objects and qualified five production bodies, three file-identity helpers and four observer/case/audit bodies, with eight actual imports. Primary independently resolved all 635 relocations in these 12 whole linked bodies to exact map targets. The 137 precompile pins remain distinct from 384 actual showIncludes header identities acquired after compilation and five source identities acquired after linking; their compiled library member bytes were already sealed. No recursive source/header closure or extra build was used.

The genuine case remains B317E0 → B305F0 → B2DBD0 using B48AF0's same actual owner+4 atomic, real cache and canonical owner registry. It checks credits1→2→3→2→1→0, real B32210(flags0) terminal/exact unbind/retirement, 32 free declaration slots, raw name cleanup and one BE25C0(flags1) VFS deletion. The CASE marker reached after these active CHECKs; intermediate count values are qualified by that inspected sequential source and marker, rather than separate value measurements.

Raw BCF910 measures `(end-begin)/4`, including null tombstones. BCFCA0 writes the matching slot zero without changing the end pointer. The existing BE25C0 → BE1F60 → BDA790 path genuinely unregisters the current VFS identity before outer free. BD0400 later decrements the raw end before dispatch and skips zero slots. The preserved native/source audit is `local/output/cc10_stock_case_post_vfs_manager_count_audit/evidence.zip`, SHA `9979df0a4f9c544c6eedef02b0b773f0f659b505d6385fa17d9e116c0217d0ce`. Its BE1F60 export truncation is qualified by the pinned original PE tail, not a new Ghidra repair.

The fixture captured the actual manager and both registered identities after genuine VFS/pool registration, without assuming registration order. These observations were logged:

| Boundary | Manager / begin / end / capacity | Raw length | Slots |
| --- | --- | --- | --- |
| Initial capture |00570d28 /005849b0 /005849b8 /00584db0|2|[0056ea80,02787020]|
| After VFS deletion |same captured geometry|2|[00000000,02787020]|
| Before final manager drain |same captured geometry|2|[00000000,02787020]|

VFS index 0 was a genuine zero tombstone; pool index 1 retained the captured live pool identity. The freed VFS identity was compared as a word and never dereferenced. No vector normalization, reset, re-registration or extra VFS shutdown was added. Final CRT audit logged all 25 exact token buffers returned while gate0, pool ring 23→48, declaration-pool unlink and real BD0400 manager drain, followed by publications null/gate1. Both CASE and final CRT audit markers reached.

The explicit XLive profile requested `C:/Windows/SysWOW64/xlive.dll` with SHA `79da26ab6b2dc25936c3354087de0ba41da1a8b62924972e9100c29b95d34385`, preceded by the existing dependency API's exact local legacy MSIDCRL40 payload with SHA `623cb6ca98e566357abbd0e76b15713921e1d7e1144c0c4f589a0407c7eff1ee`. Neither basename was preexisting. No profile fallback, installation, download, forced message or SDK initialization occurred.

The helper held read-only expected handles before loading, pinned same-handle size/hash/file identity and rechecked them. HMODULEs were MEM_IMAGE with the matching AllocationBase. Reported and K32 mapped NT filenames were separately opened and matched to the held expected file object. All six guarded handles closed normally.

| Profile | HMODULE | Volume / file index | Ordinal address / allocation |
| --- | --- | --- | --- |
| Legacy MSIDCRL40 |27500000|12bda9c6 /0001000000aac5ad|43:275684f0 /27500000|
| Stock XLive |55d90000|e8dcd6e0 /0009000000155f68|5030:55e44e6c /55d90000|

Stock reported `C:\Windows\System32\xlive.dll`; the mapped NT name was `\Device\HarddiskVolume3\Windows\SysWOW64\xlive.dll`. Expected, reported and mapped opens matched file ID, size and SHA. This is guarded current filename/backing-file correlation, not historical physical loaded-section identity or full relocated-memory integrity. No guessed alias allowlist or redirection override replaced the check.

The actually logged platform observations are two BECCD0 pumps, two focus callbacks and **zero actual ordinal5030 calls**. This case supplies no ordinal5030 invocation or SDK initialization credit. It used no posted message, input/online activation or sampler/source0 activation.

The retained kernel child handle pinned PID/path/FILETIME. CIM supplied the exact parent and `--fixture-child --bsp-native-data-handoff=00000184` command; its ExecutablePath was unavailable and its creation time differed from the kernel value by 5 ×100ns. The optional-CIM rule retained mandatory kernel identity. One ACK preceded the existing bootstrap's resume. Parent 72316 / child 88716 both exited 0; no timeout, termination, cleanup or hash-audit error occurred. Final exact discovery and both handle audits reported closure complete with zero remaining owned processes. The actual exec tool return was captured directly: chunk `a8905d`, exit 0, 3.8252474 s, no session.

The existing native-data bootstrap/handoff checks were accepted. This declaration log does not contain an explicit BAND BEFORE/MEM_FREE occupancy row, so none is claimed.

Ignored immutable evidence under `local/output/cc10_stock_xlive_tombstone_case_preparation`:

| Evidence | SHA256 | Rows / members |
| --- | --- | --- |
| preparation_evidence.zip |b73baeb3ce9a6fd56a2db8d8f6fc498a4e6625d43b97ff8142427cdbfcbe44f7|622 /623|
| before_run_inputs.json |00989ad5d775f60bb19fcc34454d7cc89ee93876ebf35c7cf6555f07659fc914|522 current pins|
| runtime_evidence.zip |3e9bc26d05bf7c6164b5fe834310e53bcb5934af01903b2c748d37220f78507e|19 /20|

All direct archive rows were reread and rehashed; the runtime ZIP embeds the immutable preparation ZIP and exact release, raw tool result, logs, identity/ACK and closure receipts. Prior failed path/controller/raw-count cases and their evidence remain unchanged and are referenced by the previous file-identity composition packet. This success closes this explicit source fixture's tombstone expectation; it is not a copied-native ABI/EH, arbitrary alias-domain, message-pump, full startup or game validation claim. Quiescent caller dictionary/owner lifetime obligations remain separate from the actual observed teardown.

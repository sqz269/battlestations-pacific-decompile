# Second bounded original sampler capture

The second original-game attempt closed before the executable entry rendezvous. It captured no sampler input. One new owned child was created, bringing the confirmed total to two attempts. The first run remains preserved in `NATIVE_SAMPLER_ORIGINAL_CAPTURE_CC10.md`; the independently exercised first-chance policy is in `NATIVE_SAMPLER_EXCEPTION_POLICY_CC10.md`.

The primary reviewed the complete v2 controller and its reporting correction before authorizing exactly one run. Its distinct one-use receipt binds the run name, layout and 13 absolute artifact paths/hashes. Fresh profile backup/authentication, ordinary-copy metadata/identity checks and executable/XLive hashes precede launch. The retained wrapper and helper protocols remain unchanged apart from the separately reviewed exception policy. Launch reporting distinguishes attempted launch from a confirmed created child and leaves incomplete evidence unknown. The final preparation run created no original process.

Run02 created PID34772/TID97404. First-chance `C0000008` at `7FF9CFC64EBA`, flags80h, was forwarded as `DBG_EXCEPTION_NOT_HANDLED`. A second-chance record followed with address `CFC64EBA`, the low32bits of the first address, and the same flags. The helper then closed its owned process. Helper exit1, child exit125, both jobs active0, owned-handle wait0, no detach, timeout or cleanup error were recorded. All53 loaded-module hash records succeeded, including the copied `xlive.dll` identity.

The first exception record maps to native `ntdll.dll`, with current code bytes and native/WOW64 contexts and stack windows retained. The second raw record address is MEM_FREE; its code read returned0/32 bytes with error299. Its native context points into `wow64.dll`, while the WOW64 context remains available. These observations do not establish the causal API, caller, handle or module. Context capture requests are not a full-XSTATE or complete-unwind claim.

The owned entry patch was restored from CC to E8. Cleanup reports protection20h and compares all32 expected entry bytes with no cleanup error. It does not emit a separate raw post-cleanup readback. Initial entry protection20h and cleanup-observed prior40h remain unexplained. Entry rewind and hardware-marker arming were never reached.

The scoped Documents/Battlestations-Pacific diff is empty; options retain SHA256 `ff5cf3959932cb634374b8a9bf5e6c0d04c50699cc6ba585e7fa7969f7b563d8`. No profile restore occurred. This does not establish complete Windows/XLive profile isolation or an exhaustive post-run write audit of the original installation.

The private copy retains67,814 files. Only the already-empty `bsp_debug.log` write timestamp changed, from1790245832039167000 to1790248352714395600. Its empty-stream hash, file identity, creation time and attributes are unchanged; the other67,813 file metadata/identities match, with no new or missing files. This comparison is not a fresh full-content hash of all unchanged files. Causal attribution remains open. The previously reviewed exact overlay now fails and was neither refreshed nor restored.

The integration receipt is `reports/native_sampler_original_capture_run02_cc10.json`. Its immutable local archive includes all61 controller/preparation-index artifacts, all24 run-index artifacts, both indexes, the primary authorization and closure review. The primary reverified every indexed byte count/hash and all13 bound artifact identities. No additional original execution was performed for closure, and no universal sampler value, successful startup, compiler activation, native ABI/FH3 or gameplay claim follows from this run.

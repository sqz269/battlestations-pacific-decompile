# Main-menu callers, picture ownership and GUI input

Addresses: 004BA6D0,00AA3910,00AA5D70,00A9BEC0,00598B60,00585810,
00585B40,005E6F70,00594BF0,00582280,005823B0,005896F0,005C6A70,
005C89B0,005C81F0,005C7B80,005C8AE0,00AA2660,00582F30.

The menu now has reconstructed activation and objective-row callers using the
same screen, selection globals, Listbox and Text owners. Canonical command
services bind these callers after their shared objects exist, and reject
missing or mismatched bindings. Activation preserves checkpoint focus-last
on every prompt arm and reloads the current manager/profile at native call
boundaries. Remaining session, page and checkpoint providers stay explicit.

The layout owns one intrusive description list at each of280/298/2A4. Both
the row builder and existing listeners use these exact head/count cells.
594BF0 clones real Text owners, preserves category/header ordering and native
row data, and uses the actual Listbox clear operation. Its secondary/hidden
headers still appear when their category is empty; every hidden row reads the
same first copied score-map value, as verified in the image. Full Text clone
and content/resource completion are required; this synchronous caller does
not resume itself after a pending child operation.

The spacing producer was found in582F30 after current14 (normally5861B0),
outside the layout binder. `compute_main_menu_spacing_00582f45_fragment`
uses authored Text positions and measured normalized heights, retaining the
binary64 difference spills before the final binary32 field stores. It writes
the same2B0/2B4 optionals and returns the captured470 continuation receiver.
The remaining registration phases are documented in MAIN_MENU_SPACING.md.

Mission records now retain actual native picture textures and UV storage.
Copy construction, assignment, reread and destruction use the same +04 atomic
and current0 owner. Full loading requires real manager/atlas/renderer and
native scratch-preimage services. The explicit metadata route leaves pictures
unresolved; the canonical selection service rejects that state. An engaged
native null remains valid.5966F0's unit UV arguments remain unchanged.
GameMissionHost can bind the real services before loading and reports picture
completion separately; its default metadata path does not claim texture work.
Its sprite COM textures cannot stand in for these native texture owners.

GUI frame/Listbox input can use the actual raw backend publication and the
existing finite device dispatcher. No raw device is cast to the typed input
interface and no second input owner or sample cache is introduced. AA3910's
complete normal pointer caller preserves repeated mouse queries, latch writes,
x87/SSE arithmetic and cursor reloads. Review corrected widescreen clamp
argument roles: D5BEC0 is upper, read first; D5BEBC is lower, read second.
AA2F10/AA8BD0 hit testing remains a required concrete provider.

The integrator repaired A9BEC0's post-free gap,594BF0's25-byte epilogue,
5C8AE0's637-byte continuation and both5C9530 free gaps. Stored bodies now reach
the actual returns. Prior function names/signatures/comments/labels were
archived and checked before annotation. Objective reanalysis split one
generated16-byte local into8-byte and pointer locals; it did not remove user
comments or labels. Old compiler-category names on mission record routines
are superseded by provisional record-operation names, with prior values saved.

Validation and the exact built/promoted commit are recorded in
reports/orch5_menu_input_batch.json. Release Win32 /W4 /WX builds and both
existing CTests pass. Mechanical checking covers402 direct native call rows;
50 indirect/IAT rows retain separate native table/argument evidence. Focused
fixtures compile only their own translation units against primary libraries:
checkpoint prompt selection, constructor-backed raw input and disabled pointer
latching, picture ownership, and sole objective-list nodes. Existing GUI frame,
Listbox and installed mission-table checks are separate regression evidence.
No permanent test cases were added; two existing metadata test calls were
renamed for the explicit reader API.

Enabled cursor movement/hit testing, full objective GUI execution, full menu
registration and gameplay remain unvalidated. The worker's isolated executable
attempt crashed before mission loading and provides no picture runtime proof.
This batch is reconstruction progress, not a native ABI or playable-game claim.

Follow-up work:

- Connect complete582F30 registration and actual pending Text clone/resources.
- Close AA2F10/AA8BD0 hit testing and native input class-vector publication.
- Supply remaining session/page/profile/prompt providers and actual texture startup.
- Route the executable through the canonical menu owners and validate the full
  rendered menu, mission startup and gameplay.

Exact built/promoted code commit: `e242b220c8e6f1f795c20364cb09d3f41777159d`. Both CTests
and all seven scoped fixtures passed against those production libraries.
Initial and promoted evidence remain separately pinned in the batch report.

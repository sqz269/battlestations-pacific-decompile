# Natural owned-window message admission diagnostic

The single explicitly released stock-profile case acquired a genuine queued message for its owned window and deliberately stopped before ordinal 5030: the first `PeekMessageA` result was `0x031F`, while the reviewed observer admitted only `WM_PAINT` (`0x000F`). The installed Windows SDK 10.0.26100.0 `um/winuser.h:2512` defines `0x031F` as `WM_DWMNCRENDERINGCHANGED`. This symbol identification does not establish its producer or broaden the admission rule.

The coherent provider/build snapshot is `656ba8326c08ec9f7c74db0acf525884d823b81f`. The ignored probe uses the genuine `GameWindowHost`, window configuration and `game_window_procedure`; the platform state is published before reentrant creation and retained through destruction. It verifies the current per-window procedure, class default procedure, 24 extra bytes, state pointer and owner process/thread. `ShowWindow` is followed by the existing host-added `UpdateWindow`, without native UpdateWindow credit. The schedule is exactly one non-alertable zero-handle 5000 ms `MsgWaitForMultipleObjectsEx` and one invocation of the unchanged `00BECCD0` drain pump. There are no forced or posted messages, timers, invalidation, fabricated/default MSGs, SDK initialization or original-game execution.

## Observed stop and teardown

The wait returned `0x00000000`; this alone does not prove a returned MSG. The subsequent actual Peek output was logged at address `0x001AE7F4`: HWND `0x093E13DE`, message `0x031F`, wParam `1`, lParam `0`, time `293260468`, point `(3952,941)`, owning child PID `43832` and thread `52128`. Admission rejected this message and threw at frozen `probe.cpp:181`, before ordinal verification/delegation inside the observer. This is an explicit `std::runtime_error`, not a failed CHECK or a stock DLL fault.

The direct stop log records acquired MSGs `1`, ordinal 5030 attempts `0`, normal returns `0` and focus updates `0`. The final case marker is absent; controller marker-derived counts remain null, and `fixture_protocol_completed`, `qualified_message_normal_return` and `case_success` are false. This is an admission refusal, not the zero-message inconclusive outcome. The unchanged pump did not complete its normal drain/focus path.

The catch retained the failure while executing the reviewed teardown: `DestroyWindow` returned, the HWND was dead, the class was unregistered, and the active projection was cleared. Current state, procedure, log and profile receivers remained live during destruction callbacks. Expected profile file handles were rechecked and closed before the exception was rethrown. No post-destruction SDK pump ran.

## Profile and process receipts

The explicit stock file is `C:/Windows/SysWOW64/xlive.dll`, SHA-256 `79da26ab6b2dc25936c3354087de0ba41da1a8b62924972e9100c29b95d34385`; the ordered local dependency is `local/output/cc10_local_live_redist_readiness/payload/msidcrl40.dll`, SHA-256 `623cb6ca98e566357abbd0e76b15713921e1d7e1144c0c4f589a0407c7eff1ee`. Neither basename was preexisting. Six expected/reported/mapped file records and six closes are logged. The stock module reported `C:\Windows\System32\xlive.dll`; its opened reported path and mapped NT backing resolved to the pinned SysWOW64 file identity (volume `0xE8DCD6E0`, file ID `0x0009000000155F68`). This is guarded current file/mapping correlation, not historical physical-image proof.

The legacy module base was `0x27500000`, ordinal 43 address `0x275684F0`; stock base was `0x55D90000`, ordinal 5030 address `0x55E44E6C`. Both ordinal allocation origins were verified. Resolving ordinal 5030 does not credit a call. The XLive object remained process-owned through exit; no SDK unload or shutdown proof follows.

Parent PID `38448` and child PID `43832` both exited `2`; controller exited `1`. The retained kernel path/creation identities and exact CIM parent/command/creation admitted the suspended child before the single ACK. CIM image path was unavailable; its creation time differed from the kernel FILETIME by five 100 ns units. Both retained handles were signaled, final exact scoped discovery completed, and remaining recorded processes were zero. A later independent read-only CIM query found no recorded parent, child or child of that parent. No timeout, termination action, cleanup, capture or hash-audit error occurred.

## Frozen evidence and limits

Preparation archive `local/output/cc10_natural_message_window_preparation/preparation_evidence.zip` is unchanged: SHA-256 `5ee32949bfeba7cdf101f9687d57eee445a26d620c46eb2164c794c5d3bd7621`, 1147 rows / 1148 members. All 665 launch pins were rehashed before and after the case. Source SHA-256 is `368fa509f0339c4de7537482c80f2f58ac31987d2f0b9da80ec30a8bfe5e9bd8`; executable is `9dfae63e5a0cf028ca8ec0bedcf78e28569c00ba70b8da601b81becec0fe0a16`; controller is `5a049501fb4783393719250a6dfdbfe5c0b8574f2b1200f1acbd4407d9766d88`; launch manifest is `5bbb7d554651de3e4638af79d66d0bd51cdda562dc1120a7fb03e58ea6419c75`.

Separate runtime archive `local/output/cc10_natural_message_window_preparation/runtime_evidence.zip` is SHA-256 `ace4b982b964e659a32badb5c38d2ed2231ad6b661bd0f81e41a992b7fec973e`, 24 rows / 25 members. It preserves release, direct actual exec result, process/ACK/closure receipts, logs, independent process observation and postflight hashes. The direct exec return parses identically to `run.json`; no tool session was created. There was one invocation and no retry or input revision.

This result supports genuine owned-window creation, natural queued MSG acquisition, guarded profile identity and completed window/process teardown. It gives no ordinal 5030 delegation, stock guard branch, normalized or numeric BOOL return, normal pump/focus completion, SDK initialization/shutdown, native CRT, ABI parity or full-game credit. No native BSP ledger/function/byte credit is added. A future message-domain audit or broader admission proposal needs separate review; this case remains immutable.

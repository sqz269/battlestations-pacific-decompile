# Stream and copied Text ownership integration

This batch connects AA9520's Model copy call to the actual flags3E stream-copy
sequence and adds retained copied Text factory adoption. Work is isolated in
`agent/orch5-20260911`; worker addresses and files use separate leases.

The actual index constructor/factory now shares production mapping and terminal
ownership. B729A0/B72A70 create new streams, map source and destination, copy the
native byte count, and unlock in order. Mesh/Model flags26 still share streams;
flags3E creates distinct streams. The widget adapter returns the same acquired
Model for AA9520's final4C publication and138-bit change.

Copied Text owns one stable factory operation, source borrow, destination layout,
borrowed constructor implementation, copied lifetime and derived continuation.
Suspension keeps those identities intact. Completion transfers that implementation
into its canonical widget owner and clears operation borrows. A throwing suffix
cannot be replayed as a fresh constructor or pending child.

The native raw1F4h Text allocation remains opaque transport. AB8BB8 passes the
actual Text identity to B18A40, which retains raw+04. A semantic widget counter or
an unconstructed pool slot cannot provide that identity. Full copied Text and game
execution therefore remain unvalidated.

## Analysis corrections

The official locked flow repair restored returning-free fall-through at B22DBC
and B4B131. Both exports were refreshed and Ghidra saved; B4B197..B4B19F remains
unreachable alignment. B4B380 is the canonical index-pool allocation forwarder;
its previous static-destructor classification was incorrect. Existing comments
and annotation preimages are preserved. Names remain reconstruction hypotheses.

## Validation

Worker validation and exact integrated validation are separate in
`reports/orch5_stream_text_copy_batch.json`. The final code pin, production
library hashes and replayed fixture evidence will be recorded after integration.
The fixtures use a real D3D9 HAL device with explicit renderer, immutable-profile,
declaration and pool-header inputs. These inputs do not establish native global
producers, original binary ABI compatibility, rendered Text or gameplay.

## Follow-up packets

- Establish the real retained Text identity and its raw header/count producer
  used by AB8BB8/B18A40, preserving the actual allocation, reference counter and
  destructor domain. Coordinate AA9520/ABB2C0 ownership before extending them.
- Complete concrete cursor renderer38/5C, material, declaration and layout
  bindings in the executable; then exercise a positive copied Text construction,
  suspension/resume, admission and retirement through that same identity.
- Recover AB8530's callee-local shadow mesh/section/material creator exception
  state. Current factory retention covers its own base and cursor acquisitions;
  it does not reconstruct this callee's SEH/unwind.
- Integrate the established menu/widget path into executable startup and validate
  visible behavior. The reconstruction goal remains active.

# Raw distortion storage lifetime

The actual26Ch distortion owner can now retire genuinely constructed raw
camera34 and scene3C through the published raw providers. The new overloads
share the existing holder/post/frame release, record and base engines; the
legacy interface remains available. This does not activate B107F0 or migrate
the2664B initializer.

| Entry | Complete native range | Bytes |
| --- | --- | --- |
| B4EE70 clear | [B4EE70,B4EF8E) | 286 |
| B4F150 destructor | [B4F150,B4F1C0) | 112 |
| B4F540 scalar delete | [B4F540,B4F55E) | 30 |

All428 normal bytes and32 compiler-boundary bytes were compared with live
Ghidra and the installed PE. CBFC50[8] and CBFC58[14] are the base/record
funclets; CBFC66[10] is a missing live handler function, ending at CBFC6F
inclusive (CBFC6B JMP length5). Root owns subsequent definition/annotation.
The frozen read-only readiness also records caller/initializer EH gaps; those
addresses are outside this implementation packet.

`NativeDistortionStorageLifetimeContext` borrows the existing effects/frame
providers plus raw tree retirement and actual profile tables. Effects and
tree contexts must name the SAME actual-owner registry and decrement cell.
Fresh persistent acquisitions remain disjoint from payloads. They record
active call sites, captured numeric identities, consumed cleanup states and
the nested camera unlink operation; they provide no ownership credit.

Clear10/14 loads a fresh decrement import on each reached nonnull field.
Capture18 precedes a single retained import used for18/1C/20/38 and, after
camera unlink,3C/240. Every later member is read after the preceding callback;
each nonnull slot clears only after terminal/unlink returns. Camera34 calls
raw B6DFA0 with its required genuine current18/current0 dispatch. Scene3C uses
the already-bound NativeGuiSceneReference over the SAME actual+4. Zero
terminals require the observed current count still be zero and check
current0 then fresh4; there is no extra decrement or post-terminal payload
read. Required tree bindings must admit genuinely constructed raw cameras and
their actual pool/canonical lifetime; legacy camera/scene reinterpretation is
not an alternative provider.

Destruction stamps D61F1C, arms state1 for resource clearing, consumes state0
before resize254->0/free current backing, then consumes state-1 before
B0F5E0. An escaping member callback leaves its raw prefix intact and invokes
the record/base C++ cleanup projection without retrying members. Cleanup
states are consumed before calls; cleanup failure terminates. Native FH3/SEH
and native exception identity remain evidence-only. Scalar deletion reads
the current flags LOW BYTE only after the full destructor; bit0 controls the
genuine CRT free and EAX retains the numeric original identity.

Raw destruction first prepares the actual12B record-header C++ lifetime with
byte-preserving memcpy out/back, after pure admission and before native
stores. Aggregate/trivially-copyable/size assertions prevent implicit member
initialization. No count, pointer or capacity value changes in that host
lifetime step. Actual extent/alignment and readable initialized preimages are
required; diagnostics and bindings outlive callbacks and explicit disposition.

B4F0C0 writes neither34 nor38 nor3C. B107F0 allocates26Ch through ordinary
operator new and has no intervening stores there. B4F560's capability-false
branches return before their first publication, and the caller can immediately
release the owner. Valid cleanup preimages or full successful child
construction are explicit admission requirements; no zero fill, allocator
substitution or arbitrary original heap-safety claim was added.

Strict MSVC Win32 build and all three existing CTests passed. Compiled inspection
confirms the18 capture before retained import, camera unlink before current3C,
clears after calls, and low-byte flags load after the complete base destructor.
The one ignored fixture compares all428 copied native bytes against source in
two normal cases. Both construct a full raw B71A80 camera using the actual
application renderer, real CRT math and45Ch production pool; a genuine B724E0
24h scene uses the same actual registry, and B1FBB0 constructs the real frame.
Scene weak operations use a fully bound **isolated genuine** weak pool/list/
singleton domain, explicitly drained after scene retirement. This is not
application-owned weak-domain composition.

Baseline and callback variation compare complete26Ch output (only stale freed
backing identity normalized), reference retirement, physical camera-slot
return and event trace. The variation changes the current decrement provider
after genuine frame decrement, publishes scene3C late, and changes flags1->0.
The trace is retained-A(frame), current-B(camera), current-B(viewport),
retained-A(scene), proving the captured epoch survives the nested current
calls. No new tracked tests or broad suite was added.

The native fixture changes seven rel32 calls and two IAT address operands;
indirect instructions are unchanged. Original frame/scene numeric profile
words use explicitly relocated callback tables for genuine deleting providers.
The scene bridge restores D62D48 before canonical admission; source retains
the original numeric profile. These declared harness adaptations do not prove
arbitrary profile dispatch or native binary ABI. The inherited camera provider
also temporarily relocates its actual renderer table to the same genuine
B1FF60 provider and restores it before the application resumes.

Attempts1/2 both passed the428B comparison but failed later application drain:
the fixture had registered a weak mutex into the app manager without its
deletion binding. Changing frames1->2 did not solve that provider error.
Attempt3 used the fully bound isolated weak domain, passed both comparisons
and ordinary drain, exited0 with final COM device/api counts0/0. All logs and
the earlier fixture/binary remain archived. The existing application diagnostic
run uses real fullscreen HAL (120 concrete/46 unimplemented host methods), not
complete startup/gameplay; B107F0/B13010 remain unactivated. Native EH,
arbitrary invalid preimages, nonnull holder/post20 branches, and full distortion
initialization are outside the new dynamic comparison.

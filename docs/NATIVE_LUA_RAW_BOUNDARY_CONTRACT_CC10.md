# Native Lua raw-call admission contract

The three existing bootstrap/file/service headers now state the recovered error-selection and current C++ composition limit. This is fourteen added comment lines; non-comment tokens, signatures, providers, call/cleanup schedules and ABI are unchanged. No protection conversion, error carrier, guard, rollback or automatic close is added.

`B6A020` opens libraries and executes fundamentals with raw Lua calls. The PC/X360/region chunks use protected calls with ignored statuses. The fresh state has no inherited outer Lua handler for its raw bootstrap calls. `B66CA0` ignores parser status, releases its stream before raw execution and frees its byte buffer only after normal return. `B69D40` loads the initial chunk before arming the override-vector cleanup. `B69E00` uses the existing borrowed owner/frame/argument/path schedule.

Original `A69330` selects the current Lua error handler; without one it invokes panic and exits if panic returns. `B669C0` returns0 after top-string conversion. This native raw fatal/current-handler selection remains intact. Script-level protection can affect the current handler when DoFile executes.

The current linked-library source is compiled as C. Its raw protection restores `errorJmp` after normal return or Lua's own nonlocal transfer; pcall restores its additional fields on that path. The protected named/string/iterator adapters cover their own operations. They do not surround raw bootstrap/file execution or transport a foreign C++ service exception.

`NativeLuaServiceBindings::do_file` and `append_overrides` directly invoke genuine C++ services. Their active-binding check, allocation and current VFS/provider interfaces can throw; no callback catch/status carrier exists. If a C++ exception escapes through an active Lua C protection frame, normal restoration can be skipped and `errorJmp` can refer to ended stack storage. An interrupted state is not admitted for reuse, replay, further Lua API calls or automatic close merely because a C++ caller caught that exception. Retained backing is diagnostic; ended or cleaned backing cannot be dereferenced. The two direct fundamentals getter calls occur outside the bootstrap raw Lua call and are a distinct C++ failure path.

This contract makes no categorical claim that Win32 longjmp performs or bypasses a particular destructor, `__finally`, Activation restoration or private native FH3 cleanup. Original handler evidence and the current C++ source are separate. Genuine live state/tracking storage, same actual pooled strings, current VFS/fundamentals domains and stable active service backing remain required. C30570 source-owner/reserve/child obligations and application/MSG/source0/W readiness remain external.

Validation: one strict MSVC Win32 build and all three existing CTests passed. Token comparison confirms every non-comment token in all three headers is unchanged. There is no new test, Lua/runtime fixture, producer execution or application activation.

Read-only audit: `local/cc10_texture_source_lua_boundary_readiness/PROPOSAL.md`. Qualified archive `revision02/evidence.zip`, SHA256 `cc385191e99259de78a0d95f894d5caa1f5ca9b2695a7d27d5baaca28ff9278e`, nests the immutable initial132-row archive. Eleven original PE windows match: seven complete bodies1,765B plus four handler/FunctionInfo windows92B. There are twelve actual stdout disassembly pages,593 instructions. Revision02 corrects the collector's mistaken24-page count, which included empty stderr siblings.

The audit's three existing Lua C AR members/seven COFF functions and `_longjmp`/`__setjmp3` relocations are **existing-object inspection**, not new-link, original-ABI or runtime proof. The implementation evidence under `local/cc10_lua_boundary_contract_implementation/` records the actual diff, immutable before-build inputs/current Git blobs, token check and build receipts. Historical09ed fixture pins remain historical and unchanged.

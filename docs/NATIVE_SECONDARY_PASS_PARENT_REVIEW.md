# Actual secondary-pass source review

Addresses: 00B45E00, 00B45EE0 (caller00B463B6).

The unfinished worker source has no identified normal-schedule defect in the
reviewed B45E00 overload or narrow caller adaptation. Both production translation
units compile with strict Win32 flags. The corrected retained fixture passes
using explicit new objects ahead of the published root library. This packet
does not establish CMake registration or full canonical effect admission.

The224-byte native B45E00 receives effect in ECX and returns with plain RET.
The new C++ interface borrows actual construction/copy/registration/cache/profile
services and a persistent caller-owned child. It adopts the child before pool
allocation, uses the actual B44B10 constructor, reloads C8, publishes100, binds
metadata and calls B455C0. Four setters and the final root08 store each reload
current100. Other slots are zeroed; previous secondary owners are not released.
Generated COFF preserves those reads/publication and all seven native callee
references. The B463B6 caller forwards the same services and retains failures.

The prepared worker fixture could not compile: it referenced the old error
texture variable, attempted assignment of a service containing a reference,
and retained an unused internal helper under W4/WX. Only the copied review
fixture was corrected. It also lacked the original224-byte body and20-byte
D61BE8 profile; both were captured live and matched the installed executable.
The same executable failed fast without pass.bin and passed after only that
verified profile was supplied. Original failure logs and all corrections remain.

The source runtime uses real HAL texture acquisition, actual primary/secondary
constructors, canonical pass/state metadata, state copying and four overrides.
Distinct constructor/copy failures retain the appropriate prior effects.
The untouched original body runs only its empty branch and agrees across all
376 effect-storage bytes. Effect storage remains an explicit caller preimage;
full B45EE0/B46950, active original children, native FH3/SEH, B2 drawing, teardown
and gameplay remain unexecuted. No worker production file or Ghidra annotation
was modified by this review.

# Native SBH free source (CG)

This packet reconstructs the complete 788-byte `C11D68` body against the accepted `NativeCrtSbhState` reference view and the complete actual `BF87E0` memmove source provider. It owns no heap, descriptor array, feature word, bootstrap, lock or exception frame. The original two-argument cdecl entry has no meaningful EAX return contract. The source exposes a void function with the two original pointers, the existing state view and a separate reference to actual canonical `0109EEA4`.

Frozen BW discovery `b7fc7f55138b4f39c2ee6b7c94d56602f8dbd1f1`, report SHA256 `87b36100df5798838a0f8b7a58479ab7d22fbb30f032043b851df864a66a1e5e`, supplies all 259 native instructions and four call sites. All 69 original local artifacts are retained unchanged with the original document/report. Fresh supported BSP queries verify `C:/Users/sqz269/bsp.gpr`, `/battlestationspacific.exe`; the complete 788-byte live span equals the installed PE SHA256 `b682a82c52f81f957b2c70222077305a933f72481686c88843077f714b956dd6`.

Source base is accepted BZ `614376653822c988d7fb0f923fd142f8f2620fff`. Its shared state header and existing initializer remain unchanged. E310 stays a volatile uint32 reference containing raw pointer bits. The feature reference is separate; no incompatible pointer reference or new field is inserted into the view.

## Complete behavior and source domain

The caller supplies the actual bootstrapped native heap/state, external lock-4 contract, coherent descriptor/group metadata, boundary tags and rings, and valid mapped writable extents. The raw access helpers retain native DWORD/byte widths, read-modify-write operations and intervening reloads. Pointer/size arithmetic wraps at 32 bits. Size classes use SAR by four, decrement, then an **unsigned** clamp to 63; a negative candidate therefore clamps to 63. This differs from the allocation body's signed clamp.

The body reads metadata, region and header before the already-free bit return. It captures the successor header before the predecessor footer. It coalesces and unlinks the successor first, then the predecessor. An unchanged predecessor class keeps its existing ring position. Singleton removal clears the group mask before decrementing the class byte and conditionally clearing the descriptor mask. Unlinking reloads both neighbors between the two stores. Insertion preserves previous/next/sentinel publication, the intervening reload, the byte-count update and descriptor-before-group mask order.

Header and footer stores precede the allocation-count decrement. Only zero enters cache eviction. The old cache descriptor and group are read in native order; the actual VirtualFree provider is captured once and used for both decommit and whole-region release. Decommit uses size 8000h/type 4000h and its BOOL result is ignored. The body then rereads current cache cells, sets the available-group bit, clears only the selected high group mask, decrements the class-63 byte, rereads current metadata and conditionally clears the descriptor's high bit zero. It preserves the conditional descriptor reread before the all-groups-available check.

Whole-region release uses size zero/type 8000h through the captured provider. The following real HeapFree uses current cached metadata and current canonical heap. Both BOOL results are ignored. Current count, cached descriptor and array base produce the wrapped compaction size. The actual complete memmove provider copies the overlapping descriptor tail from `D+14h` to `D`; it receives the separate actual feature-word reference. After normal return, the current descriptor count is decremented, the original descriptor argument is adjusted only when unsigned-greater than the current cached descriptor, and the rover receives the current array base. Final publication stores the adjusted descriptor in E310 before the captured group in ED78.

No validation, overflow repair, host free/memmove, second free, callback, rollback or synthetic cleanup is supplied. No local lock or SEH frame is inserted. `BF9DC8`'s owning lock/SEH domain remains separate. API or copy faults may occur after prior native writes; this source does not claim original asynchronous-fault, DF recovery, frame or instruction identity. Original argument-slot scratch writes are replaced by source locals and are not claimed as ABI compatible.

DF=0 is a caller precondition, including the compaction provider's scalar forward REP paths. The feature reference must bind the actual canonical cell, with CPU/OS SSE2 support for any selected vector path. The complete copy provider retains its native direction/count/feature gates and signed-pointer alignment behavior. Extents must satisfy that provider's valid nonwrapping contract and be disjoint from active source/provider frames and live argument/return slots. No CPU API projection or private feature state is accepted as an original owner.

## Dependency and validation gate

The complete CF `native_crt_memmove.hpp` provider was independently and primarily accepted at `f4177d8500e2ab0ab9b01722a699ec9957610d38`, then merged in dependency commit `377d1a309362ac719f86df582b5183cd1aea14fc` before the dependent build. The source calls that actual provider, which retains the accepted CC vector dispatcher and engine. All 138 frozen CF artifacts and the primary acceptance record are retained alongside the free discovery. No placeholder declaration or link stub was used.

The initial prebuild byte gate detected Git's CRLF-to-LF conversion in the two memmove files. LF-normalized bytes were identical. The checkout bytes were retained and the exact accepted compiler-input bytes restored, leaving an empty normalized Git diff. The build gate then verified all four CF/CC source and header hashes against the accepted report. `dependency_line_endings.json` records both byte representations; `accepted_cf_dependency.json` records the actual source and acceptance bindings.

The strict MSVC Win32 build passed with unchanged compiler inputs; all eight seeds match the installed PE and both existing CTests (`reconstructed_math`, `native_math_differential`) passed. The current free body is 1,513 bytes / 537 instructions. Full current compiler command/read/write logs, source/header hashes, all five relevant objects, unique current archive membership and equal extracted members are retained.

The first successful build exposed a material emitted-code defect: MSVC replaced the apparent constant `VirtualFree` capture with two independent IAT calls at offsets 045Ah and 054Dh. Its complete compiler/object/archive evidence remains in `attempt01_rejected_emission`. The source now uses a small inline-assembly capture barrier for the actual import pointer. Build 02 passed and emits one IAT load at 043Dh, after the cached-group read at 043Bh and before the cached-region read at 0452h. Calls at 0468h and 0558h use the same captured `[EBP+8]` value, without an intervening write to that slot. Real HeapFree is called at 0579h. The four-argument CF call at 05A7h has its actual REL32 relocation at 05A8h and caller cleanup of 10h bytes. The current 988-byte memmove, 227-byte vector dispatcher and 135-byte engine exactly equal the accepted CF/CC code artifacts.

Full emitted review confirms native record widths, neighbor reloads, four unsigned SAR-class clamps, mask/counter/tag ordering and current cache publications. The volatile descriptor-count decrement lowers to load/DEC-register/store; it retains current access order under the external lock and stable canonical-reference domain, without claiming the original single DEC-memory instruction. The compiler also moves local descriptor-adjustment arithmetic after rover publication; the determining current-cache read/compare and final adjusted-cache publication retain their observable order. Compiler scratch/flag/instruction identity remains outside the source contract.

Existing math tests do not validate SBH free behavior. No new tests, free/copy execution or game execution were performed. The accompanying report binds the final focused emitted review and the two complete local evidence inventories.

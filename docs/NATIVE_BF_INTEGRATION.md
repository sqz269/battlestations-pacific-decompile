# Validated camera and model-base integration

The camera now forwards the actual name header through the raw node/string-pool providers and uses that same persistent domain for cleanup. The model-base D62D78 provider supplies construction, three-current-token testing, actual pool allocation/return, factory cleanup and canonical reference retirement. Original callback context is restored before node destruction.

Exact combined source `f773d275c813c0ad8d625225eb746255f18c77ab` passed strict MSVC Win32 compilation, both existing CTests and eight original seed comparisons. Both external fixtures compiled only their own probe against the three current libraries. All2442 tracked build inputs and the four accepted source/header pins remained unchanged. The worker archives and final validation archive have separate content manifests and hashes in `reports/native_bf_integration_validation.json`.

Camera verification compared13 original/source snapshots (14508 camera bytes per path) and nine events at CW027F. Its actual raw-getter allocation failure leaves base/tail ended without name or camera-pool return. Model verification executes original scalar/destructor/BD30E0 bodies with explicit shared-provider bridges and checks actual pool returns, canonical lookup, current tokens and retirement reentry. Factory construction and retained-binding logic_error remain source-only checks.

Six new model-base bodies account for214bytes; three camera extensions preserve830existingbytes with zero new-body credit. Nine reviewed names, original calling conventions and full bodies were saved in Ghidra and their exports refreshed. The formerly misclassified B74EC0 allocator thunk is now named for its verified behavior. Separate analysis defines the resource forwarder, factory exception handler and model type body.

Actual startup/asset selection, full cockpit construction, original binary ABI/FH3 behavior and gameplay remain unvalidated. No game installation files were changed.

# Warning owner construction and complete initialization

Addresses: 0098A020,009870A0,00973F20,00987080,00987090 and13 direct helpers in
`reports/warning_owner.json`. The report records complete listing endpoints,
full-span live/disk byte hashes, original ABIs and reconstruction classifications.
Descriptive names are hypotheses.

`construct_warning_owner_0098a020` is the actual1B0h owner constructor. It creates
the roster,19 NativeStrings, pending/applied queues, message and escape tables,
eight separate opaque native tree headers/sentinels, two null effect slots and
an empty prompt callback. Critical-section creation00BD1860 is last. Untouched
allocator/scalar words are explicit allocation inputs. The stale array-helper
inventory tag contradicts this full body; its existing descriptive name remains.

`WarningManagerState` and `WarningMessageTables` are the canonical reused C++
projections. No second pending queue or warning message map is introduced. The
unconsumed trees at D4,F8,144,150,15C,168,184,1A4 retain real native headers and
allocated sentinels. Host `sizeof(WarningOwner)` differs from native1B0h; startup
allocates the host size, constructs, publishes game+21E0 and then initializes.

The full009870A0 body publishes00F8A0C4 and resets the four warning deadline/
active fields before constructing its temporary Lua owner. It opens mask1,
runs `Scripts/datatables/Warnings.lua`, captures Warnings and releases globals.
It rebuilds the roster from the current world, clears suppression, and loads
entity escape, player-unit escape and message tables through the three existing
canonical table routines. Assigning the selected Lua reference releases its old
reference and copies the newly selected one before releasing the temporary.

Warnings, escapecharacters and selected messages remain retained through effect
acquisition, hook publication and the final eight float resets. The earlier
`load_warning_tables_009870a0` fragment remains a table-loading fragment; its
shorter reference lifetimes do not implement this full initialization sequence.

The two acquired effects are `EnvironmentUSHorn` and `EnvironmentAirRaid`.
When replacing a pointer, the owner publishes and retains the new effect before
releasing the old one. After any old-effect callback, the temporary output slot
is reloaded for cleanup. Same-pointer assignment does not add an owner retain.
Real Interlocked operations precede current virtual0 zero-reference dispatch.
The accumulator resets, the current hook owner at00F8BBCC receives00987080 at230,
00F8BF4C receives00987090, and all eight clocks reset before Lua references close.

00973F20 clears the roster and traverses the actual nested checked source trees
at `[00E188A8]+6B4`. The outer sentinel flag is at29D; the inner tree embedded at
outer node128 uses sentinel flag15. Each destination item copies the DWORD at
inner node+C. The world pointer is reloaded at every outer comparison. Source
iterator increments00581290/0057F510 are required adapters over the real native
owner/node pair, not invented iterator order. Destination list allocation/count
and source STL algorithms remain library boundaries.0096F8C0 has a returning
count-growth path and a length-error arm; its old whole-function throw label is
incorrect.

The two recovered hook thunks reload00F8A0C4 and forward the original ECX or
ECX/EDX arguments to00985C50/00985250. C++ dispatcher bindings are explicit;
these interfaces are not binary-compatible x86 thunks. Effect acquisition and
those hook targets still require real implementations.

The primary adopted saved worker source after its usage-limit failure and
finished evidence and integration. The combined Win32 build and both existing
checks passed. The focused fixture executes original valid-path iterator bytes
on actual native node storage, tests world replacement, Lua weak-reference
survival, reentrant effect-slot changes, same-pointer retention, hook-owner
reload and final resets. The primary defined missing00987080 through RET0098708C
and00987090 through RET0098709D from matching live/disk bytes. Logs and exact
library hash are recorded in the report.

Host cleanup rejects populated opaque trees and is not native0098A2A0. Native
SEH unwind, native container layout for projected state, complete runtime
destruction, binary replacement and gameplay remain unvalidated.

Follow-up packets: recover0098A2A0 full runtime destruction and the two hook
targets00985C50/00985250 if unleased. Continue to reuse canonical warning tables
and queues when their runtime consumers are connected.

# Main-menu frame and mission storage integration

Addresses: 00A9D030,00A9DA60,00A9C400,00696470,00AA0F50,00AA0F70,
00AA4F80,00AA7EF0,004F8F20,00598B60,0051DDA0,0051CC90,00519DC0,
0058F5B0,005C5B40,005C5DA0,005C6A70,005C27E0.

Mission readers and menu consumers now use one set of side fields. The obsolete
executable copy pass is removed and four Island Capture Lua keys are corrected.
The real Lua adapter reads143 installed campaign records and34 multiplayer
records, including102 populated Island Capture lists. This is table execution,
not validation of the full executable, which failed before tree loading.

Listbox current40 now runs through the actual widget frame runtime with one
owner borrow covering its gates, base update, navigation, callbacks and highlight
tail. Directional84 and step88 use the existing selection/paging lists. Its
manager70 gate reads the actual manager field produced by AA4F80. Input696470
preserves46/47/4A call order; actual game4D92B0 and typed device current2C remain
required providers. Native selector0 returns null; constructor120=1 uses the
established highlight path. No successful fallback is supplied for the invalid
zero branch or missing providers.

The same listener114 registry routes04/08/0C. Main-menu screen+8 binds04 to the
existing required598B60 provider,08 to5966F0 and0C to the proven RET0C body at
4F8F20. Widget screen+40 current04 remains5993A0. Callback registration rejects
owner mismatch and active unbinding. Native subobject/stack evidence is kept in
reports/orch5_listbox_listener_slots.json; these are new C++ interfaces.

Briefing51DDA0 loads the actual page map from duplicate-preserving5C5B40 keys.
58F5B0 binds authored Group pairs, without clones, to the menu's sole2CC/2DC
vectors. Its factory aliases the same selection count references and layout
slots;519DC0 resolves their actual canonical widget owners. Independent review
confirmed field identity, callback reloads and native string sequencing.

The manager frame preserves raw70 publication and normal-clear order, actual
page-pointer snapshot semantics and callback-sensitive highlight resets. Review
corrected a query that previously allocated a timed owner for an empty list.
See docs/GUI_MANAGER_FRAME.md for the remaining pointer and Screen providers.

Validation and immutable promoted source commit are recorded in
reports/orch5_menu_frame_storage_batch.json. Combined MSVC Win32 Release and
both existing tests pass; ignored focused fixtures link the same production
libraries. Mechanical verification checks265 numeric call rows, with symbolic
virtual rows supported separately by table evidence.18 annotations are saved
with prior names/comments preserved.4F8F20 is defined from three verified bytes;
the reachable51DFAF three-byte free fall-through gap is repaired. Alignment gaps
are not invented functions. No permanent tests were added.

Follow-up packets:

- Supply actual typed input current2C and full game4D92B0/Listbox registration.
- Reconstruct full598B60 activation and594BF0 Text/Listbox objective rows.
- Bind AA3910 and validate actual Screen page traversal/snapshot lifetimes.
- Connect mission picture texture ownership and remaining menu providers.
- Validate the full menu in the rebuilt executable, then rendering/gameplay.

Build, fixture and installed-table evidence do not establish original ABI,
complete menu execution, rendering or game equivalence.

Exact built/promoted code commit: `2c0cba7f7b0a52a07788bfba03d895f7fde37caa`. Both existing
tests and all five scoped fixtures passed against that production library.
Initial and promoted logs/hashes are retained separately in the batch report.

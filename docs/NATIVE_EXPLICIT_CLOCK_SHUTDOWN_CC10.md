# Explicit particle-clock unregister and deletion, CC10

`destroy_published_native_particle_clock_00736930` reconstructs the complete 164-byte original explicit F8D420 shutdown schedule through genuine existing providers. It remains separate from `00BD0400` manager drain. The application/startup caller is not wired and no new runtime or test case is added.

The caller borrows SAME actual AA0 manager publication, retained `NativeParticleClockShutdownContext` and current CE7D38/CE3818 table views. Null F8D420 returns before manager or profile access. Otherwise capture the first manager's actual `+10h` section, construct its actual 8-byte CE37FC guard, enter and increment current physical `+18h` with one memory ADD, then arm source cleanup. After the publication recheck, the second manager lookup precedes capture of the current unregister owner. Existing BCFCA0 clears only the first matching vector slot, preserving length, capacity and later duplicates.

Reload F8D420 after unregister; read the CURRENT owner profile and supplied CURRENT slot-zero word at that boundary. Admit complete CE7D38/004DE340 and base CE3818/00412440 only, passing flags1 to existing scalar providers. Unsupported profiles or targets throw after completed unregister mutation. Clear the same publication only after normal completion, including its redundant clear after a scalar returns. If publication became null before the post-lock recheck, skip that store. Release the captured first section after a single physical decrement ADD, regardless of current manager publication.

Source failure after arming calls existing 411EE0 with the actual guard and rethrows. It adds no clock free, normal-path clear, re-registration, rollback, replay or second count operation. Existing nested clock destruction may already clear publication and stamp its base profile during its own source cleanup; the outer provider does not repair those effects. A dispatch rejection can leave an unregistered live owner requiring retained backing/context obligations. Duplicate registration is not normalized. Actual objects, pool/records, canonical companions, IAT cells and profiles remain live through their genuine terminal paths and failure obligations.

Original `00736930` is cdecl, no inputs, RET0. This explicit source API adds four cdecl inputs, 16 caller-cleaned bytes; its normal epilogue retains RET0. The 24-byte shutdown context and 180-byte deletion binding remain unchanged. Numeric native profile words identify existing reconstructed scalar providers; original code addresses are not called. Win32 lock imports use genuine current host imports. Source ABI, compiler FH3 frames and CRT/exception behavior are not drop-in native ABI/private FH3/SEH or hardware-fault equivalence.

Native evidence includes all 44 original instructions, first-match BCFCA0, actual 411EE0 guard, both scalar providers, profile slots, original 8-byte C86000 unwind/10-byte C86008 handler and DB54B0/DB54B8 map/FuncInfo. The sole immediate caller is 737F30 at 738319. Its broader shutdown graph remains unbound in `GameStartupHost`; this packet does not activate that ordinal or full application shutdown. Nonempty producer/admission work, source0/W, platform/MSG and arbitrary native virtual targets remain separate.

One fresh strict MSVC Win32 build and all three existing CTests passed. Fifteen source checks and 52 emitted checks with nine call/import relocation checks cover null/guard arming, repeated current reads, both scalar paths, normal clear, captured-lock release and catch/rethrow. Seven exact core members supply 16 complete COFF function sections and nine unique full relocation-masked linked matches. The new 366-byte normal/catch section is absent from the application link; its 29-byte source handler has ambiguous matches, so both remain COFF-only evidence. Its rooted 88-byte source catch/unwind/try/FuncInfo metadata identifies the inline catch at offset 102h; this is compiler-source evidence, not original FH3 parity.

All 18 prior clock-TU function sections and relocations remain unchanged. Five borrowed provider members are whole-file identical. Header-triggered recompilation of `native_singleton_destruction.obj` changed only its four-byte COFF timestamp; all other bytes and its 12 function sections/masks/relocations are identical. The failed initial whole-file assumption and corrected collector are preserved. The built shutdown object equals its exact frozen library member. The resulting application PE retains its embedded `asInvoker` manifest, and existing Release math checks remain active independently of `NDEBUG`.

Evidence is under `local/explicit_clock_shutdown_provider_cc10`; approved readiness, native bytes and recovered existing fixture source remain immutable under `local/application_clock_explicit_shutdown_readiness_cc10`. Source/compiler/library/object/link inputs and outputs were pinned around build; prior current outputs were verified before authorized replacement, and all old frozen evidence remains unchanged. No behavior of the new explicit entry, source failure path, base-profile path or application has been executed. A later fresh explicit-unregister fixture requires separate review and release; prior empty-clock BD0400 fixture evidence does not cover this entry.

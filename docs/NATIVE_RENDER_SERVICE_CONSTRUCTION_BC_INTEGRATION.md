# BC raw render-service construction providers

Addresses: 00b6fbf0, 00b6fc10, 00b6fe10, 00b6fe20, 0041e870, 00b52550, 00b20090

Four packets cover seven complete native entries and 926 bytes: three new
bodies and four existing raw-context extensions. Prior ledger identities and
evidence remain. Analysis-only 005CC590 attribution and service EH repairs,
and separate BD analysis, add no BC reconstruction credit.

Exact combined source `d6cd5f085cc8d521c31e5bf327daa10b47710e9a` passed the strict Win32 build,
both CTests, three current-library-only probes and the depth getter
complete-library-member byte check. All seven original signatures, calling
conventions and full stored ranges were saved/read back and exports refreshed;
112 numeric call rows pass.

- camera: Six original/source pairs, 6744 compared bytes, all 84 original bytes unchanged. Source EDX argument address and plain RET differ from the native stacked word and RET 4; private native stack aliasing is not certified.
- string: Eight pairs, 11396 normalized state bytes and six event DWORDs; 83-byte original parent with two external-call operand relocations. Actual raw pool getter returning/throwing mutations, source/header aliases and backward overlap are bounded parent composition evidence.
- texture: One full 752-byte original/source pair and 204-byte owner/preimage equality; four real hot alias-cache hits per path, saved unsigned dimensions 0x8000000F/7 versus COM 8x4, temporary spacing, shared canonical counts and real COM teardown. 16 external E8 operands, four publication operands and three profile slots adapt current providers.
- depth: No runtime fixture. Complete seven-byte symbol in the exact combined library member equals 8B818C190000C3; actual field 198C producer/current profile/consumer evidence supports the depth surface owner hypothesis.

Full evidence: `reports/native_render_service_construction_bc_validation.json`.

- New source interfaces and their borrowed actual-storage/provider domains are not whole-game binary ABI replacements.
- Original-byte parents share reconstructed current descendants; original allocator/pool/CRT/provider instruction equivalence is not independently certified.
- Texture fixture is hot-cache only: no cold resolver/VFS/D3DX load, overlap/failure/FH3/SEH execution, arbitrary current profile or gameplay proof.
- Retained cache/VFS acquisitions must survive unresolved failures; no invented rollback, owner, allocator, resolver or identity matrix is supplied.
- Full B14A10 service and B3C800 cockpit construction, native EH identity, unrestricted concurrency and gameplay remain open.
- 005CC590 attribution and service EH boundary repairs are analysis-only with zero reconstruction credit. Separate BD analysis already in the tested commit is preserved and excluded from BC credit.

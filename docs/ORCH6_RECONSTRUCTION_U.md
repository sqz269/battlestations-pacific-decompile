# Orchestrator 6 reconstruction batch U

Addresses: 00926C80, 00926D90, 00925A00, 00926390, 009263C0, 00924B10, 009267F0.

U reconstructs pending destroy/kill production, cancellation and scene lifecycle notifications over borrowed actual owners. Complete byte comparisons establish reuse of two existing list providers. Names remain hypotheses and all source APIs remain distinct from the native ABI.

| Change | Evidence |
| --- | --- |
| [Destroy/kill producers](NATIVE_PENDING_ENTITY_PRODUCERS.md) preserve exact flags, causes, recursion and publication order | 7 native/source pairs; 5,852 input and 5,856 output bytes per side, zero differences; real canonical providers create two malloc-backed nodes |
| [Cancellation](NATIVE_PENDING_ENTITY_CANCEL.md) retains parent gates, captured lock and removal/store order | 8 native/source cases, 294 verified bytes and 13 relocations; actual remove provider remains required |
| [Lifecycle notifications](NATIVE_SCENE_LIFECYCLE_NOTIFY.md) use existing actual observer dispatch | 6 native/source pairs cover callback-mutated state and captured listener order; 119 owned routine bytes |
| [Library reuse](PENDING_ENTITY_LIBRARY_REUSE.md) supplies node allocation/count growth | 12 live/disk spans, 520 bytes; complete body and EH graph identity, with no new STL body |

The initial cancellation provider types imposed `noexcept` as a fixture restriction. Native925A06 calls the allocating getter before section capture/entry. U removes that restriction; a focused source-only check confirms `bad_alloc` propagation without touching flags/list bytes or invoking later providers. No automatic post-entry lock cleanup or rollback was introduced. The worker's native FH3 attempt for producer exceptions exitedE06D7363; the failed attempt is preserved and native exception transport remains unproved.

MSVC Win32 Release and both existing CTests passed at `3e28789bad8e40e16b6d68c21f317dc79f0a4be8`. Executable SHA256: `d6ec8e5dec50ccca3554fae41b47876048f9a99bf8cfb065e54e155929680752`. The 120-frame USN01 compatibility run retained 18,557 finite trajectory rows, 241 unchanged Airfield2 samples, 2,400 avoidance queries, 1,080 cruise reads, 10,080 generic ticks and 420 valid world nodes. All 77 unit prefixes were torn down while the observer owner was live, before manager drain.

Both pending queues stayed empty through 240 passes. The mission run therefore checks existing application behavior, while the new nonempty queue/lifecycle behavior is established only by the scoped fixtures. Actual unit field routing, copy/remove providers, renderer/base callbacks, original exceptions/concurrency and full visual/gameplay parity remain incomplete.

The archive retains 345 verified worker artifacts, 36 root static-comparison artifacts and 39 root exception-probe artifacts. Three missing FH3 selectors were formally defined from verified bytes and saved. Reviewed annotations and affected exports were refreshed; V owns the still-missing scene getter006D1E80 definition.

## Follow-up packets

V connects actual unit byte views, recovers scene-handle producers/getter/clear behavior, and reconstructs the native pending drain with raw scratch ownership and explicit required copy-provider contracts.

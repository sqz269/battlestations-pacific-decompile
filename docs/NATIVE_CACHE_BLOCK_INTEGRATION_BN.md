# Native package, cache and reader foundations BN

Sixteen complete ordinary bodies, totaling 1154 native bytes, now have registered actual-storage Win32 C++ source. They cover package-name construction and VFS block counters; resource-cache lookup, rotations, predecessor and node allocation; and reader/reference-slot initialization, assignment and release. The [integration report](../reports/native_cache_block_integration_bn.json) pins worker commits, original ABIs, artifacts and combined source `91db57c19f3134c5a3b0819ccb14744607dfdc77`.

Package naming preserves first case-sensitive `.mpak` matching, the length-four/no-match case, identity clearing and suffix cleanup. Cache routines preserve live header/link reads, captured pivot returns, returning CRT handler effects and output aliases. Nodes own copied keys but borrow raw resource pointers without AddRef. On construction failure, the allocation catch frees the captured outer node; the native placement-delete action is a no-op and adds no partial-key cleanup.

Reader assignment publishes and retains the new stream before releasing the captured old stream. Release leaves consult the current table/slot only when the interlocked decrement reaches zero, then clear the captured slot after normal callback return. Callback replacement is overwritten by that native clear; an escaping callback skips it. These source leaves reuse the existing explicit terminal dispatcher and do not implement the missing game-resource destructor.

All five translation units compiled in the first Win32 Release build at `9673354d`. After reviewing and merging incoming main `9af75431027fdaea91a5091d6cbdf487a8e784a6`, the combined build passed both existing CTests again. Primary reviewed the complete new source/listings and verified all five workers' source/dependency and retained-fixture hashes. Existing focused fixtures cover actual storage, alias order and selected failure paths; their dependency libraries remain pinned to the earlier BL134578 build. No permanent tests were added.

The whole-report verifier passes 36 direct/tail rows, including supporting callees and EH transfers. The reader packet has seven indirect calls, separately established by full 157-byte/70-instruction live/PE evidence, import mapping and a callable-adapter fixture; a zero-direct-row result is not presented as indirect-call verification. The package report previously used `target` instead of `native`, silently skipping its seven calls; the primary corrected the schema and retained the preimage.

Ghidra now owns all 21 bytes of `Catch_All@00b7f712`, including the verified returning-free continuation and rethrow. Its seven instructions, compiler name and prior evidence were preserved. The two 10-byte FH3 handlers were defined. Sixteen source names/ABI views plus catch/unwind/handler evidence were saved, prior comments retained, and exports refreshed. All sixteen source flow reports show no gaps. Analysis signatures remain distinct from native binary/SEH compatibility.

Incoming main's model-numbering source passed 104 call-row checks during this integration. Its native-loop fixture and the incoming torpedo-range USN02 result remain their original owners' separate evidence. This primary ran no new mission for BN. The full production resource loader still needs cache insertion, package callbacks, reader buffer/owner/root dispatch, actual manager and resource lifetimes, and the current metric contract. Those remaining dependencies are explicit in the packet ledger; the broader game reconstruction is not complete.

The final main merge also includes the separate mount-frame diagnostic rule and its corrected asin saturation contract. Its CMake conflict was resolved with the reviewed registry-union helper: all incoming and BN source registrations remain present once, and the order-dependent native-data-placement include remains last. The final configure/build and both existing tests pass.

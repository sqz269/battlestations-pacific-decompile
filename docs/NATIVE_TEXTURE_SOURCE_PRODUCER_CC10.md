# Native texture-source producer C30570

`populate_native_texture_source_00c30570` supplies the complete recovered normal producer schedule over an already-live, genuine C30470 `NativeTextureSourcePayload`. It uses existing raw Lua, string-pool, renderer texture-cache and typed child-append providers. This packet adds a source interface and static-library member; it activates no application caller and has no producer runtime evidence.

The original function is C30570..C30893, 804 bytes and 209 instructions. It receives the complete owner in ECX and one stacked actual eight-byte name, returns with RET4, preserves EBP/EBX/ESI/EDI, and uses an eight-aligned 568h local frame and private FH3 cleanup map. The new C++ interface is cdecl over a live payload, requested name, borrowed contexts and a persistent acquired frame. It is not a binary ABI replacement. The original 53-byte C307AD..C307E1 append fragment was credited previously; native ledger/accounting is reserved to primary integration rather than repeated here.

The initialized byte at actual owner+1C is read and, on body entry, set to one before any callback. Bootstrap uses B66BD0/B6A020 mask1, the genuine `shaderfx/animtextures.lua` literal (25 characters plus NUL), and B69D40 flag0. The same caller-live eight-byte scratch is reused for script and child-name phases. Script data is captured before disarming its normal cleanup state, then returned through the current genuine raw pool getter and BD1510 with freshly loaded length+1. No semantic noexcept release substitutes for that raw path.

Raw globals/Animated/requested/FPS/Textures operations preserve tracked-cell and release behavior. FPS uses B66270 with no positivity, finite, nonzero or type-normalization gate. The compiler emits B66270 CALL, only integer MOV/ADD, FSTP32, then MOVSS bit copies into payload rate. No FP arithmetic or external call intervenes before the float32 spill. This lowering preserves the returned float bits but differs from the native direct owner FSTP.

VALUE end predicates and integer KEY predicates remain the existing raw operations. The borrowed VALUE string must be nonnull, NUL-terminated and live through unchecked scanning and copying. There is no empty substitute or null guard. The child scratch is zeroed length then data, resized through raw 41DD40, and freshly loaded data/length+1 is copied with `std::memmove`; native BF7680 admits overlap. The immutable proposal's memcpy was corrected before the implementation build, with a separate exact delta and independent review receipt.

One stable child/cache diagnostic frame is allocated before capturing the current renderer. The implementation loads the captured renderer's current profile and, for the supported D5F0A8 profile, its genuine +64 table entry. It admits only the observed B319B0 target and passes the captured receiver, same scratch, flag0 and retained genuine cache context to that provider. It does not hardcode a provider while bypassing current dispatch capture. Unsupported profiles/targets are explicit source-interface errors, not reconstructed native alternatives.

After the child provider returns, its result is recorded without extra owner credit. Child-name data is captured before state9, returned through the same raw pool domain, then the existing typed append leaf consumes the exact result. Null results are appended unchanged. The leaf retains native wrapped signed growth, fresh count/data reads after reserve, reached scalar-pointer lifetime/store, and unconditional fresh count increment. A returned result may remain unappended if name return or append fails; the retained diagnostics record that obligation and do not automatically release or roll it back. Null-child retirement safety is not established.

Normal iteration uses B67190 and fresh VALUE predicates. Normal completion clears selected actual+8, releases VALUE, KEY, Textures, selected and Animated in that order, then closes the Lua state. The native cleanup-state observations are exactly 0,1,0,2,4,5,6,5,7,8,9,10,9,8,7,5,4,0,-1. State3 exists in the original cleanup map but has no observed normal store. These recorded states do not execute the private native map.

The acquired frame is one-shot, nonmovable and retained before entry. Its 4C8 owner backing and seven distinct 14h Lua-object cells are uninitialized storage; lifetimes start only through the genuine reached constructors/getters and their returned pointers. Owner backing is aligned to `NativeLuaStateStorage` (Win32 alignment4). This source type alignment does not claim the original eight-aligned native stack frame. Scratch, requested name, payload/header/backing, borrowed contexts, genuine literals/table and raw Lua tracking addresses must survive callbacks. Lua tracking capacity/refcount bounds remain caller obligations. Reentrant calls require distinct frames and retain the same live payload; initialized gating is unchanged.

A caught C++ exception records metadata and rethrows. It performs no Lua API, cleanup, classification, rollback, failed-return conversion, close/discard/replay or resource-credit transfer. In-progress acquired destruction terminates rather than inventing recovery. Raw fatal/nonlocal Lua behavior may not reach that catch. Unknown or foreign Lua C escapes remain unadmitted; retaining backing does not restore abandoned C frames. The source requires already-active retained Lua services and a genuine shared `ActualNativeStringPoolStorage`/`NativeStringRawPoolContext` domain. Internal existing noexcept-release boundaries are not private FH3 parity.

The first strict build rejected the proposal's `alignas(8)` acquired layout with C4324/C2220 under /W4 /WX. Its exact inputs, log, actual tool receipt and failed artifacts are frozen in `failed/attempt01.zip` (SHA256 165887b1d9f6c6c3fb276be7a51511f44fe9e4bc6c5946bf63e0e445c1366ba9). The approved alignment-only correction uses the actual type alignment and changes no native-domain behavior. The separately sealed second strict Win32 Release build passed, followed by all three existing CTests. No warning suppression, test addition, runtime case or original-game launch occurred.

Complete emitted evidence covers every file-backed raw section and relocation in the new producer and twelve old/new provider objects. All twelve existing providers have identical complete file-backed raw sections and relocations. The new producer has 107 unique code sections totaling 5607 bytes, including host diagnostics/templates. Its normal entry symbol slice is 1604 bytes; the complete same section is 1675 bytes and includes catch/error continuations. A separate 60-byte section contains metadata allocation unwind and generated EH handling. Those two relevant sections total 1735 unique bytes, with full linear decoding and CFG/relocation coverage; overlapping symbols are not counted twice.

The original `coff_sections.py` collector sliced `blob[rp:rp+size]` even when `PointerToRawData` (`rp`) was zero. Its original `compiled_sections.json` therefore contains synthetic COFF-header slices for 13 old and 14 new `.bss` records. These are declared uninitialized storage with zero physical raw bytes; those JSON slices are not initialized-data evidence. The separate corrected physical-section review at `local/cc10_producer_physical_section_review/physical_sections.json`, frozen in that folder's `evidence.zip` (SHA256 617976eea25ea7ca661adb73425c98c43c734d91194943d3c93ace7746517818), proves the actual file-backed sections and twelve existing-provider comparisons remain unchanged. The 107 code sections/5607 bytes, exact AR-member and relocation conclusions are unaffected. The original producer archive (SHA256 8616f71c4baa5eb4cc29408e0f2c5785e4aed6b9d81beffb15508d482e44a6d7), source and build remain immutable; this qualification does not rewrite that historical collector output.

All eleven native conditional branches are mapped, including the three delegated to the unchanged 67-byte append leaf. Compiled normal raw calls, current field/capture events, nineteen state stores and final release order are checked in `compiled_schedule.json`. Source-only phase/target guards and metadata allocation branches are explicitly additional source interfaces. Complete bytes/relocations are in `compiled_sections.json`; the full section and EH boundaries are also preserved in `compiled_schedule.json`, and the complete entry/catch listing is `producer_complete_listing.txt`.

The exact new full core library, thirteen selected exact AR members, full old core library and twelve old members are preserved. The application PE, map and actual link receipts are pinned. Its map does not select the new producer member, so there is no linked producer body, linker-selected new caller or execution proof. Source/Git, native 804-byte window, toolchain/SDK, successful and failed build receipts, immutable design, separate original producer audit and conditional Lua independent review are nested in the implementation archive.

Evidence is under `local/cc10_texture_source_full_producer_implementation/`; the final archive is `local/cc10_texture_source_producer_evidence.zip`. Baseline is 656ba8326c08ec9f7c74db0acf525884d823b81f, with historical pre-sync selected providers preserved separately. This packet does not establish original private FH3/SEH, foreign Lua error transport, nonempty clock/sampler execution, platform/MSG prerequisites, source0/W, startup/full renderer activation, original game behavior or universal cleanup.

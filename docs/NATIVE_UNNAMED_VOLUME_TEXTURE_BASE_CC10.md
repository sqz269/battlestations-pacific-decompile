# Unnamed volume-texture base B340A0

`construct_native_unnamed_volume_texture_base_00b340a0` reconstructs the complete original B340A0..B340E6 leaf: 71 bytes, 18 instructions, no calls or branches. Original ABI is ECX owner, borrowed COM and flags on stack, EAX the same owner, RET8. The explicit-serial C++ interface is a new MSVC Win32 cdecl interface, not a binary replacement.

The source preserves CEB130 then D5F1F4 profile writes; starts one `std::atomic<int32_t>(1)` at the native B340B5 +4 store; zeros +8/+C/+14; stores borrowed COM +10 and flags +1C; stores the old SAME shared serial0108D6E8 at +20; rereads and increments the current serial with DWORD wrap; installs D5F2C0 and returns the owner. +18 and all bytes beyond the 24h prefix remain untouched. COM receives no retain or release. Size, alignment and always-lock-free assertions bind the count to the inspected installed target. B33FC0 already has explicit count placement and is unchanged.

Admission requires the actual writable 24h prefix and valid reached scalar fields, aligned four-byte +4 storage, and fresh or fully retired exclusive backing. The same live process serial must not overlap owner storage or be concurrently mutated; no private/default counter is admitted. Prior count operations, canonical borrowers, old resource use and failed-constructor diagnostics must have ended before reconstructing a reused slot. Atomic placement establishes only this count, not an entire owner, string/cache header, payload or COM lifetime. It must not reset a live count.

This base ends at D5F2C0. Original B3D720 later installs D618B0 and performs current COM calls; that constructor and B2A5A0 runtime-volume factory remain unreconstructed here. The leaf has no fallible service call under its storage preconditions and adds no rollback, retry, cleanup or failure admission for those later operations. Existing canonical companions require completed construction and borrow +4; terminal volume deletion can return the pool slot before canonical unbind/Entry erase. Pool availability or its lock alone is not proof of safe replacement. Complete-owner, pool-reuse and application/dispatch admission remain external.

One strict Win32 build on `ff47303b226d1a94085a35cae959894377eea2e2` plus this leaf passed `/W4 /WX /O2 /fp:strict /std:c++17` and all three existing CTests. No new test or runtime ran. The exact source matches approved revision02. Complete new COFF inspection covers nine unique code sections, 290 bytes, including the 177-byte/62-instruction entry and emitted header/local helpers; all physical/code relocations are zero. The entry has no call, branch, catch or private EH metadata. Fifteen emitted byte/order checks pass. Exact raw objects match their two selected AR members. The unchanged B33FC0 section and relocations match the preserved historical descriptor-build member; this comparison is qualified by its historical provenance, not treated as a current-baseline rebuild. The new leaf is unselected in the actual application map, so no linked-entry or execution claim follows.

Evidence is frozen under `local/cc10_unnamed_volume_texture_base_implementation/`: `compiled_inspection.json` contains all physical sections, raw hex, symbols and relocations; `compiled_schedule.json` records full code and fifteen order checks; `native/00b340a0.native.*` contains the complete original bytes/decode; `before_build_inputs.json` seals eight source/config and eleven tool/header pins before build; selected command/read tlogs and 53 postbuild dependency pins are separately qualified. `build_receipt.json`, raw build logs and `LastTest.log` retain the successful build/tests. `correct_old_member_identity.json` corrects an initial collector label without altering the preserved object; a renamed inspection script avoids Python's stdlib `inspect` shadowing. Both preparation outcomes remain preserved and add no behavioral credit.

The prior descriptor build's four outputs and member were preserved before sync/rebuild; all 54 prior ZIP paths remain unchanged. Original 142-row readiness ZIP `5b443095793a8b8af6eb3cd6e624dd66be35998e88162ae2e94b55e8836b799d`, revision02 11-row ZIP `a596fb16171a418c7400daf509b8050e51749769878677116b5223b96b8905af`, and installed-MSVC traits ZIP `e8dfb486aeae5ff6867e12bf75e2a66e6805f366a472c77db54a38806e45fa46` are nested unchanged. The traits finding applies only to MSVC14.51.36231 Win32 C++17 with `_HAS_CXX20=0`; it does not classify all atomic implementations. Final `index.json` and detached `freeze_receipt.json` identify the implementation archive and exact committed Git blobs. No Ghidra mutation, native FH3 identity, fixture, COM behavior, concurrency or game validation is claimed.

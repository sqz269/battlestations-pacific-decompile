# Application secondary-pass providers

The renderer's existing `GameNativeMaterialCompilerOwners` view now supplies `NativeMaterialPassCopyAccess`, secondary-pass registration, and `NativeMaterialProgramNumericRendererDomain` for the already reconstructed B45E00/B455C0 route. `CompilerOwnersGraph` retains the copy/numeric contexts, borrowing its same pass lifetime, VFS strings, actual owner registry,0108FBF8 pool and F8D394 renderer cell. The numeric domain borrows the application's actual `texture_loading.cache` and D5F0A8 table. Borrowing the ready-phase view performs no native creation, texture lookup, count adjustment, registration or loading.

The existing constructor-state callback recipe is factored into `bind_state`. It selects only D61A2C/D61A34/D61A3C and constructs the existing state reference/retirement metadata in the existing canonical registry. Constructor registration still visits render18, third1C, sampler20 in the same order. B455C0's existing copy callback now uses that same recipe after fresh-state publication and native count-value copy, before row-copy allocation can throw. B45E00's pass registration uses the existing `bind_pass`/D61BE8 companion after publishing secondary100 and before copy. No original copy/constructor/terminal body is edited or credited again.

Existing state production placement-news actual14h storage before count1. Copied counts use a load from the current source atomic and store into the independently live destination atomic; they do not copy an atomic lifetime. Count2 stays2. Positive residual native credits may keep a copied state and its companion alive after a pass release; this composition adds no balancing reference, normalization, eager disposal or hidden cleanup.

Compiled inspection found the existing state initializer writes atomic default0 on fresh exclusive storage before its CEB130/count1 stores. That source initialization predates this change. All three copy/state/pass core members are byte-identical to the previously frozen registry build, including98 function sections/relocations. This packet adds no native count store and claims no exact native intermediate-store equivalence for that initializer.

The callback remains noexcept under the existing companion policy: metadata allocation/registration failure or unsupported profile terminates. A later row-copy failure retains the already published/bound state. The numeric secondary-pass caller must retain its exact one-shot frame and actual backing on constructor/copy failure. There is no automatic rollback, pool-slot return, retry or replay. Actual state/pass terminals validate their numeric profiles, run genuine scalar deletion, then retire metadata via canonical unbind, host companion accounting and metadata deletion. The retained raw pointer is identity only after native storage ends.

Texture loading is declared/constructed before compiler owners and destroyed afterward. The pass lifetime and canonical registry also outlive compiler contexts. Host normal/exceptional shutdown drains native owners before renderer and VFS destruction; the existing after-drain live-companion guard remains unchanged. Callers must keep their views, operation frames and application contexts alive through actual retirement and exclude drain/destruction while a source failure retains work. This borrowed view does not track caller frames.

The Win32 source aggregate grows from28 to44 bytes: three reference fields12 plus compiler registration16, then copy reference4, secondary registration8 and numeric reference4. Production CMake callers are compiled together against the new header. Earlier ignored fixture binaries were not rebuilt or reused. The new view/metadata ABI is not an original binary interface. The readiness's first mistaken16-to32 size comment is preserved and explicitly corrected; the reviewed111-line diff itself is unchanged.

Native evidence reuses complete live/disk-matched B455C0..B45895[726] and B45E00..B45EDF[224], plus the four state/pass8-byte profile pairs. B455C0 original ABI is ECX destination88h and stacked source88h, RET4; B45E00 uses ECX effect178h and RET. Descriptive names and new C++ contexts are source hypotheses/interfaces. The composition fragment atB45E00 credits zero new native bytes.

One strict MSVC Win32 build at `b23f7b122`, using the180-byte singleton deletion-binding header, passed all three existing CTests. Twenty-three bounded source/build/config pins and cl/link hashes were frozen before building; eighteen actual output pins include the separately frozen `bsp_game` renderer object, libraries, executable, test log and compile/link tracking logs. The application object is not a `bsp_core` member. No new test or runtime was added.

Validation includes14 source checks,31 emitted byte/order checks and11 symbol-relocation checks. Thirty-two focused complete COFF sections from the application object and three exact core-library members decode completely;22 have unique complete relocation-masked linked matches. The inline application constructor, new state callback, constructor-state registration, retirement and live-companion checks have linked evidence. The44-byte borrowed getter, out-of-line graph constructor and pass-binding callback have full emitted sections but no unique linked match; they receive no execution/linking claim. Actual compile/link logs confirm the application object, new header/include dependencies and strict flags. These are bounded build/static observations, not application or native EH execution.

The application still does not construct full `NativeMaterialEffectProgramsContext`, NativeChildren, actual compiler-tail/CompileContext or mutually borrowing effect Loading/Cache contexts. Their sampler/F8D420/platform services and caller-owned source0/B-16/W evidence remain separate. Shader source_mode1 does not bypass the two B3B280 descriptor-sampler calls. No successful null/default provider or alternate XLive load is supplied. Full material loading, startup activation, newly bound failure/teardown runtime, original ABI/FH3/SEH and game behavior remain untested.

The approved read-only proposal and all prior registry/compiler evidence are preserved. Current packet evidence is `local/application_secondary_pass_providers_cc10`; its report is `reports/native_application_secondary_pass_providers_cc10.json`.

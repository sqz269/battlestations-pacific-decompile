# Orchestrator 6 reconstruction batch W

Addresses: 00779AF0, 00928C80, 00824F39, 00803BA0, 004BCA80, 0054D510, 00CA70D0, 00C9184D.

W reconstructs the complete normal killed-unit caller and base handler, a bounded wreck-handler fragment, and two lifecycle services. The source interfaces borrow actual owners and retain explicit required provider contracts.

| Change | Evidence and limits |
| --- | --- |
| [Killed-unit caller](NATIVE_UNIT_KILLED_TAIL.md) | Complete 109-byte sequence; three original-byte/source cases preserve listener capture, signed live recon records and base delegation. |
| [Base killed handler](NATIVE_UNIT_KILLED_BASE.md) | Complete 709-byte normal body; two original-byte cases use actual Lua objects, pooled strings, pose and captured sections. One source-only exception case checks cleanup. |
| [Wreck effect handles](NATIVE_UNIT_WRECK_TAIL.md) | Only 172 bytes at 00824F39..00824FE4, with explicit continuation 00824FE5. Eight original-byte/source pairs pass; the rest of the handler remains incomplete. |
| [Lifecycle services](NATIVE_UNIT_LIFECYCLE_SERVICES.md) | Complete recon dirty marking and listener publication, six original-byte/source scenarios; renderer/node behavior remains an explicit boundary. |
| [Reference slot qualification](VOLATILE_REFERENCE_ADOPTION.md) | One canonical implementation accepts genuinely volatile pointer cells. W provides build/CTest coverage; X owns native reuse and actual effect-reference fixture evidence. |

The complete base handler evaluates trace arguments but its 004254B0 target is a literal RET, so that native call emits no log. The distinct 00923050 base call is also inert. This corrects the older semantic logging interpretation. The recon helper still checks the world when its slot is null; listener publication precedes renderer resolution, and callback changes are preserved.

MSVC Win32 Release and both existing CTests passed at `9c53c9ce4b71b9ccb4a8f7ae3eeb6a9b019458fc`. Executable SHA256: `92a082d2ab8f600385cda686c0d2e605a74ebaabbcbd91f97426cc1c41af7265`. The 120-frame USN01 compatibility run produced 18,557 finite trajectory rows, 241 unchanged Airfield2 samples, 2,400 avoidance queries, 1,080 cruise reads, 10,080 generic ticks and 420 valid world nodes. All 77 unit observer prefixes were torn down before manager drain while the owner remained live.

The pending queues remained empty through 240 passes. This mission run checks existing application behavior. New killed/wreck execution is supported by the focused fixtures, whose remaining getter, Lua wrapper, renderer and release boundaries are explicit. Full wreck coverage, application routing, original exceptions/concurrency and visual/gameplay parity remain incomplete.

The archive retains 688 worker artifacts, 207 service-fixture artifacts and 12 metadata artifacts. Both previously missing EH selectors were defined from verified bytes and saved, with prior annotations and refreshed exports retained. No existing function was recreated. The archive audit was extended to retain compiler-discovered SDK headers and VsDevCmd; all expected hashes are now covered.

## Follow-up packets

X recovers renderer listener updates using the existing scene-root provider, effect-removal/reference providers using exact canonical reuse, and the actual F878FC mission-entity lock getter and owner.

# Validated node name-domain composition

The node runtime can now be composed with the actual raw string-pool context without constructing a semantic pool. Its existing semantic constructor remains available. Each typed consumer checks the selected domain before allocating or changing construction phase; explicit per-owner storage routes remain available.

Combined source commit `18e234cdaa616982d5d521be9034293b19b8a7bd` passed the strict MSVC Win32 build and both existing CTests. A focused external probe compiled only its own source against the three current libraries and completed the five-argument raw node constructor/destructor lifecycle. Opposite-domain destruction left all 0x178 node bytes, pool/manager identity, gate, name header and return count unchanged. The accepted15 source/header inputs and all2430 current build inputs were checked before and after verification.

The first worker fixture used the older four-argument raw adapter. That control is preserved separately; final acceptance uses the corrected raw-context/current-constant overload. The sealed worker capture and the final current-library replay have separate hashes in the validation report.

Associated analysis repairs restore the helper and viewport free continuations, one complete unwind tail, three handler bodies, and the264-byte caller containing the attachment clear. B3C650 has a saved descriptive name, original thiscall signature, both contained callers and corrected canonical-lookup evidence. These are analysis records, with zero new C++ body credit.

See `reports/native_node_name_domain_be_validation.json` for exact source/library/evidence pins. This migration changes13 source files and21 previous member accesses; it adds no native body. Raw camera forwarding, original binary ABI and exception-runtime compatibility, and gameplay remain unvalidated.

# Post-effect constructor BR and dust initializer BS integration

The batch adds three reconstructed native bodies covering 1284 bytes: B4E470 (974 bytes), B52860 (306 bytes), and the B4CBF0 material getter (4 bytes). B4E470 constructs a 20h/32-byte post-effect object. B52860 constructs the existing 24h/36-byte child for `oldfilm_dust.mshd` and binds its texture. The primary integrated worker `30b9eec4` as `32e81952`, retaining both independent CMake additions; main remains separately owned.

MSVC Win32 Release and both existing CTests (`reconstructed_math`, `native_math_differential`) passed at exact source commit `32e81952f34ffd0afe2e98afdb146aee50cf4d9b`. All 2559 tracked build inputs were unchanged with a clean tree before/after. The BR worker's separate build ran its one configured CTest and compared 2561 inputs captured during/after compilation; that is not a prebuild stamp. The BS preliminary dirty-tree build is retained separately. No tests were added, and none of these runs executes either constructor or the initializer.

The primary independently reviewed the BR header/source, all 325 native instructions and twelve-state cleanup map. An independent Astra worker reviewed the BS header/source, all 82 initializer instructions, the getter and three-state cleanup map. No blocking source findings remain. The primary rechecked 16 live Ghidra/installed-PE spans and 66 source/provider entries. Two BR provider checkout hashes differ only through CRLF/LF; their committed blobs and normalized bytes match, and exact root bytes remain build-pinned. Call auditing passed 80 direct/tail rows; 14 indirect rows retain separately reviewed slot/profile and provider evidence.

The compiled BR constructor section is 5453 bytes, SHA256 `6138764264b0af28ab6cc85e77dcff5e7623ca848435dc5adb353e55a7dfd881`, identical to the worker's reviewed section. Its seven initial writes preserve the sole count1 initialization and +08/+18 preimages; all seven x87 operations, section-store order, model-name capture before mask clear, stream decrement epoch and final shared section/mesh epoch were inspected. The primary reviewed the parser's argument mapping and admitted branches. The whole object SHA256 is `f7acd976a6b0731ad66b9c0c15b2f8006c4dba3e1aa57e62313b5af658cbdc6b`.

The BS getter is exactly `8B 41 14 C3`, with no relocation. The 712-byte initializer section and 27 symbolic relocations match its independently reviewed preliminary build. Generated code preserves initial writes, child publication before normal name return, texture capture before child/material reads, binding before current D7A24C capture, and all reset fields. A private-mask comparison moves before publication and compiler EH stores follow C++ generation; hardware-fault and private-frame timing equivalence is outside the claim.

BR uses the real raw-name and canonical-owner providers, a fixed four-vertex stream allocation, both count-dependent section branches, and the original separate stream creator decrement. All 27 observed callers pass count 3 and null optional sizing. BS uses count 2 and null optional sizing. If BO completes natively but host binding then throws, BS preserves the completed child for external disposition without inventing parent publication; it cancels only prepared, unentered child reservations. Persistent storage must remain alive through external disposition and quiescence.

All three descriptive Ghidra names and original thiscall signatures were saved and read back; prior names/prototypes/comments were preserved, and exports force-refreshed. The ten-byte CBFBAB and CC0014 handlers were separately defined, saved and exported with zero body credit. The new naked fastcall getter uses the same physical ECX input/EAX output/RET behavior as the original leaf; this does not establish broader binary ABI compatibility.

The real callable renderer+48 prerequisite, material-factory inaccessible completed identity, provider/terminal noexcept domains and external quiescence remain. Secondary cleanup failures consume remaining actions and propagate the newest C++ exception, outside native FH3 equivalence. No optional-size/nonstandard-count execution, constructor or initializer execution, injected failure, native differential fixture, drop-in ABI, SEH, GPU or game validation is claimed. B529A0 composition and worker BT geometry integration remain open.

See `NATIVE_POST_EFFECT20_CONSTRUCTION_BR.md`, `NATIVE_POST_EFFECT_DUST_BS.md` and `reports/native_post_effect_br_bs_review.json`. The sealed attempt retains exact tested inputs, primary libraries/objects, source reviews, call audits, saved annotations and exports. Worker evidence copies exclude five large worker library/executable artifacts, whose hashes were independently checked in the preserved worker worktree. Earlier BO/BQ/BP/BN and BK/BL/BM evidence retains its recorded scope.

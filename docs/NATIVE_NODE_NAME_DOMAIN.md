# Native node name-domain composition

Addresses: 00B6F440, 00B71A80, 00B7C6B0, 00B7C710, 00B724E0, 00B72430, 00B72580, 00B8F5E0, 00B75030, 00AA5840, 00AA6640, 00AB8530, 00AC59A0, 00AF90A0, 00B08F60, 00B0B6A0. These are source-composition associations, not newly reconstructed native bodies.

`NativeNodeDestructionRuntime` borrows exactly one immutable name domain for its lifetime. The original `SizedStoragePool` constructor remains semantic; the new `NativeStringRawPoolContext` constructor borrows the actual raw context object. Both validate the shared scene runtime and node predicate. Neither reads current publication values or allocates a name. The caller must keep the context and its referenced publication cells alive through all node cleanup and terminal callbacks.

`require_semantic_name_pool()` and `require_raw_name_pool()` reject the opposite mode with `std::logic_error`. Typed GUI, light, group, model, camera and particle entrypoints check semantic admission before allocating or changing construction phase. The model's explicit `actual_names` branch remains independent. The two-argument `destroy_native_node_00b6f440` checks semantic mode before entering destruction; explicit `NativeStringStorage&` and raw-context overloads retain their storage bindings.

The external fixture at `C:/Users/sqz269/bsp-be-node-name-domain/probe.cpp` constructs the runtime without any semantic pool, constructs an actual raw B6F5A0 node, checks that the typed destructor rejects without changing node bytes, then completes explicit raw B6F440 and matching provider teardown. `run.ps1` compiles only that probe against the selected repository's current three libraries with an embedded manifest and records input hashes and output. This establishes the host composition and provider lifecycle; it does not establish original FH3/SEH, concurrency, arbitrary allocator profiles or gameplay behavior. No raw GUI, model, camera, group, light or particle owner route is added here.
The tracer allocation and construction bridges each check the semantic accessor. These are separate assembly bridge calls, so they do not share a captured C++ reference; the repeated check is safe because runtime mode is immutable.
The sealed worker capture is `C:/Users/sqz269/bsp-be-node-name-domain/worker_capture.zip` (SHA256 `0b9ff5b3470a88e4e36bc2312865723b04b8082b111387fdc1a600bb6faa6c4c`). Its manifest verifies 31 members and hashes 1203 compile inputs. The preserved first control used the four-argument storage adapter; the corrected replay uses the five-argument raw-context constructor with live volatile constant cells and also checks unchanged provider state on typed rejection.

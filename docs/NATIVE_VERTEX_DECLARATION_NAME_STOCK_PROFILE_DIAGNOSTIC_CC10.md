# Explicit stock-profile declaration diagnostic

The controlled fixture established that the legitimate legacy MSIDCRL40 dependency and stock XLive can both complete their eager load calls in this isolated process. It then failed the fixture's explicit reported-path assertion, before ordinal5030, platform pumping, declaration cache lookup, canonical owner credits or final CRT retirement. This packet adds diagnostic metadata and zero native bytes; it changes no production C++ or default loader profile.

The executable uses the previously reviewed genuine B317E0 → B305F0 → B2DBD0 composition, B48AF0 producer and canonical owner retirement contract. Only the explicit stock/dependency selection, identity receipts and parent-only suspended-child ACK were added. The original bootstrap remains the sole child creator/resumer. No message was posted, SDK initialization invented, production loader changed, copied native code executed or original game launched.

## Build and source qualification

The coherent build snapshot is `8f9f0c94ac064791e209cba4d7a512ee5c591177`, with 176-byte singleton deletion bindings. Strict MSVC Win32 and all three existing CTests passed. A later four-file comparison against `b23f7b122` records 180-byte bindings; this fixture did not silently mix those layouts or claim compatibility with the later snapshot.

The current game archive contains 71 actual non-main CMake objects. The actual fixture link selected 525 exact members across four libraries: 35 game, 458 core, 28 Lua and 4 zlib. Each payload equals its current CMake object. Five bounded COFF/linked functions agree except declared relocations: B48AF0, B317E0, B305F0, B2DBD0 and genuine `register_cleanup`. The constructor has one count-one store at actual owner+4; the real atexit registration remains present. The I386 executable has an embedded manifest and active assertions. No production body was recompiled inside the fixture.

Relative to the earlier diagnostic's 523 selected members, this current link additionally selects `gameplay_settings.obj` and `unit_fire_flooding.obj`. Their narrow source pins were acquired after linking and match exact 8f Git blobs. The four singleton/host comparison pins were similarly acquired after the build. They are not represented as prebuild pins or recursive repository/header closure.

The approved receipt source SHA is `caac2984a10175bc0d04e6a4e17706b7234ab4e9d16b43a51c7736ab048c9f0f`. The unchanged 670208-byte executable SHA is `91502e19b89a6ced5930195a838367ed3662a6ba6a6d29fa340a6a3132d0c76d`. Compile attempt01 was a command-path refusal before cl; its immutable archive remains separate. Resolving the shell command path produced a successful compile/link without source/library changes.

## Two separately released attempts

Attempt01 used controller `d7e4c250...` and 130 sealed inputs. Parent86340 exited2; suspended child37700 was terminated1460 using its verified retained handle. CIM supplied exact command/parent/creation but a null ExecutablePath; the controller refused ACK before any DLL load. Both processes closed. Its observed raw tool-return fields were transcribed after an orchestration `store(undefined session_id)` failure prevented the planned immediate file save; stdout was reconstructed from the actual unchanged controller run.json. This limitation is preserved explicitly. No case or loader credit follows.

Primary review separately approved the narrow controller correction and attempt02. Controller `80bba5ba...` treats CIM image path as optional agreement while retaining mandatory kernel image/FILETIME and exact CIM command/parent/creation. Discovery first scopes the exact owned command, then verifies the opened kernel handle even if CIM path is null. Independent parent cleanup and final scoped discovery preserve unknown closure on missing/unverified identity. Attempt02's 133-input seal includes all original 130 unchanged pins, this controller, the reviewed direct raw-tool capture wrapper and the immutable first-attempt archive.

Attempt02 acknowledged actual child66028 of parent48960. Both exited `3221226505` (`C0000409`), propagated after the fixture printed `FAIL line100`. No controller error, timeout or termination occurred. Both retained handles were signaled; final discovery completed, closure_complete is true and remaining_recorded_processes is zero. The actual exec_command return was saved directly through apply_patch; no wait call was required. All 133 inputs remain unchanged.

The case reached genuine VFS construction and six canonicalizer probes: getters12, returns12, lowercase39 and two registered slots. Both DLL basenames were absent before load. The explicit legacy dependency loaded at `27500000` from `local/output/cc10_local_live_redist_readiness/payload/msidcrl40.dll`, SHA `623cb6ca98e566357abbd0e76b15713921e1d7e1144c0c4f589a0407c7eff1ee`. Ordinal43 resolved to `275684F0`, MEM_IMAGE with AllocationBase27500000 and protection20.

Stock XLive loaded at `55DA0000`. GetModuleFileNameW reported `C:/Windows/System32/xlive.dll`; hashing a read through that reported path produced the expected stock SHA `79da26ab6b2dc25936c3354087de0ba41da1a8b62924972e9100c29b95d34385`. The fixture's normalized string comparison against the explicit `C:/Windows/SysWOW64/xlive.dll` request failed at line100. This is a diagnostic assertion failure after successful load returns, not a demonstrated stock DLL crash. The reported path/hash combination does not replace the required equality check with a proven physical-file identity equivalence.

No ordinal5030 origin receipt or call count was reached; neither declaration cache nor final CRT audit passed. Zero-byte fault/host logs provide no exception or retirement proof. No third run, path allowlist, source change, executable rebuild or fallback is included.

## Immutable evidence

All archives are under ignored `local/output/cc10_stock_xlive_declaration_replay_design/` and every direct manifest row was reread and verified:

| Archive | SHA256 | Members / rows |
| --- | --- | --- |
| `preparation_evidence.zip` | `d33e4826924b60463e3f484114826a4ba2a002a81974b628bd0cf42841a0e539` | 200 / 199 |
| `runtime_attempt01_evidence.zip` | `2521d40bf8236d5ef3b9f459e2101bdb8be6aa94d79f105b42596c4354a3f3f9` | 209 / 208 |
| `preparation/attempt02/preRun_evidence.zip` | `4ff7344a1b80ff86401714460eab6daff0f4e971b63012f726b4a6545d93d67a` | 5 / 4 |
| `runtime_attempt02_evidence.zip` | `10e464c811ef01a1dea9ab90d07ce91cdc31bd102e1d51a4fecd0b93f18413c6` | 18 / 17 |

The second preRun archive contains the exact first-attempt archive; the second runtime archive contains that preRun archive. The earlier design, local redistributable readiness, AlterBSP failures and component archives remain immutable. The companion report records concrete receipts, hashes, source pin timing and zero-byte credit. Future investigation of Windows reported-path identity requires separate read-only evidence; it is not part of these runs.

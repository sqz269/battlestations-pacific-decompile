# BD raw node lifetime providers

Addresses: 00b6f5a0, 00b6f440

Two existing complete bodies cover1111 original bytes; no new body count.
The constructor preserves current constants, inline name reloads and x87 matrix
copy order. The destructor consumes cleanup states before potentially throwing
raw pool work so remaining base cleanup still runs.

Exact combined source `4548c163d4f97d227fdd7a2e411df1f9fd81ded7` passed strict Win32 compilation, both CTests,
two current-library fixtures and the actual library materializer-store inspection.
Two original signatures/CC/full ranges were saved and freshly read back; all58
recorded numeric transfers pass.

- construction: One original/source alias-current-pool pair compares778 bytes; a separate source-only real getter throw checks772 bytes. Complete814-byte original parent and103-byte x87 matrix copy; eleven declared parent operand relocations. Current constants alias actual slot+54/+F4/+13C; a real manager callback observes preimages and changes the source header. Complete final-library materializer stores only zero DWORDs at+4/+54/+58; caller restores+4 to1 before later native reads/providers.
- destruction: One current-library fixture includes a real semantic-pool typed control and raw419CC0 allocation failure under a disposable-probe-only job quota. Array cleanup is consumed, name header/return count stay unchanged, baseCEB130 is reached, count/physical-slot tail preserved and explicit matching pool/manager teardown succeeds. This is source-provider exception evidence; no original-byte exception execution.

Evidence: `reports/native_node_lifetime_bd_validation.json`.

- Two existing bodies are extended; zero new native entries are claimed.
- New explicit storage, raw pool and current-constant interfaces are not original binary entry points.
- C++ materialization transient writes, asynchronous/fault-time observation and private native stack identity are excluded; exact native store counts are not claimed.
- Constructor original-byte parent/matrix compose current reconstructed pool/CRT providers; zero-copy, other aliases, nested cleanup exceptions and unmasked floating-point behavior were not executed.
- Destructor fixture does not execute original FH3; foreign or malformed point-light backing, nested exception search, concurrency and gameplay remain unproved.
- NativeNodeDestructionRuntime still requires its existing semantic pool to be constructed. The raw overload avoids it; the typed fixture control actually uses it. Optional raw-only runtime construction and persistent camera context forwarding remain explicit follow-ups.
- Analysis-only node caller/EH ranges and cameraCC1B36 handler receive zero reconstruction credit. Full camera, cockpit helper, render-service startup and gameplay remain open.

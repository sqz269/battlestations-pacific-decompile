# Menu selection concrete providers

Addresses: 00AB2690,00AB27A0,00AB17B0,00A9C310,00A9C540,00AA0F50,00A9ABD0,00A9E230,00A9D870,00A9D9D0,00A9D560,00594B60,009052D0,0043BC30,00851E90,00851EC0,00851F50,00851FA0,00AC06C0,00AC0820,00580820,00AA7970,00ACF120,00ACEB30,00AA8240,00B6DA70.

This wave fills concrete dependencies of5966F0 using the same menu storage and GUI owners. Icon texture mutation and sizing preserve retain/release order, borrowed UV aliasing and live constant reads around texture callbacks. Listbox now maintains the native borrowed14C paging list separately from FC while retaining actual row owners and selection identity. Medal display uses game+6B4 MissionProgress, distinct from profile reset storage+650. The date builder emits native localization markup through its own129-byte allocation and actual NativeString storage.

The new canonical selection-services adapter supplies eight recovered operations: Icon mutation and sizing, relative fitting, localized date construction, highlight positioning, medal update, node visibility factor and FrameBox current58. Its real mission-tree, command-bar, texture and objective-vector producers remain required abstract accessors. It does not construct a complete menu.

Review corrected fixed Icon constants to live aliases read after release and around dimension callbacks, and restored AC0820's pre-call width float spill. A suspected pending-Text issue was retracted after confirming the existing pending exception stops the outer caller. The Icon table begins D5C4C0; its current80 entry is D5C540, not the mid-table dump address D5C530. Listbox C540's second argument is the paging low byte, not a selected flag. Selector0 returning null is retained; the later full-frame path that dereferences it remains unresolved.

Validation consists of the combined MSVC Win32 build and both existing CTests, plus four fixture-only executables linked against the combined production libraries and headers. These exercise actual D3D9/Lua inactive Icon states and callback constant mutations, canonical Listbox/Group paging and ownership, six date locales and allocation ordering, and30,720 non-Text native measurement comparisons with focused owner placement checks. Full medal widgets, active Icon rebuild resources, actual Text/FrameBox rendering, native ABI and gameplay remain unvalidated. Exact hashes and call/annotation records are in reports/orch5_menu_concrete_providers_batch.json.

Follow-up packets: bind mutable mission-tree, command-bar, picture-texture and objective-vector producers for the actual5966F0 menu; establish Listbox selector0/full current40 and directional84 contracts; implement non-null row insertion and native teardown with borrowed14C lifetime discipline; execute actual Text/FrameBox resource paths and medal widgets only after their real dependencies are ready.

{"address": "00776230", "name": "BSP_PeerManager_PumpMessageQueues", "evidence": "Hypothesis, not a recovered symbol. Packet game_session_polls, read-only analysis. __fastcall(void* peer) with ECX = game+1EF0h, ret at 0077648a. Returns at once when 00e188a8 is null. Picks the network object as peer+188h if set else peer+18Ch, then drains two locked queues on it: the 12-byte list at net+28h (head +2Ch, count +30h, CS +34h) running 00774ab0, 007752d0 and 007717a0 on each node[2]; and the list at net+18h (head +1Ch, count +20h, CS +24h) running 007702e0 when node[2]+D50h is set, then 00772ec0. The pseudocode shows a return after the first node, but the listing at 007762ff-00776309 falls into jmp 00776286, so each queue is drained fully. Between the queues, when peer+F4h is set, 01090ab0 virtual +20h gives a {counter, frequency} pair and a gap over 00d7a348 since 00e0af18 runs 00784c40 and 007850e0. Only reachable from BSP_Game_OnMove through the drain loop at 004e5462 that the decompiler removed as unreachable. See docs/GAME_SESSION_POLLS.md."}
{"address": "00778560", "name": "BSP_Multiplayer_Tick", "evidence": "Hypothesis, not a recovered symbol. Packet game_session_polls, read-only analysis. __fastcall(void* peer, float seconds) with ECX = game+1EF0h and RET 4 at 00778582. Body is two calls: 00778450(peer, seconds) at 0077856b and 0076ffc0(peer, seconds, 0) at 0077857c, with ECX restored from ESI. Sole caller BSP_Game_OnMove at 004e5036 with the raw frame delta; the call is skipped when the state is 0Dh, 00f876b0 >= 1 and the scaled delta is positive. See docs/GAME_SESSION_POLLS.md."}

{"address": "00bcf910", "name": "BSP_PointerVector_Count", "evidence": "ECX vector owner, EAX count,RET. Nullbegin+4 returns0; otherwise subtract begin fromend+8 and arithmeticshift right2. Assembly is19bytes; descriptive genericpointervector role. | Null begin returns0; raw32-bit subtraction then signed SAR2. Original ABI: ECX vector; EAX count; RET. Evidence docs/SINGLETON_LIFETIME.md; reports/singleton_lifetime_audit.json. Scope: typed_function; no broader generic-helper claim. Composed system prefix integration: docs/SYSTEM_CONSTANT_BUILDER.md; reports/system_constant_builder_audit.json. Earlier bounded-worker limitations describe the worker snapshot; current root composition uses explicit actual owners and has a new typed ABI, without gameplay validation. | Complete 19-byte original function. Capture begin+4; null skips end read and returns0; otherwise raw32 end+8 minus begin, SAR2. Full naked MSVC Win32 raw DWORD/register/stack schedule. Actual SDK memmove_s and shared source-CRT malloc/free/invalid-handler domain replace original CRT internals; no typed manager projection. Strict main Win32 build, both existing CTests and8 reference seeds passed. Actual final main archive SHA256 8e1737daf051ec94a3d69ab5d0a73aac7ae925a5223f5c6474dfb63c993f5b40; prior complete sixleaf objects occur unchanged in this final archive. Native post-free flow gaps repaired with prior CALL_RETURN values recorded, exact native bytes retained. No new routine execution, original static-CRT/SEH/catch-domain identity or game validation. Descriptive names are hypotheses. reports/native_singleton_vector_leaves_audit.json"}
{"address": "00bcfca0", "name": "BSP_SingletonLifetime_Unregister", "evidence": "Hypothesis, not a recovered symbol. void __thiscall f(manager, object): ignores a null object, then linearly scans the manager pointer vector (begin manager+4, end manager+8, count through BSP_PointerVector_Count) and writes 0 into the matching slot without erasing it, so the vector keeps its length and later walks contain holes. Every singleton destroy and unregister helper calls it with the manager from BSP_SingletonLifetime_GetManager in ECX. | Ignore null; linear scan; zero first matching slot; retain holes/length; subsequent duplicates remain. Original ABI: ECX manager; stack owner; RET4. Evidence docs/SINGLETON_LIFETIME.md; reports/singleton_lifetime_audit.json. Scope: typed_function; no broader generic-helper claim. Composed system prefix integration: docs/SYSTEM_CONSTANT_BUILDER.md; reports/system_constant_builder_audit.json. Earlier bounded-worker limitations describe the worker snapshot; current root composition uses explicit actual owners and has a new typed ABI, without gameplay validation."}
{"address": "00bcfeb0", "name": "BSP_PointerVector_AllocateSlots", "evidence": "Unsigned count*4 overflow throws bad_alloc, else explicit native/host pointer-size allocation boundary. Original ABI: ECX count; callers zero EDX; EAX raw pointer; RET. Evidence docs/SINGLETON_LIFETIME.md; reports/singleton_lifetime_audit.json. Scope: typed_allocator_helper; no broader generic-helper claim. Composed system prefix integration: docs/SYSTEM_CONSTANT_BUILDER.md; reports/system_constant_builder_audit.json. Earlier bounded-worker limitations describe the worker snapshot; current root composition uses explicit actual owners and has a new typed ABI, without gameplay validation."}

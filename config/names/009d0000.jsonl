{"address": "009d06d0", "name": "BSP_BotStateTorpedoAttackRun_Construct", "evidence": "Hypothesis, not a recovered symbol. State* __thiscall(ECX = this)(Approach*), RET 4. Vtable 00D212C4; the same +18h period, +1Ch stagger and +20h offset shape as the depth-charge 009A36A0. docs/BOT_TASK_STATES.md, reports/bot_task_states.json."}
{"address": "009d07b0", "name": "BSP_BotStateTorpedoAttackRun_Tick", "evidence": "Hypothesis, not a recovered symbol. void __thiscall(ECX = this)(float dt), RET 4. Vtable 00D212C4+0Ch. The torpedo run-in: the same re-roll as 009A3770 with variant 0 to 007F0280 and an extra bias from approach+90h/+88h, writing the heading to cmd+2C0h and back to approach+60h. It does not release. docs/BOT_TASK_STATES.md, reports/bot_task_states.json."}
{"address": "009d2570", "name": "BSP_BotStateTorpedoDone_Exit", "evidence": "Hypothesis, not a recovered symbol. void __fastcall(ECX = this)(). Vtable 00D21320+8h, the torpedo analogue of 009A48D0: 007BBBA0 at 009D258A when state+98h is at or above 00D7A218, then JMP 009BDE40. The torpedo release therefore fires on leaving done or prepare, not from attackrun. docs/BOT_TASK_STATES.md, reports/bot_task_states.json."}
{"address": "009d3050", "name": "BSP_BotTaskTorpedo_Construct", "evidence": "Hypothesis, not a recovered symbol. Kind 0Eh, size 7DCh. Base at 009D3076, approach 009D2DA0 at 009D3091, vtables 00D213C8 / 00D213C0 / 00D213BC at +530h, initial state +544h or +580h. docs/BOT_TASKS.md."}
{"address": "009d4a70", "name": "BSP_BotTaskTorpedo_UpdateCruiseProfile", "evidence": "Hypothesis, not a recovered symbol. Vtable slot +54h of the torpedo task. Writes Pilot/Torpedo/CruisingAlt (+430h, 500) to the pilot control block +394h and the descriptor-row float at *(this->+40Ch), replaced by [00CE3850] when below [00D7A370], to +398h, plus [00CE4C04] to +39Ch, then clamps the attack distance to Pilot/Torpedo/AttackDist (+434h, 2200) times the speed ratio. docs/BOT_TASKS.md."}
{"address": "009d4e30", "name": "BSP_BotTask_MakeTorpedo", "evidence": "Hypothesis, not a recovered symbol. The 0099A170 torpedo arm. __fastcall(ECX = bot, EDX = target): operator_new(7DCh) at 009D4E51 then 009D3050 at 009D4E6D. docs/BOT_TASKS.md."}
{"address": "009d4fb0", "name": "BSP_UnitInstance_RaiseTurnAssistLoad", "evidence": "Hypothesis, not a recovered symbol. Packet unit_command_producers, read-only analysis. LISTING ONLY: no Ghidra function covers this address; 009D4E30's body ends at 009D4E95 and the enclosing-candidate heuristic mis-attributes it. Body 009D4FB0-009D4FCE inclusive, void __thiscall(unit, float request), RET 4: fld [ecx+102Ch]; fld [esp+4]; fcomip st,st(1); fstp st(0); jbe 009D4FCE; movss xmm0,[esp+4]; movss [ecx+102Ch],xmm0. A maximum latch: the field is raised only when the request is strictly greater, and an unordered compare takes the jbe. No cross-references, so it is a dead out-of-line copy of an inline function; the live sites are inlined in 009F3F80 and 009DE5B0. See docs/UNIT_COMMAND_PRODUCERS.md."}
{"address": "009d4fe0", "name": "BSP_UnitInstance_RaiseSecondaryLoad", "evidence": "Hypothesis, not a recovered symbol. Packet unit_command_producers, read-only analysis. LISTING ONLY: no Ghidra function covers this address. Body 009D4FE0-009D4FFE inclusive, void __thiscall(unit, float request), RET 4, byte-for-byte the same sequence as 009D4FB0 with unit+1034h (kUnitOffTurnAssistLoad2): fld [ecx+1034h]; fld [esp+4]; fcomip st,st(1); fstp st(0); jbe past the store; movss [ecx+1034h],xmm0. A maximum latch. No cross-references; the live sites are inlined in 009F3F80 at 009F4606, 009F4912 and 009F4AB4. See docs/UNIT_COMMAND_PRODUCERS.md."}
{"address": "009d5240", "name": "BSP_ShipAi_SetDirectionLatch", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_arm_tail, read-only analysis. __thiscall(blk)(int direction, char force), RET 8, body 009D5240-009D5291, complete, read from the disk bytes: no Ghidra function starts here and no decoded call reaches it, so every use in the image is an inlined copy. 009D5244 returns at once when the argument equals blk+35Ch; 009D524F COMISS 0.0f against blk+360h with JA requires 0 > blk+360h unless the force byte is set; then 009D5269 writes blk+35Ch, 009D526F blk+374h = -1.0f (00D7A260), 009D5277 blk+384h = 0 and 009D5289 blk+360h = 1.0f (00D7A24C) for a non-zero direction or 0.0f for the stopped code. 009EEFE4, 009EF087, 009EF1B2 and 009EF1D4 inside 009ED6B0 are four inlined copies, all with force = false. docs/SHIP_AI_NAVIGATION_ARM_TAIL.md"}
{"address": "009d53b0", "name": "read_NavigatorBot_parameters_009d53b0", "evidence": "Hypothesis, not a recovered symbol. thiscall ECX=descriptor; stack LuaObject*, signed level index; RET8. concrete_normal_path_body: complete 9 scalar stores, lookup/release/scope order; six valid level indices. Complete assembly/raw bytes; docs/ROBOT_CONFIG.md; reports/robot_config.json; field callsites and offsets in reports/robot_config_evidence.json. Win32 build/CTest1/1 and ignored real-Lua fixture passed; not native-ABI/game validated. Ghidra read-only; parent owns missing starts/flow repairs and annotation."}
{"address": "009d56a0", "name": "validate_NavigatorBot_parameters_009d56a0", "evidence": "Hypothesis, not a recovered symbol. thiscall ECX=descriptor; AL=bool, other EAX bits unspecified; RET. concrete_normal_path_body: complete native SSE/x87 comparison and cleanup sequence. Complete assembly/raw bytes; docs/ROBOT_CONFIG.md; reports/robot_config.json; field callsites and offsets in reports/robot_config_evidence.json. Win32 build/CTest1/1 and ignored real-Lua fixture passed; not native-ABI/game validated. Ghidra read-only; parent owns missing starts/flow repairs and annotation."}
{"address": "009d58f0", "name": "BSP_ShipAiPathNode_AttachCornerRecord", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_search, read-only analysis. __thiscall(node), RET 0, body 009D58F0-009D5929, complete. Called at 009E32A3 and 009E32F7 on a freshly built corner node. Does nothing when node+10h is already set, node+8h is null, or the signed short at node+0Ch is negative. Otherwise 00417610 on the zone with the index plus one when the signed side byte is negative gives the corner record, stored at node+10h, and 00423190 refreshes it. The follower reads node+10h at 009E3D75 for the lateral offset. docs/SHIP_AI_PATH_SEARCH.md; reports/ship_ai_path_search.json"}
{"address": "009d5a20", "name": "BSP_ShipAiPathNode_RouteResolved", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_search, read-only analysis. __thiscall(node) -> bool in AL, RET 0, body 009D5A20-009D5A7A, complete, recursive. Is there a route from this node to a leaf across resolved edges only: a node with neither link answers true (009D5A27 and 009D5A2B), otherwise +20h is followed only when +31h is set and +24h only when +32h is, and either side answering true is enough. 009EC703 reads it as the search being done. docs/SHIP_AI_PATH_SEARCH.md; reports/ship_ai_path_search.json"}
{"address": "009d68b0", "name": "BSP_ShipAi_CircleTangentOrOffsetPoint", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_approach_update, read-only analysis. float* __fastcall(ECX = float out[2], EDX = const Circle*)(const float point[2], float clearance, int side), RET 0Ch at 009D6A3A and 009D6ABF, body 009D68B0-009D6AC1, complete. The circle is three floats: centre x at +0h, centre z at +4h, radius at +8h. side picks which tangent 009D6550 returns, index = (side == 0) ? 1 : 0 from the SETZ at 009D68C7 feeding the EBP*8 scaling. When 009D6550 fails and the query point is at least 1.0 from the centre the query point itself is used; when it is closer the routine returns centre + radius * (sin a, cos a) for a random a in [0, 2*pi) drawn from stream 1 at 009D69DD. Finally, when the clearance exceeds the chosen point's separation from the query point, 004F47B0 at 009D6A8A supplies a replacement pair and the same index picks one. docs/SHIP_AI_APPROACH_UPDATE.md; reports/ship_ai_approach_update.json."}
{"address": "009d6b40", "name": "BSP_ShipAi_SnapThrottleToProfile", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_obstacle_tables, read-only analysis. float* __thiscall(blk+4h)(float* value, float low, float high), RET 0Ch, body 009D6B40-009D6D57, complete. Both ship-AI call sites pass this = blk+4h (009F44A7 and 009F4875 LEA ECX,[ESI+4]; 009E10D9 ADD ESI,4 in 009E04E0), so the object is a 65-byte cost profile over the throttle axis at blk+4h with a bypass byte at blk+45h (009D6B4F CMP byte [EBX+41h],0). Bin i is the throttle i*0.0625-2.0: 009D6B87 FLD 16.0 (00CED9F8), 009D6B8F FLD 0.5 (00D7A280), 009D6B99 CALL 00BF7420 (CVTTSD2SI at 00BF7435), inverse at 009D6CE0 FILD/FMUL 0.0625 (00CEF290)/FSUBRP against 2.0 (00D7A308). low >= high stores the midpoint and returns (009D6B63 JC, 009D6B67); a zero byte at the current bin returns with the value untouched (009D6BDF, 009D6D48); otherwise it scans down and up for the cheapest bin, tie-breaks on distance and on the walls at 009D6CB1, writes the bin value and clamps into [low, high] at 009D6CF2. docs/SHIP_AI_OBSTACLE_TABLES.md; src/ship_ai_obstacle_tables.cpp"}
{"address": "009d80c0", "name": "BSP_ShipAi_ObstacleNodePointInNearBox", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_sector_scan, read-only analysis. char __thiscall(node)(const float* point), RET 4 at 009D8148 and 009D8154, body 009D80C0-009D8156, read whole. Sole call site 009EB8CA in 009EB660, given the hull position blk+184h: a true result drops that neighbour from the straight probe. The same oriented-box test as 009D8160 but against centre node+20h/+24h and half extents node+38h/+3Ch, sharing the axes node+28h/+2Ch and node+30h/+34h (009D80E3..009D8141). Returns false when node+68h is set (009D80C3); it does not test node+69h. docs/SHIP_AI_SECTOR_SCAN.md"}
{"address": "009d8160", "name": "BSP_ShipAi_ObstacleNodePointInAvoidBox", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_sector_scan, read-only analysis. char __thiscall(node)(const float* point), RET 4 at 009D81F2 and 009D81FE, body 009D8160-009D8200, read whole. Two call sites, 009EB8C0 and 009EBE0E, both in 009EB660 and both with the sector's probe point; a true result blocks the sector at that list index immediately (009EB925, 009EBEB3). Returns false when node+68h or node+69h is set (009D8163, 009D816D); otherwise d = point - (node+44h, node+48h) and the point is inside when |d.x*node+28h + d.z*node+2Ch| < node+5Ch (009D818D..009D81C2) and |d.x*node+30h + d.z*node+34h| < node+60h (009D81C4..009D81EB). The sign clears are the AND 0x7FFFFFFF at 009D81AE and 009D81D6. docs/SHIP_AI_SECTOR_SCAN.md"}
{"address": "009d84e0", "name": "BSP_ShipAi_ObstacleNodeChoosePassingSide", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_sector_scan, read-only analysis. float* __thiscall(node)(float* out_point, const float* observer, float reference_bearing, int other_side, unsigned char* out_flag), RET 14h at 009D8858, body 009D84E0-009D885A, read whole. Sole call site 009EBF67 in 009EB660. Bears the four corners of the node's avoid box (centre +44h/+48h, axes +4Ch/+50h and +54h/+58h, half extents +5Ch/+60h, built 009D8509..009D871B) from the observer, keeps the most positive and most negative bearing relative to the reference (seeds -pi at 00CE684C and +pi at 00D7A264, loop 009D8720..009D87B8), adds 25 degrees (00D1F6C0) to whichever side other_side names (009D87FA, 009D880F), and returns the corner of the smaller turn with *out_flag set for the max side (009D8825 FCOMIP, 009D8829 JC). 009EBF83 turns the flag into sector+28h = flag ? 2 : 1. Early out at 009D84E9 when node+68h is set: the box centre and no flag change. docs/SHIP_AI_SECTOR_SCAN.md; reports/ship_ai_sector_scan.json"}
{"address": "009d8b90", "name": "BSP_ShipAi_NeighbourClosingSpeed", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_obstacle_tables, read-only analysis. float10 __thiscall(neighbour)(const float* our_xz, int pass_side), RET 8, body 009D8B90-009D8C51, complete. One call site, 009F477E, where ECX = EDI = the obstacle sector's blocker pointer (blk + index*2Ch + 82Ch, loaded at 009F4662) and the arguments are &blk+184h (009F4775) and the sector's pass side at +830h (009F4669). Returns 0.0f when this+14h is null, when [this+14h]+5Eh is set, or when this+68h is set (009D8B96..009D8BB5, exit 009D8C49). Otherwise it takes 0092D730 on [[this+14h]+1018h] (009D8BC1) and flips the sign when the dot product of (our position - this+44h/+48h) with the forward vector this+54h/+58h disagrees with the pass side: ahead of it (dot > 0) side 1 flips, behind it side 2 flips (009D8C05..009D8C38, the 0.0f-x at 00D7A208). 009F4787..009F479D compares the result against blk+3C4h * 0.75 (00CEC9D8) and arms the one-second astern latch blk+380h above it. docs/SHIP_AI_OBSTACLE_TABLES.md; src/ship_ai_obstacle_tables.cpp"}
{"address": "009d8ce0", "name": "BSP_ShipAi_PredictTrackCrossing", "evidence": "Hypothesis, not a recovered symbol. void __thiscall(self)(void* candidate, void* other), RET 8 at 009D913F, body 009D8CE0-009D9141. Reads both units' published order slots at unit+0A98h+54h*[unit+0B40h]: heading +44h through 006BC0C0 (009D8D63, 009D8DCE), self +40h (009D8DAA), other +48h (009D8DB9). Intersects the two heading rays, exits at 009D9120 when |closing| <= 0.001f (00D7A23C at 009D8ED6), then sets self+8Ch to 1 or 2 by which range to the crossing is shorter (009D906D). Only caller 009F3E6D, in the undefined routine 009F3E30-009F3E77. Packet cc_ai_throttle_ring, read-only. docs/SHIP_AI_ORDER_CONSUMER.md"}
{"address": "009d9150", "name": "BSP_ShipAiPathNode_Init", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_planner, read-only analysis. __thiscall(node)(const float2* xz, int layer) -> node in EAX, RET 8, body 009D9150-009D91BE, complete. Constructor of the 54h-byte path node: vtable 00D214F4 at 009D915D, the point at +18h/+1Ch, the layer key at +40h, word FFFFh at +0Ch, every other field zero. Called twice by 009E3780 at 009E393E and 009E3963 on operator new(54h) results. docs/SHIP_AI_PATH_PLANNER.md."}
{"address": "009d9230", "name": "BSP_ShipAiPathNode_LinkNext", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_planner, read-only analysis. __thiscall(node)(node* next), RET 4, body 009D9230-009D92CE, complete. A null argument is a no-op (009D923C). Otherwise this->+20h = next, next->+28h = this, this->+34h = the planar length of the segment through the same x87 kernel as 00414C60 (009D9276 against the double 1e-10 at 00CE3820, then the CRT square root at 009D9286), and this->+31h set when that length is below the 1.0f at 00D7A24C. Call site 009E3973 with ECX = plan+20h. docs/SHIP_AI_PATH_PLANNER.md."}
{"address": "009d92e0", "name": "BSP_ShipAiPathNode_LinkAlt", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_search, read-only analysis. __thiscall(node)(next), RET 4, body 009D92E0-009D937E, complete. The link_plus mirror of 009D9230 BSP_ShipAiPathNode_LinkNext, instruction for instruction with +24h for +20h, +2Ch for +28h, +38h for +34h and +32h for +31h. A null argument is a no-op at 009D92EC; otherwise node+24h = next, next+2Ch = node, node+38h is the segment length through the same 00414C60 kernel, and node+32h is set when that length is below the 1.0f at 00D7A24C. docs/SHIP_AI_PATH_SEARCH.md; reports/ship_ai_path_search.json"}
{"address": "009d9550", "name": "BSP_ShipAiPathSearch_PruneToChain", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_search, read-only analysis. __thiscall(node), RET 0, body 009D9550-009D9690, complete, recursive. Called at 009EC715 on the state 3 to 4 transition. At every fork it destroys one branch through its vtable slot 0: an unresolved edge loses, a branch 009D5A20 rejects loses, otherwise the cheaper of child+3Ch plus link length plus link cost wins with a tie going to +24h at 009D959B. The survivor is moved into the +20h slot, +38h takes the 1000000.0f at 00CF87D0, +31h is forced and +45h cleared, and 009D9633-009D968B splices out every following node whose incoming link is under one unit. docs/SHIP_AI_PATH_SEARCH.md; reports/ship_ai_path_search.json"}
{"address": "009d96a0", "name": "BSP_ShipAiPathSearch_SmoothOnce", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_search, read-only analysis. __thiscall(node) -> bool in AL, RET 0, body 009D96A0-009D98BB, complete, tail-recursive. Called at 009EC72F in state 4. Removes at most one node per call: for each triple node, a, c it removes a when the path does not bend past min(10 / max(node+34h, 1), 0.05) radians toward the side a+14h names (009D985E and 009D9871), or when either leg is shorter than sqrt(9) units (009D9714 and 009D9734), which also forces node+31h at 009D98AF. Returning true sends the state back to 5 so the new shortcut edge is probed again. docs/SHIP_AI_PATH_SEARCH.md; reports/ship_ai_path_search.json"}
{"address": "009d9cc0", "name": "BSP_ShipAiPathPlan_Init", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_planner, read-only analysis. __thiscall(plan)(void) -> plan in EAX, RET 0, body 009D9CC0-009D9D32, complete; no Ghidra function starts here (INT3 at 009D9CB7-009D9CBF and 009D9D33-009D9D3F). Constructor of the 68h-byte path plan block: vtable 00D2152C, +0Ch = 1600.0f the squared capture radius, +10h = 2500.0f the squared goal-drift tolerance, +14h = 0.5f the revalidation delay, +4h/+8h = 20.0f, +30h = 1.0e7f, everything else zero. The navigator embeds two of these at +224h and +28Ch. docs/SHIP_AI_PATH_PLANNER.md."}
{"address": "009d9d40", "name": "BSP_ShipAiPathPlan_Reset", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_planner, read-only analysis. __thiscall(plan)(void), RET 0, body 009D9D40-009D9D73, complete. Releases the node list through head->vtable[0](1) at 009D9D53, then clears plan+24h, +20h, +34h, +38h and +1Ch and sets +30h to the 1.0e7f at 00CF58EC. Call sites 009E37B7 and 009E39F5 in 009E3780, 009ED558 in 009ED3E0, and the same sequence inlined at 009ED642-009ED66A. docs/SHIP_AI_PATH_PLANNER.md."}
{"address": "009d9e50", "name": "BSP_ShipAiPathPlan_RemainingLength", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_planner, read-only analysis. __thiscall(plan)(const float2* pose) -> float in ST0, RET 4, body 009D9E50-009D9F8C, complete. Walks the node graph from plan+20h for at most plan+34h + 1 steps, deciding each undecided node's +4h direction from the two link costs, falls back to plan+24h when the walk runs out, and returns that node's +3Ch plus the straight-line gap to the pose. The navigation arm reads it at 009EE6AC. docs/SHIP_AI_PATH_PLANNER.md."}
{"address": "009da250", "name": "BSP_ShipAi_RudderFromHeadingError", "evidence": "Hypothesis, not a recovered symbol. float10 __thiscall(blk)(float heading_error), RET 4 at 009DA3A2, body 009DA250-009DA3A4. The only writer of blk+1D4h inside 009F3F80 (stores at 009F44FC and 009F46A0). raw = -error / ([[blk+3FCh]+538h]+524h * 1.2, 00CEC160) clamped to [-1,+1]; negated when blk+35Ch == 2; zero below 0.4 speed (00CE65D0) and scaled by clamp(|v|-0.4,0,1) above it; negated again when the latch disagrees with the speed sign. Packet cc_ai_throttle_ring, read-only. docs/SHIP_AI_THROTTLE_TO_RING.md | Packet cc_ai_class_field: the class+524h the FLD at 009DA268 loads is no longer unnamed. 00828F20 BSP_ShipClass_DeriveTurnFields derives it once per class as 0.5 * MaxRotAngle / MaxRotAngleChangeRatio; there is no Lua key for it, and 009DA268 is its only reader in the image. For VehicleClass[20] DeRuyter the value is 0.100000 rad exactly (MaxRotAngle 0.122173, MaxRotAngleChangeRatio 0.610865), so full rudder is applied beyond 0.12 rad of heading error. See docs/SHIP_AI_CLASS_FIELD_0524.md."}
{"address": "009da4e0", "name": "BSP_ShipAi_ClearPathPlan", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ship_ai_state_steps, read-only analysis. __fastcall(blk), RET 0, body 009DA4E0-009DA58D, read complete. Releases the two owned path objects at blk+244h and blk+2ACh through their own vtable[0](1) (009DA4E9, 009DA524) and clears blk+240h/248h/250h/254h/258h/25Ch/2A8h/2B0h/2B8h/2BCh/2C0h/2C4h and the bytes +2FCh/+2FDh/+2FEh; +254h and +2BCh take [00CF58EC]. Called by both steering-mode setters, by 009DE050 (009DE082), 009DFF40 and 009E00A0. This is the body include/bsp/ship_ai_states.hpp recorded as unread. docs/SHIP_AI_STATE_STEPS.md"}
{"address": "009da590", "name": "BSP_ShipAi_GoalReachedTest", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_planner, read-only analysis. __thiscall(navigator+8h)(const float2* goal) -> bool in AL, RET 4, body 009DA590-009DA604, complete. The single arrival predicate behind vtable slot 2Ch of both navigation states. False at once when nav+2FEh is clear; otherwise true when the goal is within sqrt(6400) = 80 units of the active plan's latched goal at [nav+2F4h]+40h/+44h (the double at 00D21530), and clears nav+2FEh and returns false when it is not. docs/SHIP_AI_PATH_PLANNER.md."}
{"address": "009dab10", "name": "BSP_ShipAiState_GoalReachedThunk", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_path_planner, read-only analysis. __thiscall(state)(const float2* goal) -> bool in AL, tail JMP, body 009DAB10-009DAB1A, complete; no Ghidra function (INT3 at 009DAB07-009DAB0F and 009DAB1B-009DAB1F). Loads [state+4h]+8h into ECX and tail-jumps to 009DA590 at 009DAB16. It is slot 2Ch of the movetopos vtable 00D21628 and of the moveonpath vtable 00D21688, called at 009E5821. docs/SHIP_AI_PATH_PLANNER.md."}
{"address": "009dabf0", "name": "BSP_ShipAi_SetSpeedCommandedFlag_Unused", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_goal_vector, read-only analysis. void __thiscall(brain)(void), no Ghidra function, MOV byte ptr [ECX+0B38h],1 at 009DABF0 then RET at 009DABF7; previous routine 009DABE0 ends with RET 4 at 009DABEA and three INT3 at 009DABED-009DABEF, eight INT3 follow at 009DABF8-009DABFF. A whole-image scan finds no call, jump or 4-byte reference to it: an emitted but unused inline setter for the byte 009F4DAF reads. docs/SHIP_AI_GOAL_VECTOR.md"}
{"address": "009dae20", "name": "BSP_ShipAi_MoveToPosCommandObject", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ship_ai_state_steps, read-only analysis. MOV EAX,0E08F68h; RET at 009DAE20-009DAE25, no Ghidra function, INT3 padding from 009DAE26. It is vtable 00D21628 slot +24h, the command object the movetopos state serves, and the same value 009E5837 compares the director's +54h against. docs/SHIP_AI_STATE_STEPS.md"}
{"address": "009dae30", "name": "BSP_ShipAi_GetGoalVector_Unused", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_goal_vector, read-only analysis. void __thiscall(brain)(float out[3]), RET 4, no Ghidra function, body 009DAE30-009DAE50 (RET 4 at 009DAE4E); previous routine 009DAE20 ends with RET at 009DAE25 and ten INT3 at 009DAE26-009DAE2F, INT3 follows at 009DAE51. Copies brain+0B2Ch/+0B30h/+0B34h to the out buffer. A whole-image scan finds no reference to it. docs/SHIP_AI_GOAL_VECTOR.md"}
{"address": "009db7f0", "name": "BSP_ShipAi_GetGoalVectorViaAdapter_Unused", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_goal_vector, read-only analysis. void __thiscall(adapter)(float out[3]), RET 4, no Ghidra function, body 009DB7F0-009DB813 (RET 4 at 009DB811); previous routine 009DB7E0 ends with RET at 009DB7E5 and ten INT3 at 009DB7E6-009DB7EF, INT3 follows at 009DB814. Reaches the brain through adapter+4h, the same +4h 009DB820 uses, then copies brain+0B2Ch/+0B30h/+0B34h out. A whole-image scan finds no reference to it. docs/SHIP_AI_GOAL_VECTOR.md"}
{"address": "009db820", "name": "BSP_ShipAi_HorizontalDistanceToGoal", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_goal_vector, read-only analysis. float __thiscall(adapter)(void), RET, body 009DB820-009DB8CC. 009DB823 reaches the brain through adapter+4h and 009DB82F the unit through brain+0AA8h. Differences the unit pose +0FCh/+104h against the goal brain+0B2Ch/+0B34h (009DB871, 009DB87D), squares and sums at x87 precision with one float32 store at 009DB895, compares against the double 1e-10 at 00CE3820 and returns 0.0f at 009DB8BC or the CRT square root 00BF7030 at 009DB8A7. Callers 009E2BB4 (no Ghidra function, unread) and 009F396A. docs/SHIP_AI_GOAL_VECTOR.md"}
{"address": "009dbbc0", "name": "BSP_LinkedList_ElementAt", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_arm_tail, read-only analysis. __thiscall(list)(int index) -> void* in EAX, RET 4, body 009DBBC0-009DBBF2, complete. The list is {count at +0h, head node at +4h}; a node is {next at +4h, payload at +8h}. 009DBBC7 rejects a negative index, 009DBBCB an index at or past the count, 009DBBD4 an empty head; the walk at 009DBBDA follows +4h index times and 009DBBEE returns node+8h, else EAX = 0. Its only decoded caller is 009EEBB0 inside 009ED6B0, over the global entity list at [[00E188A8]+19CCh]+60h. docs/SHIP_AI_NAVIGATION_ARM_TAIL.md"}
{"address": "009dbcc0", "name": "BSP_ShipAi_ResolveCommandGoalPosition", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_goal_vector, read-only analysis. void __thiscall(record)(float out[3]), RET 4, body 009DBCC0-009DBD3B, sole call site 009F156B with ECX = brain+0AF8h. With record+14h zero (009DBD1C) the stored triple at record+18h is already a world position; otherwise 009DBCCE checks the object's pose byte +0C8h, 009DBCD9 refreshes through 00414DB0, and 009DBCED passes the triple as ECX with the object's matrix at +0CCh to 004142E0, so the stored triple is a point in the object's frame. docs/SHIP_AI_GOAL_VECTOR.md"}
{"address": "009dbf90", "name": "BSP_ShipAi_SetDesiredThrottle", "evidence": "Hypothesis, not a recovered symbol. Packet cc_cruise_command, read-only analysis. __thiscall(state, float), RET 4, body 009DBF90-009DBFD0, complete. blk = [state]+8; clears blk+1C8h and blk+1CCh and stores the argument clamped to [-1.0f, +1.0f] (00D7A260, 00D7A24C) at blk+1D0h. How blk reaches unit+0FC4h is unread. docs/CRUISE_COMMAND.md"}
{"address": "009dbfe0", "name": "create_navigator_robot_config_009dbfe0", "evidence": "Hypothesis, not a recovered symbol. cdecl, no arguments; EAX=allocated descriptor, publishesF8A688; RET. concrete_normal_path_body: typed allocation, constructor writes and publication; null allocation excluded by explicit storage contract. Complete assembly/raw bytes; docs/ROBOT_CONFIG.md; reports/robot_config.json; field callsites and offsets in reports/robot_config_evidence.json. Win32 build/CTest1/1 and ignored real-Lua fixture passed; not native-ABI/game validated. Ghidra read-only; parent owns missing starts/flow repairs and annotation."}
{"address": "009dd010", "name": "BSP_ShipAi_ObstacleNodeClipArc", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_sector_scan, read-only analysis, read for its contract rather than projected. char __thiscall(node)(const float* centre, float radius, float from_bearing, float* io_to_bearing), RET 10h at 009DD52E, body 009DD010-009DD530. Two callers, 009EBE36 in 009EB660 (the arc probe's per-neighbour test) and 009EF910. Returns false when node+68h or node+69h is set (009DD043, 009DD04C). Otherwise it builds the same four corners of the avoid box as 009D84E0 and 009DD540 (009DD059..009DD0DB) and clips the arc of that radius about that centre, narrowing *io_to_bearing toward from_bearing through 009D8A30. The caller turns the bearing change into an arc length by multiplying by the radius and keeps it only when it exceeds sector+0Ch (009EBE3F..009EBE7D). docs/SHIP_AI_SECTOR_SCAN.md"}
{"address": "009dd540", "name": "BSP_ShipAi_ObstacleNodeClipRay", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ai_sector_scan, read-only analysis, read for its contract rather than projected. char __thiscall(node)(const float* origin, const float* direction, float* io_range), RET 0Ch at 009DD9B8, body 009DD540-009DD9BA. Sole call site 009EB8E2 in 009EB660, the straight probe's per-neighbour test. Returns false when node+14h is null, [node+14h]+5Eh is set or node+68h is set (009DD54B..009DD568). Otherwise four support-point rejections through 009D8860 against the ray direction, its negation and the two perpendiculars, then the four corners of the avoid box (centre +44h/+48h, axes +4Ch/+50h and +54h/+58h, half extents +5Ch/+60h) clipped edge by edge against the ray by 009D8210, which narrows *io_range. The caller keeps the result only when the new range exceeds sector+0Ch (009EB8EB..009EB8F6). docs/SHIP_AI_SECTOR_SCAN.md"}
{"address": "009de050", "name": "BSP_ShipAi_SetNavigationGoal", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ship_ai_state_steps, read-only analysis. __thiscall(blk)(const float* goal2d, char keep_mode, unsigned char final_leg), RET 0Ch, body 009DE050-009DE1A6, read complete. ECX is blk = brain+8h directly (009DE055); callers do ADD ECX,8 first (009E580C, 009E1509). Ten state steps call it. It latches the goal at blk+1DCh/+1E0h, stores final_leg at blk+1E4h, forces blk+1C4h = 2 (Navigate) at 009DE091 and 009DE152, drops blk+314h/+2FDh/+2FEh when the goal moved more than 2500 squared (00D20278), and on a re-plan (goal moved more than 6400 squared, the double at 00D21530) latches blk+1E8h/+1ECh and seeds blk+1F0h with 00414C60(goal - pose). docs/SHIP_AI_STATE_STEPS.md"}
{"address": "009dfc70", "name": "CG_scalar_deleting_dtor_009dfc70", "evidence": "Existing compiler-generated identity retained. thiscall ECX=descriptor, stack flags; RET4; EAX=original pointer even after free. concrete_normal_path_body: complete canonical scalar deleting body; preserve compiler-generated identity. Complete assembly/raw bytes; docs/ROBOT_CONFIG.md; reports/robot_config.json; field callsites and offsets in reports/robot_config_evidence.json. Win32 build/CTest1/1 and ignored real-Lua fixture passed; not native-ABI/game validated. Ghidra read-only; parent owns missing starts/flow repairs and annotation."}
{"address": "009dff40", "name": "BSP_ShipAi_SetHeadingAndDropPath", "evidence": "Hypothesis, not a recovered symbol. Packet cc_ship_ai_state_steps, read-only analysis. __thiscall(brain)(float heading), RET 4, body 009DFF40-009DFF91, read complete. ECX is the brain itself (LEA ESI,[ECX+8] at 009DFF41), unlike the three setters that take &state->owner. Switches blk+1C4h to 1 clearing blk+360h/+368h, calls 009DA4E0, stores the heading at blk+1D8h, calls 00605070 on it and clears blk+1CCh. Called by 009E2020 at 009E228A and by 009E23B0. docs/SHIP_AI_STATE_STEPS.md"}
{"address": "009dffb0", "name": "BSP_ShipAi_SetDesiredSteering", "evidence": "Hypothesis, not a recovered symbol. Packet cc_cruise_command, read-only analysis. __thiscall(state, float), RET 4, body 009DFFB0-009E0011, complete apart from 009DA4E0. blk = [state]+8; when blk+1C4h is non-zero it zeroes blk+360h and blk+368h and sets blk+1C4h to 0, then calls 009DA4E0(blk) and stores the argument clamped to [-1.0f, +1.0f] at blk+1D4h. blk+1C4h is the steering mode: 0 rudder, 1 heading. docs/CRUISE_COMMAND.md"}

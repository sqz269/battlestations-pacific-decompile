{"address": "006b8da0", "name": "BSP_AwardRegistry_GetAchievementId", "evidence": "Hypothesis, not a recovered symbol. Packet award_grant_path, read-only analysis. __thiscall(this, const NativeString* name), RET 4, ECX = *(00E19900). Runs the case-insensitive std::map find at this+10h (_Myhead this+14h, comparator 00443d00) and returns the dword at node+54h, which is the value struct's +40h = the XLastAchievementID column; returns 0 when the name is not a row. Registry built by 006b9450 from Scripts\\datatables\\Achievements.lua. Call sites 004e437a and 0090ef86 both accept only 1..99."}

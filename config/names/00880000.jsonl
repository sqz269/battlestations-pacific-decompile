{"address": "00884240", "name": "BSP_MissionLua_FormatSelfKey", "evidence": "Hypothesis, not a recovered symbol. __cdecl(char* dst, const char* fmt, ...), body 00884240-00884259, eleven instructions, sole caller 00885DA0 BSP_MissionLua_PushArgumentRecord. Calls _vsprintf_s(dst, 8, fmt, va) with the literal 8 at 00884250, not a sizeof. Its caller passes the format string 00CE3A34 (\"%d\") and a 16-bit field, producing the decimal self key that indexes the global thisTable. It writes into no table: docs/MISSION_LUA_MACHINE.md's mission_lua_self_table follow-up row listed it as a populator of the self object, which this corrects. docs/MISSION_LUA_SELF_TABLE.md."}
{"address": "00884770", "name": "BSP_LuaFundamentals_GetSingleton", "evidence": "Hypothesis, not a recovered symbol. Evidence docs/LUA_STATE_OWNER.md; reports/lua_state_owner.json. No args, EAX cached0Ch object; body00884770..0088482c. Global0108ff1c, singleton lifetime manager lock, allocate0Ch, construct00b68340, register lifetime. Cached bytes source contract is documented rather than global lifecycle reimplementation. | Normal-flow VFS cache composition in bsp::VfsLuaScriptFiles::cached_fundamentals_00884770, src/vfs_lua_scripts.cpp. Successful zero-size stream is cached:00b683e1..00b6840a has no nonzero-size gate. Missing/incomplete reads throw; application-owned serialized cache omits native singleton locking/lifetime registry/allocator ABI. docs/SCRIPT_RUNTIME_INTEGRATION.md."}
{"address": "00884be0", "name": "BSP_MissionLuaHost_Initialize", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __fastcall(MissionLuaHost* this), RET, no stack arguments. Called once from 004dd5b0 BSP_Game_OnInitOnce. Allocates a 12-byte LuaMachine with 00bf681b(0Ch), constructs it with 006b8740 and stores it at this+4h (00884c34), runs the chunk \"PC=true\" (00d0e714) through 006b8ad0, installs all 560 rows of the registration table at 00e0b7b8 through 006b8610, then runs the embedded chunk named \"Scripts\\\\fundamentals.lua\" whose bytes come from the singleton at 00884770. | Run-time evidence, packet cc_exe_2f: run in process over the repository's Lua 5.1.1. Seven standard libraries, the PC=true chunk, 560 bindings of 00e0b7b8 installed as plain globals and the fundamentals chunk, followed by the DoFile global from the owner layer at 00b6a303. The stock mission script Scripts/missions/usn/usn_2_java.lua then loaded and ran, and its luaStageInit reached three bindings (CreateScript 00898750, Scoring_RealPlayTimeRunning 008b87f0, LoadMessageMap 008c61c0)."}
{"address": "00885110", "name": "BSP_MissionLuaHost_RunScriptFile", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(MissionLuaHost* this, const char* path), RET 4. Opens the path through the virtual file system singleton 0109ceec with virtual +4h and mode 2; the stream's virtual +18h gates everything. Reads virtual +30h bytes into a malloc buffer via virtual +24h, then 006b89f0([this+4h], buffer, size, path, results = 0, error_out = a stack local, mode 2). The error text is released at 0088531b without being read and the return value is discarded, so a failing mission script leaves nothing behind. ghidra flow reports the fall-through gap 008852f2..00885331 after the no-return _free at 008852ed; that tail is the stream refcount release through [00ce2220] and the native string cleanup."}
{"address": "00885350", "name": "BSP_VfsFileBlockScope_Destruct", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(FileBlockScope* this), body 00885350-008853d4. Writes vtable 00d0e6ec, then compares the live [0109ceec]+14h with the depth saved at construction in [this+4h] and, when it grew, builds the empty string literal at 00ce3a0c and calls 00bdc9b0 BSP_VFS_LeaveFileBlock on [0109ceec]. The object 00887750 builds at 00887774 and 00887e50 at 00887f0b is therefore a virtual-file-system block-nesting unwinder, not a mutex; 00887750 releases it only on the refusal path at 008877a7, 00887e50 on every path at 008880e7."}
{"address": "00885a30", "name": "BSP_MissionLuaResultSink_AppendAllAsText", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(ResultSink* this, NativeString* out), RET 4, body 00885a30-00885a64. Walks the sink's vector from [this+8h] for [this+0Ch] elements with a 14h stride and calls 008855b0(element, out) on each. Sole caller 008c8483 in BSP_LuaBinding_DebugTrap, so every named-call result in the shipped game ends up as text in the debug trap's log line. 008855b0 was not read."}
{"address": "00885da0", "name": "BSP_MissionLua_PushArgumentRecord", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(const ArgRecord* record, LuaMachine* machine). Records are 20 bytes with the tag at +4h and the value at +8h. Tag 0 lua_pushnumber (006b80e0), 1 lua_pushstring from the native string (006b8120), 2 lua_pushboolean from the byte (006b80c0), 3 thisTable[sprintf(\"%d\" 00ce3a34, short at +8h)] or nil when the short is zero, 4 pushes nothing, 5 lua_createtable (006b84b0) then walks a container, 6 lua_pushnil (006b8130)."}
{"address": "00885fb0", "name": "BSP_MissionLuaHost_RunScriptWithVariants", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(MissionLuaHost* this, const char* path, char run_variants), RET 8. Runs 00885110(path) unconditionally; when run_variants is set it asks 00bdef90 on the virtual file system singleton for the content-variant names of the path (the same expander BSP_Localization_ParseTableFile 00aa0020 uses) and runs 00885110 on each, substituting the empty literal 00f87904 for a null buffer."}
{"address": "008860b0", "name": "BSP_MissionScript_RunFile", "evidence": "Hypothesis, not a recovered symbol. Packet mission_scene_load, read-only analysis. Ghidra renders __cdecl(const NativeString* name); the call site at 004e0a38 loads ECX from game+1A08h first, so a dropped this pointer is possible and the convention is provisional. Builds 'Scripts/missions/' + name + '.lua' (the extension literal is at 00CFD2C8) with two BSP_NativeString_Concat calls and hands the result to 00885fb0(path, 1). Sole caller 004dfb70 at 004e0a3f, inside the 3_ VFS block, with the script name taken from [game+5FCh]+928h + slot*8. docs/MISSION_SCENE_LOAD.md. | Packet mission_lua_host settles the provisional convention: 008860b0 is __thiscall on the mission Lua host at game+1A08h, RET 4. ECX is saved into ESI at 008860cd and reloaded for the 00885fb0 call at 0088618e, so Ghidra's __cdecl rendering drops a this pointer. The callee 00885fb0 runs the built path plus every content variant the virtual file system reports for it. See docs/MISSION_LUA_HOST.md. | Run-time evidence, packet cc_exe_2f: reached at 004e0a3f inside 004dfb70. It built Scripts/missions/usn/usn_2_java.lua and ran one chunk with no content variant, because the executable's VFS content-suffix list at manager +48h/+4Ch is empty. The name it received carries the subdirectory, which docs/MISSION_LUA_MACHINE.md gap 2 requires; the executable derived it from the scene file's parent directory because the header pass does not fill the record's script table at +928h."}
{"address": "00886280", "name": "BSP_Vfs_EnumerateResourceList", "evidence": "Hypothesis, not a recovered symbol. ECX VFS manager, stack output list/directory/extension/flags, RET10h, returns output. Normalize directory copy then existing00bdd990; host vector replaces native list allocator. docs/LOCALE_STARTUP_INTEGRATION.md; new C++ interfaces, guarded malformed tokens, no native object ABI or gameplay claim."}
{"address": "00886370", "name": "BSP_MissionLua_RunScriptFolder", "evidence": "Hypothesis, not a recovered symbol. Compiled enumeration before source; remember compiled names before execution, length-first case-insensitive source shadowing even on compiled failure, captured candidate cleanup and normal list/vector destruction. Complete normal-flow assembly; src/global_script_folders.cpp and docs/GLOBAL_SCRIPT_FOLDERS.md. Existing VFS and Lua calls reused; native STL/SEH/ABI and game validation remain outside this projection."}
{"address": "00886900", "name": "BSP_MissionLua_RunGlobalScriptFolders", "evidence": "Hypothesis, not a recovered symbol. Same captured mission owner executes Scripts/global/ flags1 then Scripts/datatables/autoload/ flags0. Complete normal-flow assembly; src/global_script_folders.cpp and docs/GLOBAL_SCRIPT_FOLDERS.md. Existing VFS and Lua calls reused; native STL/SEH/ABI and game validation remain outside this projection. | Run-time evidence, packet cc_exe_2f: run in process from the 004dc72f site inside 004dc6a0, ahead of the mission chunk. 21 scripts ran out of Scripts/global/ and Scripts/datatables/autoload/ through the mounted providers with no chunk error, which is what makes luaStageInitMulti resolve; DoFile was called 12 times from those scripts and the mission chunk."}
{"address": "00886e00", "name": "BSP_MissionLua_BuildVariantFromStack", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(ResultSink* this, LuaVariant* out, int stack_index, int depth, int max_depth), body 00886e00-008870f6. Whole body guarded by depth < max_depth, so the third literal 00887220 forwards is a maximum table-expansion depth, not a mode selector: the table case recurses at 00886f97 with depth+1 (00886f02 LEA EBP,[EDI+1]) and the same max_depth reloaded from [ESP+F8h] at 00886f77. Switches on 006b7ec0 lua_type: nil and full userdata both write tag 6, boolean goes through 006b8000 into 008869f0 and becomes the number 0.0 or 1.0, lightuserdata 006b80a0 into 00886b30 tag 4, number 006b8020 into 008869d0, string 006b8060 into 00886a10, table walks lua_next 006b85b0, function becomes the literal <Function> at 00d0e78c. At depth == max_depth the record keeps tag -1."}
{"address": "00887220", "name": "BSP_MissionLua_CollectStackResults", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(ResultSink* this, int count, int mode). Reads the top count values off the Lua stack (or all of them when count < 1) into 20-byte variant records built by 00886e00 and appends them to the sink. mode is 2 for chunk results (00885110, 00884be0) and 4 for named call results (008879b3); what the mode selects inside 00886e00 is not established. | Packet cc2_named_call_args: the third argument is not a mode. It is forwarded to 00886e00 as max_depth, whose whole body is guarded by depth < max_depth and which recurses on table members with depth+1, so 2 (chunks) and 4 (named calls) are maximum table-expansion depths. ECX is saved into EDI at 0088723d: [this+4h] is the LuaMachine, this+8h the {begin,size,capacity} vector of 14h-byte variants. count < 1 collects the whole stack from index 1 (0088726b). It pops nothing; the caller restores the top at 008879bf."}
{"address": "008874c0", "name": "BSP_MissionLuaPendingResults_Destruct", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(PendingResults* this), body 008874c0-008874fe, the non-deleting destructor of a 16-byte holder {vtable 00d0e6f0, ResultSink* at +4h, int base at +8h, char armed at +0Ch}. When armed it refetches the host through [[00e188a8]+1a08h], calls 00887220(sink, -1 - base, 4) and pops -base slots with 006b85f0, so it collects N results and drops N+1 slots. Unreached: the vtable constant 00d0e6f0 is written only at 008874c7 and 00887507, both inside this destructor and its scalar-deleting twin 00887500, and 008874c0 has no xrefs. Emitted but never instantiated."}
{"address": "00887560", "name": "BSP_MissionLuaDeferredCall_CopyArguments", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(LuaVariantVector* dst, const ArgVector* src), body 00887560-008875a4. resize(0) through 006b88e0, reserve src->count through 006b87f0, then 006edf00 for each of src->count records at src->begin + i*14h. The queued arguments are a deep copy, so a caller may release its records as soon as 00887e50 returns. Called at 008881ed on node+18h."}
{"address": "008875d0", "name": "BSP_MissionLuaResultSink_Construct", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(ResultSink* this), body 008875d0-008875e6, eight instructions: [this] = vtable 00d0e798 and +4h..+10h zeroed. The sink is 14h bytes: vtable, LuaMachine* at +4h that 00887750 stores at 008877dd, and a {begin, size, capacity} vector of 14h-byte variants at +8h. Only two sinks exist in the image (ghidra xrefs): 008c83da in BSP_LuaBinding_DebugTrap and 004276ad in FUN_00427190."}
{"address": "008876b0", "name": "BSP_MissionLuaDeferredCall_Destruct", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(DeferredCall* this), body 008876b0-008876e9. The whole body is 006b88e0(this+10h, 0) then 00bf6989 free of that vector's buffer. It never releases the two NativeString buffers at +0h and +8h: no 0041dd20 and no 00419cc0/00bd1510 pair appears. Harmless at 008881fb, where the record is the empty temporary, but 00888230 calls it on the filled node before freeing it, so a deferred call appears to leak its name and self key. Static reading, no run-time evidence."}
{"address": "00887750", "name": "BSP_MissionLuaHost_CallNamed", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(MissionLuaHost* this, const NativeString* self_key, const NativeString* name, const ArgVector* args, int stack_first, int stack_last, ResultSink* results, char force), RET 1Ch. Refuses when force is clear and game+1FE4h == 2. Pushes the global \"debugtrap\" (00d0e79c) as the pcall error handler, raises [00f87900], splits the name on '.' with 00bd20a0, resolves segment 0 with lua_getfield(LUA_GLOBALSINDEX) and the rest with lua_pushstring/lua_gettable(-2)/lua_remove(-2), optionally prepends thisTable[self_key] (00ce7494), pushes the 20-byte argument records through 00885da0 and the forwarded stack range with lua_pushvalue, adjusts game+1A18h by the stack top across lua_pcall(nargs, LUA_MULTRET, errfunc), hands the results to 00887220 with mode 4 and restores the saved top. | Packet cc2_named_call_args: all seven stack arguments attributed from their frame slots (return address at [ESP+2Ch], argument N at [ESP+2Ch+4N], and [ESP+34h+4N] after PUSH EBP at 008877bf and PUSH EDI at 008877c6): 1 self_key read at 008878c7, 2 name at 0088785a, 3 args at 0088790f, 4 stack_first at 008877e8, 5 stack_last at 00887812, 6 results at 008877c0 and 008879a5, 7 force at 00887782. Argument 7 is read once and 0088784a then reuses its slot for the debugtrap error-handler index, read back at 00887971. nargs (EBX) is cleared at 00887780 and reaches 1 only inside the self-key block at 008878dc, so it starts at 0 without a self key. stack_first == 0 is the sentinel at 008877f2 that disables forwarding and also suppresses the normalisation of stack_last; a negative index becomes top + index + 1."}
{"address": "00887b30", "name": "BSP_MissionLuaHost_CallNamedForced", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(MissionLuaHost* this, self_key, name, args, stack_first, stack_last), RET 14h. Tail into 00887750 with results = 0 and force = 1, so it runs on the calling thread and ignores the game+1FE4h lifecycle gate. Only caller is 0045f520. | Packet cc2_named_call_args: the thirteen instructions reload a1..a5 from [ESP+4h]..[ESP+14h] and call 00887750(a1, a2, a3, a4, a5, results = 0, force = 1) with ECX untouched. The force byte buys only the 00887782 gate, so the call runs while game+1FE4h == 2; the other two differences (never queues, never collects) come from not consulting the frame job pool and from the literal 0 in argument 6."}
{"address": "00887c30", "name": "BSP_MissionLuaCallQueue_InsertNode", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_named_call_args, read-only analysis; docs/MISSION_NAMED_CALL_ARGS.md. __thiscall(list* this, node* next, node* prev, const DeferredCall* value), body 00887c30-00887caa. operator_new(2Ch), stores the two links at +0h/+4h and copy-constructs the 24h-byte payload at node+8h through 00887a10, returning the node. The 2Ch settles the deferred-call node layout: 8 bytes of links then self key at +8h, name at +10h, argument vector at +18h, stack_first at +24h and stack_last at +28h. Sole caller 00888161 in BSP_MissionLuaHost_CallNamedThreadSafe."}
{"address": "00887e50", "name": "BSP_MissionLuaHost_CallNamedThreadSafe", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(MissionLuaHost* this, self_key, name, args, stack_first, stack_last), RET 14h. Returns immediately when game+1FE4h == 2 (00887e73, retested at 00887ec8). Asks BSP_FrameJobPool_GetSingleton 004c1130 and the pool's virtual +10h whether this is a worker thread; if so it queues the call at 00888101 through 00887c30 using the host's +8h/+Ch queue under the critical section taken via [00ce2218]. Otherwise it copies [0109ceec]+14h into a local scope object with vtable 00d0e6ec and runs the 00887750 body inline. 40 callers. | Packet cc2_named_call_args: the queue path is a std::list push_back, not a lock on the Lua state. this+8h is the list, this+0Ch its head and this+10h its size. 004c1570 (00888101) lazily creates an 8-byte singleton over 00f878fc whose +4h is a CRITICAL_SECTION, entered through [00ce2218] at 00888120 and left through [00ce2210] at 0088820d. 00887c30 allocates a 2Ch node; the payload is then filled: self key at node+8h with 00425f40, name at node+10h with 0041dd40 and 00bf7680, arguments at node+18h deep-copied by 00887560, stack_first at node+24h and stack_last at node+28h, the order the drain 00888230 confirms at 00888264. The two stack indices are copied verbatim and replayed on the draining thread. The inline path's scope object is a VFS block unwinder (00885350), not a mutex."}
{"address": "00888230", "name": "BSP_MissionLuaHost_DrainQueuedCalls", "evidence": "Hypothesis, not a recovered symbol. Packet cc_fixed_step, read-only analysis. __thiscall void(luaHost), RET at 008882a8, body 00888230-008882a8. Caller 00875bb0 at 00875e55 with ECX=[[00e188a8]+1a08h], the mission Lua host of docs/MISSION_LUA_HOST.md; also 00874d00. While [this+10h] is non-zero it pops the front of the list at [this+0ch] and calls BSP_MissionLuaHost_CallNamedThreadSafe 00887e50(node+8h, node+10h, node+18h, node[9], node[10]), then destroys the node through 008876b0 and frees it: the once-per-fixed-step drain of cross-thread Lua calls. Ghidra shows a spurious return after _free at 00888294; the raw listing loops. docs/FIXED_STEP_FANOUT.md"}
{"address": "00888760", "name": "BSP_LuaObject_ReadVector3", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_entity_lookup, read-only. __fastcall(ECX = out float[3], EDX = LuaObject) plus one float stack argument it never pops: the function ends RET 0 at 00888899, so the caller's float stays on the stack and becomes the fourth dword of the next call. 00944FD0 depends on that at 009452B2..009452CA, where the yaw pushed before this call is 004C6BE0's fourth argument. The body opens two LuaObjects (00B65F50), iterates with 00B66420 and stores the three values at [out], [out+4] and [out+8] (0088884E, 00888852, 0088885D); EAX = out. docs/LUA_BINDING_GENERATE_OBJECT.md."}
{"address": "00888aa0", "name": "BSP_ObjectHandle_FromLuaTable", "evidence": "Provisional descriptive name, not recovered symbol. Complete body: Lookup literal Ptr at00CFAD08 with existing00B67800; convert returned temporary using00B662D0; capture EAX inESI across00B67700 destruction. Real Lua light/full userdata semantics, not pointer-to-pointer or numeric conversion. Native ECX=LuaObject; EAX=user pointer; RET. src/object_handle_resolvers.cpp; docs/OBJECT_HANDLE_RESOLVERS.md. Win32 build and existing2/2 tests passed; one real-Lua/native-entry fixture passed. Explicit table views; not native ABI/game/world-loading validated."}
{"address": "008890f0", "name": "BSP_Game_IsScriptLoadActive", "evidence": "Hypothesis, not a recovered symbol. Packet mission_lua_host, read-only analysis; docs/MISSION_LUA_HOST.md. __thiscall(Game* this), RET, returns a byte: xor eax,eax / cmp [ecx+644h],eax / setg al, so the depth is positive. No Ghidra function starts here; the body runs 008890f0..008890fb and the enclosing candidate is 008890b0. The same test is inlined in the Lua bindings Loading_Start 008cc850 (008cc93a) and Loading_Finish 008c77f0 (008c78d4), which skip their bodies while a load owns the loading screen."}
{"address": "0088a330", "name": "BSP_Entity_SetThinkScriptName", "evidence": "Hypothesis, not a recovered symbol. __thiscall(entity, const char* name), RET 4. The callee of the SetThink binding. Registers the entity with the global at 00F89ABC through 0088A240 when it had no name and one is being set, frees any previous name, duplicates the new one through 00438E40 and clears the byte at entity+1DCh. Ghidra leaves 00888A360..0088A36C undisassembled after the free at 0088A35B and the decompiler shows a spurious return; the real bytes are ADD ESP,4 / MOV dword [ESI+1D8h],0, so the pointer is nulled and the routine falls through. docs/LUA_BINDING_CORE.md. | Correction to the line above: the undisassembled gap is 0088A360..0088A36C, not 00888A360; the transcription slipped a digit."}
{"address": "0088b1b0", "name": "BSP_MissionEntity_FindByName", "evidence": "Hypothesis, not a recovered symbol. Sole caller 00898E30. Takes a NativeString by value as two stack dwords (caller makes room with 00898F53 SUB ESP,8; callee reads the data pointer at 0088B1CA MOV EDI,[ESP+0x28]). Walks the bucket lists at [[00E188A8]+19CCh]+1Ch+n for the kind indices of the jump table at 0088B310 (range -6..0x47), skipping candidates whose flag bytes at +5Ch..+5Eh and [+5Ch] disagree, and compares each candidate's (*vtable+10h)() name against the argument; a null argument matches a null name. Returns the entity pointer in EAX or zero. docs/LUA_BINDING_ENTITY.md. | Packet cc2_entity_lookup, read-only. Corrected and completed: the list bases are at world+18h with stride 0Ch indexed by kind+6, and 1Ch is the base's head field (producer 00484540 lays a base out as {count, head, tail} and a node as {prev, next, value}). The four gate bytes are +5Ch (must be non-zero), +5Dh, +5Eh and +60h (must be zero); 00922FD0 sets +5Eh/+5Dh and clears +5Ch when an entity is released. 0088B1E0 CMP EBX,0x47/JA is unsigned, so the six negative kinds and 48h..5Ah are never searched; the fourteen searched kinds are 00h 09h 12h 13h 14h 15h 16h 3Bh 3Eh 3Fh 40h 41h 44h 47h (selector bytes at 0088B318), which are unit instances, PlaneSquadronGen, LandConvoy, NavPoint, Landscape, Path, CameraPath, SpawnPoint and six unnamed runtime classes. MovieCamPos and MovieCamLookat (kinds 3Ch/3Dh) are not findable. The callee frees the NativeString argument through 00BD1510(pool, data, length+1, 1). docs/LUA_BINDING_ENTITY_LOOKUP.md."}
{"address": "0088b840", "name": "BSP_LuaObject_IsVector3Table", "evidence": "Hypothesis, not a recovered symbol. Packet cc2_entity_lookup, read-only. __thiscall(LuaObject), RET 0, bool in AL. 0088B864 calls 00B661B0 BSP_LuaObject_IsTable and bails when it is false, then iterates the table and the tail at 0088B974 CMP EDI,3 requires exactly three entries before returning BL. Sole use read: 009451B7 inside 00944FD0 BSP_LuaBinding_GenerateObject, where a true answer makes argument 1 the position table. The element type is not checked here; 00888760 reads the three values as floats. docs/LUA_BINDING_GENERATE_OBJECT.md."}
{"address": "0088bec0", "name": "BSP_LuaBinding_Log", "evidence": "Hypothesis, not a recovered symbol; the Lua global name is genuine. Handler of the mission Lua binding `Log` (row 00e0b7b8 of the 560-entry table at 00E0B7B8, name pointer 00d11110, docs/LUA_BINDING_TABLE.md); lua_CFunction ABI, __cdecl int(lua_State*). Body had no Ghidra function; defined from disk-verified bytes (reports/lua_binding_handlers_function_definitions.json)."}
{"address": "0088c620", "name": "BSP_LuaBinding_SETLOG", "evidence": "Hypothesis, not a recovered symbol. lua_CFunction __fastcall(lua_State* in ECX), RET 0, returns 0. Row 00E0B7E0, global SETLOG, 203 scripts. Verified against the listing, 0088C620..0088C74B, 25 instructions: it opens the borrowed owner (0088C6E4) and the call frame (0088C6F9), takes the result count (0088C70A) and closes. It reads no argument and touches no game state; the only effect is the one-time 'luakod' log-category init at 00F87938 guarded by 00F8793C. docs/LUA_BINDING_CORE.md."}
{"address": "0088e560", "name": "BSP_LuaBinding_SetMotionBlurParams", "evidence": "Hypothesis, not a recovered symbol. lua_CFunction __fastcall(lua_State* in ECX), RET 0. Rows 00E0C908 and 00E0C910 both point here, under SetMotionBlurParams and SetMBP; the handler cannot tell them apart for the same reason as 00896A90. Reads three numbers from arguments 0, 1 and 2 and stores a block whose third field is (arg1 - arg0) / arg2. docs/LUA_BINDING_ALIASES.md."}
